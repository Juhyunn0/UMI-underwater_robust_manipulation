# P3 — Research + Verify outputs

<!-- Populated when P3 (/research-verify) lands. One block per question:
     ## YYYY-MM-DD <question>
     verified (+sources) · rejected (+why) · provenance -->

## 2026-06-23 — [RESOLVED ✅] Isaac Sim 5.0 + RTX 5090 학습 완주 (IOMMU 진단은 부분적이었음)
실측 결과(구현/디버깅 세션): 위 verifier들의 "IOMMU P2P가 부팅 hang의 원인" 결론은 **부분적으로만 맞음**.
- `amd_iommu=off` 적용 → kit 로그에 `IOMMU is disabled` 뜨고 P2P 검증은 사라졌으나 **부팅은 여전히 `omni.physx Using CUDA device ordinal 0`에서 행**(GPU util 0%, CPU 스핀). 즉 IOMMU P2P는 표면 증상, 그 아래 **PhysX GPU init 행**이 본질.
- `CUDA_MODULE_LOADING=EAGER` 무효. `--device cpu`는 부팅·학습 정상(전체 파이프라인 검증, 문제=GPU PhysX로 격리).
- **진짜 해결 = 드라이버를 CUDA-13(580/595)에서 CUDA-12.8 브랜치 R570(570.211.01-open)으로** (IsaacLab #3448: PhysX-on-Blackwell엔 R570 권장). Isaac Sim5.0/torch cu128/warp 전부 CUDA12.8 빌드라 드라이버도 12.8이어야 함. 결과: 2048 envs·400 iter·~19.7M steps GPU 학습 완주(보상 0.78→~76).  [UNVERIFIED: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- warp 1.8.1 swap·amd_iommu=off는 유지(각각 cuDeviceGetUuid·P2P 해결). 최종 구성/명령은 `ISAAC_AUV_SETUP_RUNBOOK.md` 상단 + [[isaac-auv-env-feasibility]].

## 2026-06-23 — Isaac Sim 5.0 부팅 hang fix로 제안된 "TiledCamera→Camera" 적대적 검증
researcher: web(IsaacLab/IsaacSim issues, NVIDIA/warp CHANGELOG, docs.isaacsim) · 적대적 cross-check

- **판정: unlikely.** 제안 fix(TiledCamera 대신 표준 Camera)는 **부팅 hang과 무관**. IsaacLab#4951이 명시: TiledCamera hang은 **런타임**(env.reset/TiledCamera.update)이며 "warp 커널은 sm_120에서 정상, hang은 omni.replicator tiled 렌더 파이프라인". 부팅은 카메라 코드 도달 전이라 이 우회는 부팅을 succeed시키지 못함.
- **부팅 hang의 실제 원인(직전 2026-06-23 entry와 일치):** Kit는 log-init **이전이 아니라** "Simulation App Startup Complete" 도달 후 PhysX CUDA context에서 stall. 마지막 줄 "IOMMU is enabled. Running CUDA peer-to-peer bandwidth and latency validation". IsaacLab#1764: IOMMU-on P2P 검증이 ~362s(0.2GB/s, 49ms) hang. NVIDIA 공식: 베어메탈 Linux는 IOMMU-on P2P 미지원→disable 권장.
- **실측 재확인:** `/proc/cmdline`에 iommu off 플래그 없음(아직 미적용). 단독 `warp 1.8.1 wp.init()` 로컬 **exit 0/~수초**(sm_120, mempool, Toolkit12.8/Driver13.0) → warp 자체 hang 아님. extscache `omni.warp.core-1.7.1+lx64/warp/config.py` version=1.8.1, `warp.1.7.1.bak` 백업 존재 → swap 정상.  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]
- **warp 버전 검증:** cuDeviceGetUuid/error36은 **warp 1.8.1 "Fixed"에서 해결**(GH-851 "Fix driver entry point error for cuDeviceGetUuid caused by using an incorrect version"). 1.8.1 changelog에 Blackwell/CUDA13/init-hang/mempool 언급 **없음** → 1.8.1은 cuDeviceGetUuid만 고침, 부팅 hang은 별개(=IOMMU).
- **실제 해법:** GRUB에 `iommu=off`(또는 `intel_iommu=off`/`amd_iommu=off`) 추가 → update-grub → reboot. headless여도 동일(P2P 검증은 headless 무관).
- **drive 주의:** Blackwell 부팅 crash는 **R590(595.xx) 브랜치**가 알려진 원인(forum scenedb)이나, 이 머신은 580.167.08(R580 validated) → 드라이버는 원인 아님.

sources: github.com/isaac-sim/IsaacLab/issues/4951 · /issues/1764 · /issues/2081 · github.com/isaac-sim/IsaacSim/issues/229 · github.com/NVIDIA/warp CHANGELOG(GH-851, v1.8.1) · forums.developer.nvidia.com/t/.../366252 · docs.isaacsim 5.1 requirements

## 2026-06-23 — Isaac Sim 5.0 headless 부팅 hang (RTX 5090) 진단·fix 적대적 검증
verifier (적대적) · 대상: warp GSP-vs-JIT 진단 probe 제안

- **반증(핵심):** 제안된 "GSP firmware halt vs soft JIT stall" 이분법은 이 머신엔 부적합. 실측: `nvidia-smi` 즉답(GPU idle P8), `dmesg` Xid **0건**, NVRM 에러 없음 → GSP class(#1111: nvidia-smi도 hang·SSH 끊김·hard power-cycle, 45분/3000req zero-gap 필요)와 정반대. 즉 제안의 진단 분기는 항상 "recoverable JIT/boot stall"로 떨어져 무의미.
- **반증(전제):** 프롬프트의 "Kit이 로그조차 안 쓰고 log-init 이전에 hang"는 디스크와 불일치. 오늘자 로그(`kit_20260623_092407.log`, 366KB/2132줄 등 다수) 전부 **"Simulation App Startup Complete" 도달 후** PhysX CUDA context(`omni.physx ... Using CUDA device ordinal 0`)에서 멈춤. warp 시작은 98ms로 정상.
- **검증(실제 stall point):** 멈춤 직전 마지막 줄이 `IOMMU is enabled. Running CUDA peer-to-peer bandwidth and latency validation`. NVIDIA 공식: 베어메탈 Linux에서 IOMMU-on이면 CUDA P2P 미지원→이 검증이 수 분 hang(보고 306s, BW 0.2GB/s) → **IOMMU off 권장**(intel_iommu=off/amd_iommu=off, /etc/default/grub+update-grub+reboot). `/proc/cmdline`에 off 플래그 없음 확인.
- **warp 1.8.1 swap 검증:** cuDeviceGetUuid/CUDA error36은 warp<1.8.1 × R580 드라이버 mismatch가 원인, **warp 1.8.1에서 fix**(NVIDIA/warp#940/#851, maintainer shi-eric). 유저 swap 정상(extscache `omni.warp.core-1.7.1+lx64/warp/config.py` 및 pip warp 모두 version 1.8.1; 캐시에 sm120.ptx 생성됨). 즉 warp는 더 이상 원인 아님 → 부팅 hang은 warp가 아니라 **IOMMU P2P 검증**.
- **판정:** 제안 fix = **unlikely**(부팅을 succeed시키지 못함; 진단만 하고 GSP 결론도 이 머신엔 오진). 실제 해법은 IOMMU 비활성화.

## 2026-06-22 — BlueROV2 회전 added mass: von Benzon/Wu 출처·수치 검증 (P2 후속)
researcher: control-theory-advisor · verifier: verifier (적대적)

- **Verified:** von Benzon et al. 2022 (JMSE 10(12):1898, DOI 10.3390/jmse10121898) 실재, **Fossen은 저자 아님**(원 리뷰의 "von Benzon & Fossen"은 오기 — Fossen은 모델 프레임워크). Eidsvik(2015) 경험식 오차 **회전 30–100%** / translational 10–20% → 회전 added mass는 order-of-magnitude only.
- **Uncertain(2차 출처로만 확증):** 회전 added mass ≈ Kp'0.189 / Mq'0.135 / Nr'0.222 kg·m²(von Benzon/Eidsvik). 여러 인용 논문이 동일 set 재현하나 **원문 표를 1차로 확인 못 함**(verifier도 PDF fetch 불가). researcher가 댄 provenance(RG fig 366613202=Hadi/Sensors)는 verifier가 **반증** — 그 그림은 PMC9824147(다른 논문) 소유.
- **Rejected:** 원 P2 주장 패턴 "roll 최소(Kp'≈0.07), Nr'≫Kp'" → **틀림**. 실데이터는 **pitch 최소·yaw 최대**(Nr'0.222 > Kp'0.189 > Mq'0.135, ~1.6×). 0.07/0.18/0.22 triple은 어느 데이터셋과도 불일치.
- **추가:** 경쟁 데이터셋 **Nr'≈0.40**(BR forum/Wu Heavy lineage) 존재 → 문헌이 회전값에 불합의(0.40을 arXiv:2405.00269에 귀속한 것도 약함).
- **판정/권고:** **지금 [0.12,0.12,0.12] 바꾸지 말 것.** ① 후보 0.189/0.135/0.222가 2차 확증뿐 ② 방법 오차 30–100%라 1.6× 비등방이 noise 내부(false precision) ③ 경쟁셋 0.40과 우열 근거 없음 ④ 우리 translational은 MarineGym set([5.5,12.7,14.57])이라 회전값만 이식하면 식별 혼합. 비등방 원하면 von Benzon **set 전체** 이식+order-of-mag 명시; 진짜 해법은 **자체 system ID**(free-decay/pendulum; docs/REAL_HYDRO_VERIFICATION.md).
- **provenance(핵심수치):** Kp'0.189/Mq'0.135/Nr'0.222 → von Benzon 2022(Eidsvik)로 *추정*, 1차 표 미확인·2차 인용으로만 확증(신뢰도 medium).

sources: vbn.aau.dk PDF · doi.org/10.3390/jmse10121898 · mdpi.com/2077-1312/10/12/1898 · arXiv:2405.00269 · BR forum 13065 · Wu2018 Flinders

## 2026-06-22 — Isaac Sim 4.5.0 시스템 요구사항 + RTX 5090(Blackwell sm_120) 지원 여부 (P3)
researcher: web (docs.isaacsim/forums/GitHub) · cross-verified across official docs + maintainer threads

- **Verified(공식 4.5.0 requirements page):** GPU min RTX 3070(8GB) / rec RTX 4080(16GB) / ideal RTX Ada 6000(48GB). **RT-core 필수**("GPUs without RT Cores (A100, H100) are not supported"). RAM 32/64GB, disk 50GB→500GB SSD, Linux driver **535.129.03**, Windows 537.58, Ubuntu 20.04/22.04 + Win10/11, Python **3.10**.
- **CRITICAL — Blackwell/RTX 50:** Isaac Sim **4.5.0은 Blackwell sm_120 공식 미지원**. 공식 4.5.0 req page에 50-series/Blackwell 언급 전무. 번들 PyTorch **2.5.1**(cu118/cu121)은 sm_50..sm_90까지만 컴파일 → sm_120 커널 없음 → RTX 5090에서 `CUDA error: no kernel image is available for execution on the device`. Kit/Warp/PhysX/NVRTC도 sm_120 미포함(IsaacLab #2652). NVIDIA: "compatibility problems between Blackwell GPUs and the Kit version in Isaac Sim (4.5.0 and 4.2.0)."
- **First official Blackwell support = Isaac Sim 5.0** (PyTorch 2.7.0+cu128, sm_120 across components; 4.5.0 blurry/noisy render bug도 5.0에서 fix). Maintainer: real fix는 5.0, 4.5 워크어라운드 아님.
- **4.5.0 워크어라운드(불완전):** `pip install --pre torch torchvision --index-url .../nightly/cu128` → PyTorch 텐서는 돌지만 viewport 렌더 noisy/voxel(Ubuntu), Kit/PhysX 레벨 sm_120 부재는 안 풀림. 권고: RTX 5090이면 **5.0+ 쓸 것**.
- **Install 방법 2가지:** ① binary/workstation(번들 python.sh 3.10) ② pip `isaacsim[all,extscache]==4.5.0 --extra-index-url https://pypi.nvidia.com` into conda py3.10.
- **sources:** docs.isaacsim 4.5.0 requirements.html, IsaacLab disc #1888/#2869, issue #2652, forum 336193, IsaacLab pip_installation(v2.1.0).

## 2026-06-22 — Learning-to-Swim(warplab/isaac-auv-env) 내 PC 실행 가능성 + Runbook (P3, 워크플로우 7-agent)
researcher: 병렬 web(Isaac Sim/Lab reqs) + repo deep-dive(general-purpose) · verifier: 4-claim 적대적

- **Lab↔Sim 짝(verifier REJECT):** "Isaac Lab 2.2.0 = Isaac Sim 4.5.0 공식 짝"은 **틀림**. 2.2.0의 1차 짝은 **Isaac Sim 5.0**(SIGGRAPH 2025 동시 GA); 4.5는 *backward-compat*만. 4.5.0의 canonical 짝은 Lab **2.1.0**. 단 repo README가 2.2.0+4.5.0을 직접 검증(commit 0d520b2)했으므로 그 조합 자체는 유효.
- **Blackwell 우회 핵심(verifier):** 4.5.0 번들 torch 2.5.1=sm_90까지(→`no kernel image`). **Lab 2.2.0는 torch 2.7.0+cu128 pin → sm_120 포함**이라 PyTorch 레벨 에러는 우회되나 Kit/PhysX/Warp sm_120 부재는 잔존. RTX 5090 정도(正道)=**Isaac Sim 5.0 + Lab 2.2.0**(Py3.11).
- **repo deep-dive(코드 확정):** task id `Isaac-WarpAUV-Direct-v1`(__init__.py:20) ✅; **symlink명 반드시 `isaac-auv-env`**(entry_point 일치; 폴더명 bluerov2_issac_paper/READMEdocker오타 isaac-warpauv-env 아님). Migration 4.0→4.5 **fully-applied**(obs/act/state space warpauv_env.py:70-72, articulation_enabled=False warpauv.py:17-19). requirements.txt 없음(deps=IsaacLab+pandas/cv2). 차량=WARPAUV 22.7kg 6-thruster(≠BlueROV2). 제공 weights는 단일파일→`play_poshold.py --play_checkpoint`(cv2 GUI 필요), 표준 play.py는 logs/rsl_rl/warpauv_direct/<run> 레이아웃 요구.
- **HW 판정:** RTX5090/32GB/60GB RAM/625GB/Ubuntu22.04/drv595 → Blackwell 외 전부 ✅. **결론=조건부 가능**(Sim5.0 권장). conda **robust 재사용 금지**(Py3.14·torch없음). → Runbook: `ISAAC_AUV_SETUP_RUNBOOK.md`. [memory: isaac-auv-env-feasibility]
- **sources:** IsaacLab disc #3167/#1888, release_notes, NVIDIA GA blog, forum 336193/337381, pytorch #159207, repo files.

## 2026-06-23 — Isaac Sim 5.0 Kit headless STARTUP HANG on RTX 5090 Blackwell sm_120 (warp가 성공적으로 로드될 때만) (P3)
researcher: web(IsaacSim/IsaacLab issues + NVIDIA forums + open-gpu-kernel-modules) · 증상-매칭 적대적 검증

- **증상 구분(핵심):** 우리 행은 *Kit 로그 쓰기 이전* pre-init 행(main thread hrtimer_nanosleep, ~24% CPU, no crash, **omni.warp.core가 warp를 성공 로드할 때만** 발생). 가장 유명한 IsaacLab **#4951 TiledCamera 행은 런타임**(omni.replicator tiled 파이프, 100% CPU)이라 *부팅 행이 아님* → 직접 원인 아님. scenedb 부팅 **크래시**(forum 366252, IsaacSim #651)는 *행이 아니라 segfault* → 별개.
- **가장 근접 원인(가설, medium):** sm_120에서 warp의 첫 GPU 명령/JIT가 **GSP firmware/driver 레벨에서 stall**. open-gpu-kernel-modules **#1111**: 580-open(580.126.20) Blackwell GB202에서 **silent hard hang, Xid/NVRM 로그 0** — nanosleep·무출력·무크래시 패턴과 일치. warp 1.7.1=`cuDeviceGetUuid`(IsaacSim #229), 1.14=API 에러 → **1.8.1만 init 성공**하나 그게 sm_120 첫 디바이스 호출을 유발해 stall 트리거.
- **구체 워크어라운드(부팅 통과 우선순위):** ① `WARP_CACHE_PATH`로 cache 분리 + 단독 `python -c "import warp;warp.init();warp.force_load()"`로 **sm_120 커널 사전 빌드**(부팅 중 JIT 차단) ② Kit에서 **omni.warp.core 비활성** (`--/app/extensions/disabled/...` 또는 headless .kit에서 제거) 후 별도 standalone warp 사용 — warp 로드가 트리거이므로 ③ RTX 렌더 우회: `--/renderer/enabled=false` 류 + enable_cameras=False면 `isaaclab.python.headless.kit` ④ 드라이버: 595.xx branch는 Blackwell+Isaac 공식 known-bad(forum 366252, NVIDIA staff) → 580.65.06(5.1 validated) 계열 유지/맞춤 ⑤ `CUDA_MODULE_LOADING=EAGER` / RtPso async(`--/rtx/...async...`) 비활성으로 비동기 컴파일 대기 제거.
- **확증 fix(타인):** scenedb 크래시는 **드라이버 다운그레이드(591.74)로 해결**(forum 365335/366252) — 행과 다른 실패지만 드라이버-branch 민감성 입증. TiledCamera 행 확증 fix=**TiledCamera→Camera 교체**(#4951).
- **권고 first-try:** warp 커널 사전 캐시 + Kit에서 omni.warp.core 제거(부팅에 warp 불필요; 학습 텐서는 torch가 담당) → 그래도 행이면 `--/renderer/enabled=false`로 렌더 vs warp 이분.
- **sources:** IsaacLab #4951/#4961/#2483, IsaacSim #229/#651, forum 366252/365335/370054, open-gpu-kernel-modules #1111, Isaac Sim 5.1 known_issues.html.

## 2026-06-23 — Isaac Sim 5.0 Kit boot-hang on RTX 5090(sm_120): warp 버전 진단 (P3)
researcher+verifier (web/gh API 적대적 검증)

- **환경 확인(ground):** `env_isaaclab`에 omni.warp.core-1.7.1+lx64 번들 warp을 1.8.1로 교체(`warp.1.7.1.bak` 백업 존재). pip `warp`도 1.8.1. Kit가 import하는 건 ext 내부 `warp/`(extension.toml path=".").
- **Verified:** cuDeviceGetUuid(CUDA err 36)는 warp **1.8.1**(commit 91fcd4d)에서 수정 — 메인테이너 shi-eric, warp#940. R580 드라이버+구 warp 조합 버그.
- **Verified:** `warp.types.array`는 **1.10.0**부터 깨짐(types.py가 5786줄→51줄 thin shim, `array` 클래스 미재export). 1.8.1·1.9.1은 real class 보유(types.py:1848/2249). 즉 omni-glue 호환 창 = **1.8.1–1.9.1**. (유저가 1.14에서 본 break는 실은 1.10.0부터.)
- **Verified:** warp **1.9.0**부터 CUDA 13 빌드 지원(드라이버 13.0 매칭). 공개 태그에 **v1.8.2 없음**(1.8.1→1.9.0) — IsaacSim 5.1의 "omni.warp.core 1.8.2"는 NVIDIA Kit 내부 라벨, pip `warp-lang==1.8.2` 불가.
- **Verified(권위):** IsaacLab #4951 메인테이너 — Isaac Sim 5.1.0은 **580 계열 드라이버 필수**(예 580.65.06), R590/CUDA13 불가. IsaacSim 5.1은 omni.warp 1.7.1→1.8.x로 올림.
- **판정/권고:** 부팅 행은 warp가 sm_120에 처음 접촉할 때의 init/mempool/커널캐시 stall로 추정(omni.replicator TiledCamera 행과는 다른 코드패스). 1차 시도: `warp-lang==1.9.1` 교체(types.array 유지+CUDA13). 보조: `WARP_CACHE_PATH` 지정+precompile, `wp.config.enable_mempools_at_init=False`, `verify_cuda` off. 상한선 1.9.1 초과 금지(omni glue 깨짐).
- **sources:** github.com/NVIDIA/warp issues#940, /releases(v1.8.1,1.9.x); IsaacLab#3477,#4951,#2483; IsaacSim#229; warp CHANGELOG; IsaacSim 5.1 release notes. [memory: isaac-auv-env-feasibility]

## 2026-06-23 — 제안 fix 적대적 검증: "Isaac Sim 5.1.0(warp 1.8.2 번들)로 올리면 5090 headless 부팅 행 해결" (P3)
researcher+verifier (web: NVIDIA docs/forums + warp CHANGELOG + IsaacSim/IsaacLab issues)

- **판정: UNCERTAIN** (likely-works 아님). 깨끗한 버전-매칭 스택 + 호환 드라이버라 시도는 합리적이나, fix의 **인과 주장이 근거 박약** — "5.1의 1.8.2가 행을 고친다"는 입증 안 됨.
- **핵심 반증(warp 1.8.2 ⊅ 추가 Blackwell fix):** cuDeviceGetUuid(GH-851)·NVRTC-unsupported-arch PTX(GH-858) 두 Blackwell fix는 **warp 1.8.1**에 들어감(공식 CHANGELOG, main=1.8.1에서 끝남). 유저는 **이미 1.8.1 패치 완료**. 5.1의 omni.warp.core 1.8.2는 1.8.1 위 **Z-bump bugfix**일 뿐, 별도 Blackwell *행* fix 문서화 없음. → fix의 전제("5.1이 fixed 1.8.2를 번들")가 유저가 이미 가진 것과 동급.
- **5.1은 행을 새로 고치지도, 재현하지도 않음(중립):** ① 5.1.0 TiledCamera/replicator **행 여전히 존재**(IsaacLab #4951, RTX5090 sm_120) — 단 런타임 렌더 파이프, 부팅 행 아님 ② 5.1.0 rtx.scenedb.plugin **부팅 크래시** 다수 보고(RTX 5070Ti/5080/5090/4090; forum 366252) — 단 주로 **595/R590 branch**. 유저 증상(hrtimer_nanosleep, pre-Kit-log, warp 로드 시)과 정확히 일치하는 5.1 사례 **무**.
- **드라이버는 OK:** 5.1.0 공식 Linux 검증 드라이버 = **580.65.06**(requirements.html). 유저 580.167.08 ⊇ 충족. 595/R590는 Isaac+Blackwell 공식 known-bad. → 드라이버 이유로 5.1 막히진 않음.
- **호환성 리스크:** 공개 태그에 v1.8.2 없음(1.8.1→1.9.0) → `pip install warp-lang==1.8.2` 불가. fix는 isaacsim==5.1.0.0 extscache로 omni.warp.core 1.8.2를 받는 경로라 이 함정은 회피하나, Lab 2.3.* 페어링·task 코드(4.5→5.0→5.1 마이그) 재검증 필요.
- **권고:** 5.1 업그레이드를 만능 해결책으로 보지 말 것. 먼저 **무비용 진단**(부팅에 warp 불필요): Kit에서 omni.warp.core 비활성 + warp 커널 사전 precompile(`WARP_CACHE_PATH`) + `--/renderer/enabled=false`로 render-vs-warp 이분(앞 6/23 엔트리). 이게 행을 통과시키면 5.1 불요. 5.1로 가더라도 행 원인이 다른 코드패스면 그대로 재현 가능 → blind upgrade 비권장.
- **sources:** docs.isaacsim 5.1.0 release_notes/known_issues/requirements.html; NVIDIA/warp CHANGELOG.md(GH-851/858 @1.8.1), /releases; IsaacLab #4951/#4961/#3477; IsaacSim #229; forum 366252. [memory: isaac-auv-env-feasibility]
- 2026-06-23 [verifier×3 workflow] Q: train 명령(`--num_envs 2048 --max_iterations 400 --seed 1 --headless`)이 Learning-to-Swim 논문 학습과 시드만 다른가? → REFUTED. iteration·보상·PPO 하이퍼파라미터·env 설정은 전부 커밋된 논문 기본값과 동일(env가 심볼릭링크라 byte-identical). 그러나 "시드만 다르다"는 부정확: (1) 논문 기본 시드=42(--seed 생략 시), 우리 기존 76짜리 run이 이미 seed 42였음 → 이미 논문설정 그대로 돌려 76; (2) warpauv_env.py:155의 무조건 `torch.manual_seed(0)`이 --seed 효과를 부분 무력화; (3) 결정타 — 논문은 Isaac Sim 4.5.0, 우리는 5.0(+RTX5090/R570/CUDA12.8/TF32) → 시드 무관하게 물리궤적·가중치 발산, 비트/궤적 재현 불가. 결론: 설정(방법론) 재현은 충실하나 "동일 정책"이 아니라 "비슷한 성능분포"가 목표여야 함. [memory: isaac-auv-env-feasibility]
- 2026-06-24 [experiment-diagnostic-analyst] Q: 여러 시드 학습(seed 42/1/2/3/4) 성능 평가 → 5개 run이 TensorBoard reward 곡선상 비트 단위 동일(maxdiff=0.00). 시드 무효 실측 확정(warpauv_env.py:155 torch.manual_seed(0)이 원인). 성능을 가른 건 iteration뿐: reward 76(400it)→93(800it)→피크 97.5(@1347)→90.8(1600, 피크후 하락). 논문 95-100 도달 = 정체 원인은 시드/Sim버전이 아니라 짧은 학습(400it). 최고 저장 체크포인트=model_1250.pt(reward 96.2). 과제성능(model_1250 vs 원본 model_399): 자세오차 중앙값 73°→29°, 위치 0.235→0.206m, 엄격성공(≤0.1m·≤10°) 0%→0.26%(여전히 정밀 station-keeping은 못함). 권고: 진짜 시드 ablation 하려면 line155 제거 필요; 배포엔 model_1599 아닌 model_1250. [memory: isaac-auv-env-feasibility]  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

## 2026-06-26 연안/조석류 0.4 m/s가 현실적인가 (해류 DR 범위 0~0.4 m/s 정당화)
- **Verified**:
  - von Benzon et al. (2022) JMSE 10(12):1898 — BlueROV2 오픈소스 벤치마크 시뮬레이터, 해류를 n-frame 상수 비회전류로 모델(Assumption 5), 사용 벡터 (0.3536, 0.3536, 0) m/s = **크기 0.5 m/s** [ResearchGate fig8 caption; PDF vbn.aau.dk/.../jmse_10_01898.pdf]. (verdict: substance verified; "0.5"는 성분 0.3536의 합성 크기)
  - Gabl et al. (2020) Data 5(3):57 — FloWave BlueROV2 실험, 해류 **상한 1 m/s**까지 sweep (대표 범위 0~1 m/s) [doi:10.3390/data5030057]. (ceiling verified; 중간 step값은 미확인)
  - NOAA: "strong tidal currents ... **eight knots or more**" (~4.1 m/s) [oceanservice.noaa.gov/facts/current.html — tutorial 페이지 아님].
  - Alderney Race 극단 조류 ~5 m/s [arXiv:2407.03827 + Phil.Trans.R.Soc.A reviews].
- **Uncertain**: BlueROV2 max forward speed — vendor GitHub spec=1 m/s(2kn), 신형 datasheet=1.5 m/s(3kn), 벤더 내부 불일치(PDF 본문 직접 확인 못함).
- **Rejected**: "BlueROV2 station-keeping 한계 ~1–1.5 kn(0.5–0.77 m/s)" — 특정 수치 출처 추적 불가(포럼/리셀러 lore, 검색LLM이 질의 echo). "벤더 공식 운용해류 한계는 미공개"라는 framing만 유효.
- **Provenance(0.4)**: soft engineering default("typical coastal/pool currents", docs/07_DISTURBANCES.md) → 문헌 검증 결과 **typical sheltered/coastal로 방어 가능**(von Benzon 0.5 m/s 벤치마크 바로 아래, Gabl 0~1 m/s의 하위 sub-band, 차량 authority 내), 단 **worst-case 아님**(강조류 1~5 m/s 범위 밖, 명시 필요). [memory: current-dr-range-justification]

## 2026-06-28 임펄스 "kick" 외란이 물리적으로 현실적/적절한가 (워크플로우 P3, 21 agents)
- **종합 판정**: kick은 어떤 단일 환경외란(난류·와류·슬래밍·항적·테더)의 충실한 모델이 **아님**. 그러나 다족로봇 push-recovery의 수중 아날로그로서 **강건성/회복 시험으로는 타당**. "관측기가 보상하는 외란"이 아니라 "회복 시험"으로 별도 보고할 것.
- **Verified(숫자)**:
  - 실제 연안/조석 난류 외력 = O(0.1–10 N) (sim 자체 계수 Xu_dot=5.5, Xuu=18.18로 재유도), kick 20–50N보다 1–2 자릿수 작음. 시간척도 초~수십초(적분 timescale ~6s, Milne 2017 RSPA), 0.15s 아님 → kick은 ~10–300배 짧음.
  - 조석 난류강도 TI ~6–10% (Thomson et al. 2012 IEEE JOE 37(3):363-374) — tens-of-% 는 tail.
  - 와류박리: 1–5N, 주기 3–8s(St·U/D), 임펄스 아님. 슬래밍: 완전침수(3m)에서 소멸.
  - DOB/ESO 설계는 d_dot≈0 가정(Chen et al. 2000 IEEE TIE 47(4):932-938; Do&Nguyen 2018 IEEE Access). 프로젝트 EAOB도 w_dot=0(eaob.py) → 0.15s 임펄스는 가정 최대 위반 → d_hat가 깔끔히 상쇄 못함(eval_dp.py도 "0.15s kicks not rejected"라 기록).
  - push-recovery 프로토콜(랜덤 시각·수평 위주 짧은 force pulse)은 표준 강건성 벤치마크 (arXiv 2407.04224 PA-LOCO, 2104.14534, 2203.01148).
- **Questionable(약점)**: ① 무토크(COM 적용) — 실제 임펄스(충돌·테더 스내치)는 off-COM이라 yaw/pitch 토크 유발, 빠진 게 가장 큰 결함 → 결과가 낙관적. ② 20–50N 크기·0.15s 지속은 환경외란 anchor 없음(엔지니어링 default), contact/worst-case probe로만 정당.
- **권고**: kick 유지하되 (1) "unmodeled impulsive perturbation/push-recovery test"로 재라벨(난류라 부르지 말 것), (2) 회복축(peak excursion+settling time)으로 별도 보고, (3) 최고가치 업그레이드=off-COM 적용으로 토크 부여, (4) 환경 realism은 kick 키우지 말고 colored gust velocity(von Karman/Dryden, tau~L/U~1–30s, TI~6–10%)를 water-velocity에 합산, (5) 옵션 상수 bias 케이스로 관측기 정상상태 상쇄 시연. [memory: kick-disturbance-realism]
- 2026-07-12 [workflow: 6 finders + 3 verifiers (P3)] Q: BlueROV2 Heavy에 Newton gripper + MarineSitu C3 부착 시 행렬(질량/관성/added mass/drag)을 어디서 구하나, 기존 구현 존재? → 기성 "페이로드-수정 행렬"은 어디에도 없음(AquaBot·UMI-U는 QYSEA+model-free 파라미터 미공개; BlueROV2+Alpha5 UVMS 오픈스택은 암 파라미터 비공개/식별만; MuJoCo ROV+그리퍼 전무). 방법론 컨센서스: 강체·부력은 벤더 질량으로 결정론적 합성(평행축), added mass/drag 증분(축당 수%~20%)은 공개 세트 간 30~100% 편차 미만이라 관행상 유지+문서화(DNV-RP-C205 build-up). 스펙 검증: gripper 524g/수중267g(구버전 616g 함정), C3 1700g/430g(Reef 스토어 공개). → heavy_gripper 변종으로 구현 완료. [memory: heavy-gripper-variant]
- 2026-07-19 [workflow: 3 researchers + 9 verifiers (P3)] Q: C3 실장착 위치를 Onshape에서 심으로 옮길 때 onshape-to-robot 쓸만한가 → 적합. v1.8.2(2026-03) MuJoCo 출력 1급 지원; 어셈블리에 `frame_<name>` mate connector를 달면 MJCF `<site>`(pos/quat)로 정확한 pose가 나옴(base body frame = 어셈블리 원점); 정적 어셈블리(FASTENED만)도 깔끔히 export. 단 `<inertial>`은 CAD 유래 fullinertia를 무조건 씀 → 절대 채택 금지(우리 hydro는 diag 필수, 검증된 합성 관성 유지), pose+mesh만 추출. 플랜 B: 어셈블리 단일 STL export(individual-parts 체크 해제, Y-up 체크 해제 — 다이얼로그 설정이 sticky라 매번 확인). [memory: onshape-c3-pose-pipeline]
- 2026-07-20 [build] C3 camera 방향 확인 + heavy_c3 변종: (1) C3 광축은 전방-수평이 맞음(평면 광학창=authored −Z가 mount에서 base_link +X=선체 반대쪽 향함; 둥근 캔=−Z=선체쪽). "방향 이상"은 빈 씬 검정 렌더 탓, POOL_TAGS 바닥 렌더로 해소. (2) 사용자 요청 "Onshape에 있는 것만 반영"→ 그리퍼(아직 CAD에 없음) 제외한 heavy_c3=heavy+C3 변종 신설: compute_payload_inertia.compose_c3/buoyancy_c3, gen_c3_variant.py, rov_model 등록, BlueROVHeavyC3.yaml, scene_bluerov_heavy_c3_tags.xml, test_heavy_c3.py 전부 통과(13.2kg, −3.1N, 카메라3 전방, 그리퍼 액추에이터 없음, PID 0.0cm). heavy_gripper는 그리퍼 CAD 추가 시 향후 config로 유지. [memory: onshape-c3-pose-pipeline, rov-model-variants]
- 2026-07-20 [build/debug] 사용자가 C3 렌더 90° 뒤틀림을 정면 뷰 비교로 적발 → 근본원인: MuJoCo 컴파일러는 mesh 주축 재정렬을 geom pos/quat에 합성함(컴파일된 quat = q_xml⊗mesh_quat 수치 확인). XML geom pose는 저작 원본 메쉬에 적용 → 구운 메쉬는 quat 불필요(identity). conj도 mesh_quat도 둘 다 틀렸었음. 수정: quat-less geom + 생성기 빌드타임 렌더-pose 검증(_verify_mesh_geoms). Onshape mate는 무결(재-export 바이트 동일). 스크린샷 정리(30개 삭제, 5개 폴더), C3 색 Onshape 외관 반영. [memory: mujoco-mesh-quat-convention]
- 2026-07-21 [workflow: 5 investigators + 4 adversarial verifiers] Q: compare_20260720_221845(새 GAINS_HEAVY 첫 full compare, 10쌍×5모드) 5질문 분석 → calm 격차=MPC 코너 과도(edge 동등 0.20 vs 0.14 cm; effort-페널티 가설 반박), C-offset 2.73→DOB 0.17(절대속도-drag 미모델), C≈CD(drift rate 0.03 N/s는 EAOB에 DC; 노브=σ√(2/τ)), dobmpc CW>CDW=단일 blowup 노이즈(p≥0.34), 코너 스파이크=일반 MPC 과도+wave 상호작용(이득 86→63%, 역전 없음), 극단꼬리=acados n_fail blowup(KNOWN_ISSUES 등재) [memory: compare-20260720-sweep-findings]  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]
- 2026-07-21 [workflow: 3 investigators + 1 adversarial verifier] Q: n=200 compare_20260720_230025에서 dobmpc CW/CDW 코너 이탈 원인 → 10-pair 결론 4개 전부 재현(200-pair가 byte-identical 상위집합); 기하=outside corner-rounding(구leg overshoot+새leg lag 합력, 89%)+안쪽 cut 클래스(10%, 꼬리의 77%, 해류 inward-broadside 정렬); 코너×파랑 ×1.29 초가산; 큰 내부침투=seed-0 공통 파랑그룹의 lap7/8 V3 이벤트(181/400 run이 t=200-210s 피크, 단일실현 아티팩트)+최심부는 n_fail 증폭(>40cm 23/23 fail); n_fail census 217run/289회, mpc 14% vs dobmpc 7.7%(EAOB가 OCP 안정화); CW/CDW 순서 뒤집힘(3.33 vs 3.41)으로 노이즈 확정(dobmpc 한정, pid/mpc는 작지만 체계적) [memory: compare-20260720-sweep-findings]
- 2026-07-21 [workflow: 2 investigators + 1 adversarial verifier] Q: preview-ON 첫 실행(compare_20260721_111349, 50쌍)에서 dobmpc 코너 이탈이 왜 여전한가 → preview 효과 확정(짝비교, pid=byte-identical 대조군): NONE 2.09/2.12→1.06(PID 1.38 역전, 전 5모드 radial_rms 우위), 코너 부호 39/39 outward→39/39 inward(선행 컷, 이론 예측 그대로), CDW 3.46→2.66(48/50 개선), n_fail 10-13→1-2, >40cm 꼬리 소멸; 잔존 이탈=참조 문제 아님 — >20cm 사건 62/62가 seed-0 wave-group 위상고정(g1 50-80s, g2 200-260s 88% + 2차 그룹) 또는 n_fail, 사건 직전 오차 <1cm, lap-7 V3 paired 피크 ON/OFF 불변 → 남은 레버 = 외란 preview(w_hat ZOH)/harmonic EAOB/자세결합(최악 70.8cm는 pitch 70°), authority는 sat_freq=0이라 증거없음; 단 no-fail 코너 피크는 +1.5~2.5cm(선행 컷의 대가), mpc엔 신규 210cm blowup [memory: mpc-reference-preview]
- 2026-07-29 [workflow: 4 researchers + 24 adversarial verifiers, + 실측 벤치] Q: C3(MarineSitu)를 Madrona/BlueOS RTSP 우회하고 데스크톱에서 DepthAI로 직결해 저지연 RGB-D를 받을 수 있나 → 가능, **컬러 지연 3000 ms → 38 ms(p50), 15 요청→15.00 실측, 드롭 0%**. 하드웨어 실측 확정: C3 = **OAK-D-W-POE**(CAM_A IMX378 고정초점 / CAM_B·C OV9282, baseline 7.5 cm, EEPROM 캘리브 존재, IR 프로젝터 없음, bootloader 0.0.28). 핵심 제약은 **XLink-over-PoE 실효 천장 ≈91.5 Mbit/s**(서로 다른 4구성이 86/91.7/91.1/91.6로 수렴 → 링크 천장이지 연산 한계 아님) → 실제 fps는 요청값이 아니라 frame_bytes로 정해짐. 최대 레버는 **디바이스측 MJPEG**(960x540서 9.1→19.1 fps, 250→123 ms; 실측 압축 6:1이라 초기 10:1 추정은 대역폭 절반 과소평가 → MJPEG_RATIO에 실측값 고정). 발견·수정한 실제 버그 3건: (1) ColorCamera는 **미연결 preview도 검증**해서 isp 1/4(480x270)가 기본 300x300 preview에 걸려 파이프라인 시작 실패 → setPreviewSize(32,32); (2) 설정 오류를 연결 오류로 오진 후 3×12s 재시도 → PipelineConfigError로 분리(_is_connection_error); (3) `--duration`이 12초 부팅을 포함해 짧은 런이 잠식됨. 위험 발견: **conda base는 python 3.12.11 + depthai 3.5.0(v3)** 이고 v3 `Pipeline(createImplicitDevice=True)`가 생성만으로 디바이스를 열어 **Madrona가 쓰는 C3를 낚아챌 수 있음** → import 시 2.x 강제 거부 가드 추가. 또 `Device.getDeviceByMxId`는 {UNBOOTED,BOOTLOADER}만 검색해 점유 중 카메라를 "없음"으로 보고 → `XLinkConnection.getDeviceByMxId(mxid, ANY_STATE)`로 교체. Madrona는 컨테이너가 떠 있어도 카메라가 BOOTLOADER(유휴)일 수 있어 이번엔 끄지 않고 붙었음(kraken API로 stop/start 도구는 제공, --yes 필수). 산출물: `c3_camera/`(discover_c3/c3_stream/c3_bench + 재사용 패키지 + 카메라 없이 도는 테스트 23개). [memory: c3-direct-depthai-client]  [UNVERIFIED×4: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]
- 2026-07-29(2) [workflow: 4 researchers + verifiers (verify 중) + 하드웨어 실측] Q: C3 직결 + ArduSub MAVLink로 SLAM용 RGB-D 데이터셋 수집기를 만들 수 있나 → 완성·실측 검증(`c3_collect.py`, TUM RGB-D). **C3에 온보드 IMU 있음 = BNO086 fw 3.9.9**(이전 "IMU 미표기" 추정 정정) → 카메라 강체+이미지와 **동일 dai.Clock 도메인**이라 VIO 1순위. 타임라인 최대 발견: **dai.Clock.now() ≡ time.monotonic()(CLOCK_MONOTONIC, 8µs 이내)** → 카메라·카메라IMU·MAVLink가 변환 없이 이미 한 시계, Unix 변환은 상수 오프셋 하나. 브리프 정정 3건: (1) **depth_scale 5000 → 1000**(5000은 TUM Kinect 고유값; mm 데이터에 쓰면 깊이가 5배 작아지고 그럴듯해 보여 발견 안 됨), (2) 병목은 디스크가 아니라 **PoE 링크**(NVMe 10 GB/s vs 링크 11 MB/s; 무손실 4스트림 상한 480x270서 11.8 fps), (3) **QGC가 UDP 14550 점유** → SO_REUSEPORT는 복제가 아니라 분배라 QGC 패킷을 훔침 → BlueOS 2차 엔드포인트(14551) 또는 MAVLink2Rest(무설정, 샘플링). IMU 실측 규칙 2개: 켠 센서 중 **가장 느린 레이트가 전부에 적용**(rotvec 같은 레이트면 무료, mag는 100 Hz로 전체 캡), 그리고 **배칭이 손실 결정** — 4스트림 동시 녹화서 batch=1이면 200 Hz 요청→130 Hz(35% 손실), **batch=10이면 199.5 Hz 손실 0**(원인=메시지당 XLink 오버헤드). 테스트로 잡은 실버그 4건: (a) `pair_mode=latest`라 프레임당 ~3 bundle을 다 써서 파일명 중복·덮어쓰기·인덱스 비단조(8 fps인데 26 Hz 기록, left/right 159 vs rgb 516) → primary seq dedup + pair_mode=timestamp; (b) **멀티 writer 스레드가 완료 순서대로 인덱스를 써서 TUM 인덱스 비단조** → frame-index reorder buffer(+실패 tombstone, close 시 flush); (c) research 모드가 depth 640x360/RGB 480x270로 **1:1 위반** → `depth_match_color`; (d) accel seq는 정상인데도 건너뜀(953중 277이 delta 2) → gyro seq로 연속성 판정(허위 드롭 제거). 미해결 2건 KNOWN_ISSUES 등재: 디바이스에 **IMU-카메라 extrinsic 없음**(T_imu_cam 미지), **정지 |a| 11.8 m/s²**(중력 대비 +20%). 산출물: c3_collect/c3_dataset_check/dataset/imu/mavlink_log/blueos_endpoint/control + README_DATASET.md, 오프라인 테스트 41개. [memory: c3-slam-dataset-collector]  [UNVERIFIED×3: 산출물 없음/예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- 2026-08-03 [workflow: 3 designers + 3 builders + 3 verifiers (P3)] Q: C3의 세 가지 미결 질문(해상도x fps 프론티어 / depth 정확도·최소거리 / 인코더 화질)을 실측으로 답할 도구가 있나 → 세 도구 완비. (1) `c3_bench.py`에 **--depth-size를 진짜 스윕 축으로 승격**(+mjpeg-quality/bitrate/keyframe), 인코더가 못 보는 축은 fold해 24→9런; 리뷰가 잡은 실버그 3건 수정 — 실패 행이 timestamp 말고 구분 불가(요청 크기를 미리 채움), **--append가 컬럼 수 다른 CSV에 조용히 3칸 밀린 행을 씀**(header 대조 후 rc=2 거부), depth 미스트리밍 시에도 depth-size가 곱해짐. (2) `c3_depth_accuracy.py` 신규(1 invocation = 1 rung, `--analyze`는 카메라 없이 재실행): 진리값 비교는 centre-pixel이 아니라 **robust plane fit의 수직거리 rho**(1 m ROI 폭 499 mm라 5° tilt면 ±22 mm — 재려는 오차보다 큼), 원점 offset c는 가정하지 않고 `rho = a*T + c`로 **피팅**(a는 훗날 수중 refraction scale). 리뷰가 blocker 2건 적발 — 희소 ROI에서 `np.argmax` 빈 배열 크래시로 **완료된 캡처가 통째로 날아감**, 그리고 **quantisation-limited rung이 gate 전부 초록으로 통과**(2 m/400p dZ=141 mm, 벽이 한 disparity bin에 들어가 resid 0.00·inlier 1.00인데 bias +28 mm) → `resid < dZ/6` 경고 추가. (3) `c3_encode_quality.py` 신규: **사용자 가설("H.26x가 대역폭을 푼다")은 현 운용점에서 틀림** — 컬러 MJPEG는 링크의 14%뿐이고 depth uint16이 86%라 컬러를 0으로 만들어도 fps 이득 0; 인코더는 **컬러 해상도를 올릴 때만** binding. 판정 지표는 PSNR이 아니라 **ORB inlier rate**(실측: 압축 8.7 kB까지 밀어도 ORB count는 1833→1816로 안 움직이는데 inlier는 1.000→0.827). 리뷰 major 4건 수정, 그중 둘이 결론을 바꿈 — **h26x는 호스트 디코드 경로 탓에 코덱 무관 상한 PSNR 45.60/SSIM 0.9962/ORBinl 0.9156**(손실 0인 bit-exact 스트림으로 측정)이라 상단 비트레이트는 디코더를 읽는 것, 그리고 **DECISION 한 줄이 산포를 안 보여 동전던지기가 결론처럼 읽힘**(마진 +0.0042 vs sd 0.0089) → UNRESOLVED 판정 추가. 카메라 없는 테스트 46→140개(46+58+36) 전부 통과, venv에 pytest 없어 베어 러너 사용. [memory: c3-direct-depthai-client]  [UNVERIFIED×4: 산출물 없음/예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- 2026-08-03(2) [build/docs] Q: 위 세 테스트를 사용자가 처음부터 끝까지 따라 할 운용 문서가 필요 → `c3_camera/TESTING.md` 작성(한국어 산문 + 영어 명령). 순서 근거: T1이 가장 싸고(물리 셋업 0, 13 combo = 7분) 나머지의 운용점을 정하며, **T1의 mono_res 선택이 T2의 최소거리를 직접 구속**(400p MinZ 298.9 mm vs 720p/800p 597.7 mm [2026-08-05 정정: 실측 300 / 600.2 mm — rectified pair는 CAM_C fx 760.28을 물려받는다] — 두 배라 720p를 고르면 T2의 130/185/280 mm rung이 전부 구조적 실패); T2와 T3은 **같은 벽을 재활용**(AprilTag 시트 = T3의 정적 텍스처 장면)이라 리그를 두 번 안 세움; T3은 유일하게 `--offline` 재채점이 되므로 마지막. 각 테스트마다 literal 명령 + 예상 wall-clock + 물리 셋업 + **답을 담은 컬럼** + 나쁜 결과 읽는 법 + 체크리스트 표. 판정식을 문서에 박음: `hold(f) = color_fps>=0.98f AND depth_fps>=0.98f AND 양쪽 drop_rate<=0.005 AND (color_lat_p95-p50)<=10ms` — **color_fps만 보면 "20 요청에 19.1 달성"을 성공으로 오독**하지만 실제로는 seq gap 뚫린 스트림이라 균일한 15 fps보다 SLAM에 나쁘다. 정직한 한계를 테스트마다 명시: T1은 이 케이블/스위치에서만 유효 + est_mbps가 --mjpeg-quality에 무반응 + 인코더 행끼리 latency/drop 비교 불가(큐 정책 비대칭), T2는 **물이 아니라 센서를 잰다**(전부 공기 중, 수중은 미보정 시 스케일 ~1.33배) + mono 수동노출 경로 부재, T3은 **reference가 same-frame이 아님**(median stack, RANK 전용). 검증: 문서의 31개 명령 전부를 argparse에 통과시키는 하네스 작성(C3Source 스텁 + --dry-run 강제, 하드웨어 미접촉) → 30/31 통과(1건은 아직 없는 캡처 디렉터리를 가리키는 --offline로 정상), 그 과정에서 내가 쓴 잘못된 bash 루프(`--label ${ARM%% *} $ARM` → 위치인자 중복) 1건과 discover_c3 상태 표기 오류를 발견해 수정. 결과 반영 규칙을 config.py 기본값 후보 표로 마감(depth_size/isp_scale/extended/mjpeg_quality/MJPEG_RATIO + c3_collect의 color_encode는 **source.py:429 getFrameType 버그가 고쳐지기 전엔 none 유지**). [memory: c3-direct-depthai-client]  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]
- 2026-08-03(3) [workflow: 7 deep-readers + 3 adversarial verifiers + 1 synth] Q: OAK-D W 핸드헬드 UMI 데이터 수집 파이프라인을 짜기 전, UMI_underwater + iPhUMI가 실제로 뭘 요구하나 → 3개 관문 질문 소스 확정. (1) **zarr 계약**: `zarr_adapter.py:42-58`이 강제하는 건 `data/`·`meta/`·`meta/episode_ends`(누적 EXCLUSIVE, 정수 dtype 필수 — `DS:409 .tolist()`에 int() 캐스트 없음) + **'camera' 부분문자열이 들어간 키 최소 1개**뿐. 이름이 literal이 아니라 substring scan(`:54`)이라 **`data/camera0_main_depth`가 카메라로 등록됨**(실측: available_cameras에 depth 포함, get_frame_shape가 (64,1,48) 반환). `camera_data = list(cameras.values())[0]`(`:66`)는 정렬 첫 키 → HWC `data/camera`가 뽑히면 `frame_shape`가 (W,3,H)로 오독(정사각 112라 은폐됨). 실행 규칙: **항상 `--camera-key camera_1`(CHW (T,3,112,112))**. `data/tracked_gripper_pct`는 adapter엔 optional이나 `add_cotracker_to_zarr.py:220-222`가 `sys.exit(1)`로 강제. 극성 = **1.0 open / 0.0 closed**(`:201`, `PROD:813-815`). (2) **depth zattrs**: 읽히는 필드는 정확히 `anchored_min`/`anchored_max`/`normalized` 셋뿐이고 **attr 이름 `depth_anything_v2`는 계약이 아님**(`DS:167-173`이 dict-valued attr 전수 스캔 + flat 최상위도 허용, zarr 2.16.0 실측). 사용자 브리프의 "없으면 경고만" 은 **절반만 맞음** — trainer 경로(`TR:171-176`)는 HARD ValueError, dataset 폴백만 경고 후 정규화 skip. 진짜 조용한 파괴자는 **`normalized` 누락**: `.get(...,False)`(`DS:169`)가 affine을 scale=local/global → 1/global_range로 뒤집어 shipped `global_max 0.32258`에서 **×3.10 게인 + clamp 포화**(실측 (0,0.4993)→(0,1.0)), 경고 0. 추가 지뢰: trainer는 raw 문자열로 키잉(`TR:98`)하고 dataset은 `str(Path(...))`로 조회(`DS:197`) → **경로 끝 슬래시 하나로 정규화가 꺼짐**(실측 True→False). (3) **goal 이미지**: 별도 배열/키 **없음**(`goal_key` grep 0건) — 같은 `base_key` 배열의 다른 프레임(`DS:551-556`), 채널축 concat(goal이 뒤), target은 current 인덱스에서만 읽음. `goal_end_random`의 `min()`(`DS:506`)은 **증명 가능한 dead code**라 규칙은 단일식 `goal ~ U{max(current, episode_end-50), …, episode_end-1}`; window 50 프레임 이하 에피소드는 경고 없이 `episode_random`으로 퇴화, 1프레임 에피소드는 goal==current. val goal도 매 epoch 재추첨(`DS:555` 무조건 호출) → best_val 선택이 확률적. 하드웨어측 확정: **depthai 2.32.0.0에 `DepthAlign.RECTIFIED_LEFT=1` 존재**(실측 열거) — c3_camera의 `depth_align="left"`는 CameraBoardSocket 오버로드라 **raw left 시점이지 rectified-left 격자가 아님**(`pipeline.py:59-63`), 새 정렬 모드 필요. postProcessing speckle/temporal/spatial/threshold/decimation + `algorithmControl.disparityShift` 전부 2.32에 존재(실측). MinZ = fx_rect·B/nd가 **mono_res로 결정** → 400p 300 mm / extended 150 mm, 800p는 2배. 재사용 발굴: `warp_cam1_to_cam2.py`(K1/dist1↔K2/dist2 14계수 rational + plane_z remap)가 **육상 rectified-left를 C3 수중 투영으로 워프하는 기성 도구**, preprocess config의 `overlay.mask_path`가 그리퍼 마스크 오버레이 기성 경로. iPhUMI 실체: `get_gripper_width_offset`(`GU:164-179`)은 Euclid 거리도 [0,1]도 아닌 **두 tvec의 부호 있는 X차(미터)**, 스무딩 없음(repo 전체 savgol/medfilt grep 0건), 보간은 interp1d 1회, `estimatePoseSingleMarkers` 아닌 **solvePnP + dist=0**(rectified 전제), DICT_4X4_50/16 mm/id 0·1. cv2 4.11엔 estimatePoseSingleMarkers가 이미 제거됨(실측). 극성이 UMI-U(`width_append.py:704-711`, 거리 감소=열림)와 **정반대**라 리그별 실측 필요. [memory: umi-oakd-handheld-pipeline]  [UNVERIFIED×2: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- 2026-08-04 [workflow: 9 auditors (verify+synth는 세션한도로 실패)] Q: "+28 mm bias"가 합성 예시였다면 다른 수치도 그런가 → **26개 문서 1028개 수치 주장 전수 감사, 630건 적출: a(산출물 있음) 346 / b(산출물 없음) 232 / c(예시가 측정치로) 52**, b+c 284건 중 high 71. 원장 `docs/MEASUREMENT_AUDIT.md` + 원자료 `docs/measurement_audit_rows.json`, b/c 243줄에 `[UNVERIFIED]` 인플레이스 표기(표는 마지막 셀 안쪽, 코드펜스 12건은 파일 말미 블록으로 이관). 최악 사례: PoE 천장 91.5 Mbit/s(4구성 수렴 근거의 bench 산출물 `c3_camera/bench/` 부재인데 `dataset.py:961,995`·`config.py POE_BUDGET_MBPS`에 하드코딩되어 모든 fps 상한이 여기 얹힘), MJPEG 9.1→19.1 fps·250→123 ms(디스크의 recording 6개 전부 mjpeg라 raw arm 재현 불가), ORB inlier 1.000→0.827 및 h26x 상한 PSNR 45.60(`encode_quality/` 부재, 값이 docstring/테스트 픽스처에만 존재 — 게다가 저널이 원문 `45.6/0.916`보다 유효숫자를 늘려 인용), consults의 MPC surge-box A/B 4건(box-released run이 recordings에 없음; PID−MPC를 실제 sweep에서 재계산하면 storm −2.26 cm로 인용된 −4.24와 불일치). 패턴 5개 정립(도구 docstring→측정치 승격 / 인용 시 유효숫자 증가 / 인용 시 헤지 탈락 / 우리 가정이 산출물 필드로 찍혀 사실화 / 한 문장 안에서 출처가 갈리는 절반-사실). 규칙 명문화: 측정치엔 산출물 경로 동반, 못 대면 [예측]/[유도]/[스펙] → CLAUDE.md에 절 추가. **감사 자체의 한계도 문서 첫머리에 명시** — 적대적 검증 미실행이라 a 346건은 재검증 전. [memory: measurement-citation-rule]
- 2026-08-04(2) [직접 측정 + 통제실험] Q: C3 공장 캘리브레이션은 공기 중인가 수중인가 → **수중이다**(이전 세션의 "IN-AIR" 단정은 `c3_collect.py:330`에 하드코딩된 우리 가정을 사실로 오인용한 것). 방법: K+rational 8계수를 `cv2.projectPoints`로 **순방향** 투영해 상이 경계에 닿는 각을 이분법으로 찾음(역투영은 광각서 수렴실패 가능해 회피, 단조성 가드 포함). 결과 `python calib/fov_audit.py --audit`: HFOV **CAM_A 63.7 / CAM_B 85.6 / CAM_C 85.1°** vs 공기 스펙 95/127/127°, Snell 수중 예측 67.2/84.3/84.3° → **세 카메라 전부 3.5° 이내**, 공기 스펙과는 최대 41.9°. 덤프 10개 전부 동일(재플래시 없음). 통제실험 4종 전부 통과, 핵심은 **C2**(진짜 127° 등거리 어안을 rational 모델에 피팅해 재측정 → 127.00°) — "스크립트가 뭘 넣든 85°를 뱉는다"를 직접 반증. **C3**은 실제 왜곡으로 닫힌 루프(Snell 적용→재피팅→재측정 129.75/128.68°, fx 757→568 = 정확히 1.333배)로 사용자 손계산 129.7°와 일치. 내가 초판에서 낸 오류 2건: CAM_A 공기 스펙을 108°로 잘못 잡아 세 번째 증거를 버림 + 그 불일치에 "4:3→16:9 크롭" 이라는 **틀린 설명**을 붙임(16:9 크롭은 위아래를 자르므로 수평 FOV 보존). 벤더 독립 확인: BR 제품페이지 "individually calibrated for underwater operation", 포럼 Tony White "calibration ... takes place entirely underwater". 결과: 공기 중 촬영분 depth는 중심부 ~1.33배 과대이나 **깨끗한 스칼라 아님**(왜곡 모델도 매질 불일치 → 주변부 반경오차); 반대로 육상→수중 warp의 목표 카메라 모델을 **이미 보유**. `calib/fov_audit.py` + `calib/FOV_AUDIT.md`. [memory: c3-calibration-is-underwater]

- 2026-08-14 [workflow: 3 researchers + 41 adversarial verifiers + 4 code-mappers] Q: QGC의 누수 경보는 실제로 뭘로 오는가 + rov_gui 5건 변경(녹화 이름/누수/지오펜스 제거/로그 스크롤·저장/런 폴더 통합)의 접점 전수 → **ArduSub 누수는 STATUSTEXT 단 하나**: `"Leak Detected"` / MAV_SEVERITY_CRITICAL(2) / `ArduSub/failsafe.cpp:286`, **20초마다 반복**, 해제 메시지 없음(감지기 쿨다운 `LEAKDETECTOR_COOLDOWN_MS=3000`). MAVLink의 `MAV_SYS_STATUS_SENSOR_LEAK`(=2, MAV_SYS_STATUS_SENSOR_EXTENDED enum)은 **입력 방향**이라 폴링해도 안 나온다 — ArduPilot은 SYS_STATUS extended 3필드를 리터럴 0으로 하드코딩(`GCS_Common.cpp:6057-6059`). `FS_LEAK_ENABLE=0` 또는 `LEAK1_PIN=-1`(기본값)이면 **침수 중에도 완전 무음**이라 침묵을 dry로 부를 수 없다 → 파라미터 읽어오기 + 3-상태(None) 구현. 부분일치 금지: `"Leak detector N error..."`/`"...auto-set to GPIO"`가 매 부팅에 뜬다. 내부기압 별도 페일세이프 `"Internal pressure critical!"`(WARNING, 30 s, FS_PRESS_MAX 105000 Pa). **검증이 잡아낸 내 오류 1건**: "해제는 선로상 완전 무음"은 틀림 — `any_failsafe_triggered()`가 HEARTBEAT.system_status를 MAV_STATE_CRITICAL로 올리므로 관측 가능하나, 10개 페일세이프 **집계값**이라 누수 귀속 불가(그래서 안 씀, 도크스트링 정정). 코드맵이 잡아낸 리와이어 누락 4건(window.py rec_dir 폴백 / MpcConfig.log_dir 기본값 / REC NAV 툴팁 / demo_e2e가 실제 sessions/ 오염) + 버그 2건(events.log가 런 중간에 폴더 이동 / meta.json `run.started`가 실제로는 **닫힌 시각**) 전부 수정. [memory: rov-gui-leak-alert, rov-gui-run-folder]

- 2026-08-17 [workflow: 6 code-readers + 7 adversarial verifiers + 1 synth, + 직접 재측정] Q: 실기 square(MPC/MPCC)는 코너에서 안쪽으로 곡선을 그리는데 시뮬은 안 그런다, path vs trajectory following 차이인가 → **전제가 틀렸다: 시뮬 MPC도 코너를 40 mm 자른다.** 외란 0(NONE)·이상 액추에이터·지연 0에서 꼭짓점 최근접(랩별 중앙값, n=40): pid 2.3 mm / mpc 40.1 / dobmpc 39.7, 참조 자체는 셋 다 2.1 mm(=20 Hz 샘플링 한계) — `recordings/20260727/compare_20260727_000850/wave01_moderate/runs/traj_square_NONE_{pid,mpc,dobmpc}_seed0_c005.3_w266.9.csv`. **안쪽 컷은 MPC의 지문이지 하드웨어의 지문이 아님**(이미 [[square-corner-error-floor]]가 "corner-cutting은 유한-authority 최적해"로 결론). 진짜 격차는 코너속도 맞췄을 때 40 mm(sim, 0.125 m/s) vs 141 mm(hw `mpc_192224`, 0.117) = **3.5배**. **파라미터화**: 시뮬은 순수 trajectory following(`run_compare.py:117` `s=(speed*t)%P`, 차량 상태 미개입, OCP는 NX=12뿐 — progress state/contouring 분해 없음), 하드웨어는 space-parameterized path following 2종(mpc/dobmpc/pid = `PathCursor` leash `path_geometry.py:341-345`, theta는 측정 선체의 단조 투영 / mpcc = theta가 13번째 상태). **그러나 이게 원인이 아님** — leash 고정점이 cross-track 0이고 타깃은 곡선 위에서 샘플(`:352`), 실측 반증 `mpc_115511`은 leash 완전포화(80% 틱)인데 선체가 자기 필렛 25.0 mm보다 **바깥** 17.2 mm. 실제 원인 순위: **①authority 불일치 11.6배** — `U_MAX=[30,...]`(params.py:96)이 `mpcc_acados.py:251`로 그대로 들어오고 좁히는 코드 없음 vs 전달 가능 `6.4*(0.5-0.096)=2.59 N`, a_lat 0.098(hw) vs 1.240 m/s²(sim)=12.7배 → `(v²/a_lat)(√2−1)`가 v=0.12에서 5.8 cm vs 0.5 cm = matched-speed 격차 10.1 cm 중 5.3 cm. mass/drag가 아니라 wrench→axis 인터페이스(`allocation.py:36-49`)라 예측모델에 아예 부재. **②데드밴드 50% 듀티** — gain 60(28/32 런)에선 u≥5.76 N이어야 0.096을 넘음, engaged 틱의 surge 50-53%가 힘 0(gain 6.4 수정 후 10-14%), 즉 수평 액추에이터가 힘원이 아니라 릴레이. **③preview 상실** — `workers.py:1098`이 매 틱 `set_target_ned` 호출 → 그 마지막 줄 `_ref_traj=None`(`mpc_bridge.py:328`) → `_xref_ned`가 `:250` `pos_world=p_ref+outer(v_ref,ks*dt)`로 낙하 = 61 스테이지 전부 직전 변의 **접선 직선 ray**로 꼭짓점 관통; 이미 있는 `set_path_plan_ned`는 호출부 0개. 부호 주의: preview는 안쪽 컷의 **원인**(sim mpc 40 vs sim pid 2.3, 동일 참조/플랜트), 잃으면 늦게 돌아 오버슛+넓은 복구. MPCC는 오히려 시뮬보다 많음(2.122 m/7.2 s). **④기동 불일치** — 시뮬 square는 전부 heading_follow **true**(base.yaml:135, 랩당 360° 노즈), 하드웨어 square 32개 전부 **false**(crab); gain-60 런은 `|ax_yaw|`도 39-50% 틱이 데드밴드 아래라 요 모멘트 0인 채 기수 최대 34° 표류 → 몸체프레임 회전이 surge/sway 분배를 재분할. **이것만으로도 저장된 모든 sim↔hw 코너 숫자가 apples-to-apples 아님.** ⑤지연: tag_age p50 0.12-0.15 s(전 square 재계산; hw_mpc.yaml:259의 0.19는 station hold 값), 위치는 전진보정되나 속도는 아님(`state_assembler.py:208`)이라 x0 내부 불일치(p=현재, ν≈0.22 s 과거) — 0.05 m/s서 0.06 cm, 0.3서 1.4 cm로 2차. **반증 8건**: 크기차(둘 다 1.0 m — `size:0.2`는 패널이 덮는 seed, meta의 `mission/panel_override`가 실제값; 32개 중 29개 1.0 m) / 필렛(MPC·DOBMPC square엔 **하나도 없음**, 참조 코너 실측 0.6-1.8 mm; 필렛 0.06/0.10은 pid·mpcc 런에만, 실측 24.9/41.7 mm = `R(√2−1)` 정확) / lookahead 컷(상한 1.35 cm, 실측 ~0) / 액추에이터 포화(gain-60 포화율 0.0000, 최대 0.0017 — 포화가 아니라 데드밴드) / gain 60→6.4가 authority를 낮췄다(`axis=u/gain`이라 **올림**, 선체속도 p50 0.027→0.217) / 코너게이트(2026-08-16 삭제, `geometry.py:383-384`) / MPCC progress 가중(≤2.4 mm) / DOB(데이터 있는 dobmpc square 0건). **meta가 거짓말하는 곳 3**: `mpc_bridge.py:476-477`이 `ref_preview:True`+`path_reference:"shared spatial plan, full horizon"`을 path 모드에도 하드코딩 / `t_traj`(CSV 12열)는 path 모드서 **초가 아니라 미터**(`workers.py:1102`) / `events.log`가 삭제된 corner-gate를 여전히 출력(`workers.py:530`). 갈라낼 실험: `path_following:false` mpc 1회(시뮬 verbatim 시계 + 진짜 preview 동시 복원, meta strategy가 `wall_clock_trajectory`로 — **유일한 apples-to-apples**) → 시뮬을 heading_follow:false로 재실행 → gain 6.4+fillet 0으로 속도사다리 재현(이번엔 포화가 새 제약, `mpc_144318`이 이미 34%). [memory: sim-vs-hw-square-corner]

- 2026-08-19 [workflow: 8 code-dissectors + 3 적대적 검증(correctness/provenance/scope) + 1 synth] Q: marinegym의 파도·해류를 어떻게 구현했고 어떻게 시뮬에 적용했으며 비주얼은 어떻게 했나(발표용 설명 준비) → **핵심 프레이밍: 외란 구현이 두 벌이고 둘 다 살아 있다.** legacy `disturbances.py`(deep-water k=ω²/g, `k·x` 항 자체가 없음, 상수 해류, Poisson kick) = `teleop.py:71,1317-1320`·`dobmpc/eval_dp.py`; modern `disturbance/`(유한수심 ω²=g·k·tanh(kh) Newton, JONSWAP×cos^{2s}를 (N_ω×N_β) 격자로 펼친 M성분, cosh/sinh 깊이프로파일, mean+Gauss-Markov 해류, Froude-Krylov) = `run_compare/run_viewer/wave_preview/verify_eaob`. **발표된 실험은 전부 modern인데 `docs/07_DISTURBANCES.md`는 legacy만 문서화**(`:36-37`이 유한수심을 아직 "future upgrade"라 적음). 적용 경로는 단 하나 — `set_mjcb_passive`(hydro.py:243) → `mj_applyFT`×3 → `qfrc_passive`, 500 Hz, `xfrc_applied`/actuator 미사용, MJCF `density=0 viscosity=0`으로 MuJoCo 자체 유체 OFF. 물리는 (a)상대속도 `nu_r[:3]=nu[:3]−Rᵀv_water`(hydro.py:149-152)로 항력·Coriolis·부가질량 유한차분까지 전부 커버 + (b)유일한 주입력 FK `F=ρ·vol·C_M·a_wave`(env.py:102-109, C_M=1, **토크 0**, ρ·vol=11.941). Morison 항력·C_a는 D행렬/M_A 중복이라 의도적 배제. 비주얼은 `pool_water_surface` hfield(48×96, contype=0)에 물리와 같은 파동장으로 η 재구성→`hfield_data`→`update_hfield`/`mjr_uploadHField`, Δ=0은 `tests/test_water_viz.py:79-104`가 2000스텝 **strict `== 0.0`**로 증명. **검증이 잡아낸 것 4건**: ① "파랑 실현이 264.8 s마다 반복"은 **틀림**(ω_min이 dΩ의 정수배가 아니라 매 T마다 전 성분이 154.29° 회전, eta(t+T)≈−0.9·eta(t), 정확반복 7T=1853.5 s; 재귀하는 건 group **포락선**) → KNOWN_ISSUES:700 문구 정정 ② `verify_hydro.py` 13/32·`verify_hydro_precise.py` 12/17 **현재 FAIL**(bluerov.xml 강체 + BlueROVHeavy.yaml 부피 혼합, 이미 KNOWN_ISSUES:251) → "hydro 검증됨" 인용 금지, 다만 T3.4 Galilean(상대속도 경로)은 **통과** ③ 렌더 hfield 축이 row↔col **전치**(mj_ray 직접 프로브로 확인) → 파면 방향이 x=y 대각 미러 ④ `water_viz`가 `use_waves`를 안 봐서 C/CD 모드에도 물결이 보임 + 120성분 트림(very_rough 분산 74.7%만) + `elev=0.6`이라 |η|>0.30 m flat-top. 스펙트럼/FK 단위테스트는 **재정규화·정의식을 되읽는 tautology**라 검증으로 인용 불가(진짜 검증은 realized Hs 12% 이내, 분산관계 잔차 1e−8, 해석가속도 vs 중앙차분). `disturbance/{waves,current,env}.py`에 **문헌 인용 0건**, 해류·sea-state 숫자 전부 산출물 없음 → "설정값"으로 말할 것. 새 미기록 결함 3건 KNOWN_ISSUES 등재. [memory: 없음 — 기존 finite-depth-disturbance-env / water-surface-wave-viz 에 흡수]

- 2026-08-23 [직접 조사: 코드 + 실기록 재집계 + Bar30/ArduSub 스펙 확인] Q: ROV를 넣으면 수위/수심을 알 수 있나, 대략 얼마인가 → **수심은 된다(Bar30 → `SCALED_PRESSURE2` / `GLOBAL_POSITION_INT.relative_alt` → `hardware.py:1312-1337` → HEALTH depth 타일 + 런마다 `*_rov.jsonl`), 해상도 0.2 mbar = 담수 2 mm [스펙]. 그러나 "수위"는 0점 조건부**: ArduSub은 depth=(ground_pressure−p)/9800/`BARO_SPEC_GRAV`이고 ground_pressure를 **부팅마다** 재취득 → 절대압은 ±200 mbar(±2.04 m 담수) 스펙이라 무의미. 실증: engage마다 `calibrate_z_offset`이 잡는 `−z_off`(= 매트 위 수면 높이)를 events.log `datum p0` z + 같은 런 jsonl의 relative_alt로 복원하니 **2026-08-18 = 1.386/1.409/1.415/1.418/1.440 m, 2026-08-17 = 1.641/1.654/1.664/1.686/1.722 m** — 하루 안 ±3–4 cm, 세션 간 0.24 m 점프(=0점, 물이 아님). 압력 무개입 하한: 채택 fix `z_ned` 최저 **−1.296 m**(p50 −1.138, n=22094, `sessions/nav_runs/*/fixes.csv`) → 매트 위 물기둥 **≥1.3 m, 최선 추정 ~1.4 m**. **config가 틀렸다**: `water.surface_height_m 0.8255` / `pool.depth_m 1.143`은 2026-05-25 최초 커밋 이후 불변인데 둘 다 하한 미달 → KNOWN_ISSUES 등재, 굴절 보정(`tagslam_core.py:880`)이 이 값에 매달려 있음. 확인만 하고 지나간 것 2건(버그 아님): CSV `pz`는 **world FLU(up +)**라 depth와 부호 반대인 게 정상(`workers.py:2981` frame_note, `_flu_of_ned`), events의 `datum p0`는 NED이고 engage 순간 `z = z_tag`가 정확히 성립(`workers.py:1228-1229`가 `calibrate_z_offset` 직후 `step`). [memory: pool-water-level-provenance]
- 2026-08-23 [session] Q: 물체가 태그 한 칸 옆 + follow 폭주 원인 → C3 스테레오 depth가 물속 1.56~1.71배 김(캘리퍼 119.73 vs 메시 187 확정, 조종사 가설 적중); --depth-scale 0.64 잠정 [memory: c3-stereo-depth-not-metric]
- 2026-08-24 [workflow: underwater-robotics + slam-perception + hardware + code/data + verifier] Q: depth-vs-MAP이 높이 따라 움직인다, 원인과 올바른 보정은? → **오차는 disparity 도메인이고 거리에 비례**. 전제 먼저 검증(`--depth-scale`이 `_check_depth_scale`이 읽는 맵에 **먼저** 곱해짐을 코드 추적 + 저장 캡처 2건이 0.64로 나눈 뒤 0.011까지 일치 → 원시 1.28 확정). **거리 의존이 실재한다는 결정적 증거는 진단 함수와 무관한 증인**: 메시 143×166×187 vs 캘리퍼 119.73 = 축별 1.19/1.39/1.56, **비등방 1.31** — 상수 배율은 모든 축을 똑같이 늘리므로 정육면체가 벽돌이 될 수 없다. r(Z)≈0.977+0.538Z [유도, 108만 샘플, 단 **다른 stereo 설정**이라 재적합 필요]. 배제: 순수 배율(1.56배면 반화각 52.7° > Snell 한계 48.75° = 불가능한 카메라) · 평면포트 굴절(부호 반대 + 10~30배 작음) · 정수 disparity(상한 +1.8% vs 필요 +56%, 게다가 출력이 1mm 조밀해 격자 안 보임) · "외부파라미터 공기 중"(매질 불변량이라 무내용). 미확정: 기전, 1.7m 이상, 화각 의존항(실재하면 곡선으로 못 고침). 부수: hardware.py의 1.71x 인용이 mesh 로그 줄을 가리키고 있었고 1.71은 sessions/ 어디에도 기록이 없어 **인용 정정**(비등방 근거로 교체). [KNOWN_ISSUES 1건 추가] [memory: c3-stereo-depth-not-metric]
- 2026-08-25 [research-verify] Q: GUI가 쓰는 mpc_tuned의 정확한 OCP formulation → 12개 주장 중 8 confirmed / 4 정밀화(K·M 드롭+슬루+heave 부호, _tuned 게이트는 path plan 유무, w는 'dobmpc' 접두사가 결정, 종단 W는 apply_terminal 게이트); live solver cost_get으로 [[75,0],[0,1200]] 회전 실측 확인; 신규 결함 1건 = meta가 회전 실적용 여부를 기록 안 함(KNOWN_ISSUES 추가) [memory: mpc-tuned-path-frame-cost]
- 2026-08-30 [workflow: general-purpose 레포 검증] Q: UMI_underwater 레포의 diffusion policy 실제 구조 (계획 수립용) → clean-room 런처(upstream Chi et al. UMI 클래스를 subprocess로 실행, 커밋 fb99783): encoder=CLIP ViT-B/16 TimmObsEncoder(attention pool, not frozen), denoiser=1D UNet, DDIM inference 16 steps, obs horizon 2 / action horizon 16 / 실행 8, action=body-frame velocity 6-dim(QYSEA 조이스틱 채널 매핑)+gripper 2-dim 문턱, 실행 10 Hz + 추론 ~1 Hz(비동기 receding horizon+시간정렬 skip); proprio는 코드상 [compass,pitch,roll]/360 — 논문의 "vehicle depth"는 부정확(코드가 권위); depth는 스테레오가 아니라 Depth-Anything-V2 모노큘러+정적 앵커 정규화; policy 학습=자기지도 수중 233 성공 에피소드, affordance=on-land 800 demos(별도 U-Net) [memory: dp-trajectory-plan]
