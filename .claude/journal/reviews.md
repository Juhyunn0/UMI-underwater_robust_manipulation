# P2 — Parallel-review syntheses

<!-- Populated when P2 (/review-change) lands. One block per review:
     ## YYYY-MM-DD <artifact>
     reviewers: <agents> · 합의: … · 이견: … · 리스크: … · 최소수정: … -->

## 2026-06-22 — Heavy 관성 텐서 [0.3291,0.6347,0.6109] (parallel-axis 유도, farol USD 대신)
reviewers: simulation-advisor · control-theory-advisor · underwater-robotics-advisor (병렬·독립)

- **합의(3/3):** (a) parallel-axis 유도법 타당, farol [0.21,0.245,0.245] 기각 정당(코너 질량 추가가 Iz를 낮출 수 없음 → 비물리·Gazebo hand-tune). (b) **in-sim non-issue** — MJCF plant `diaginertia` == predictor(params)라 closed-loop이 관성값에 불변; 논쟁은 sim-to-real에서만 유효. (c) 0.15kg/thruster는 예산 bookkeeping(실제 T200 ~0.344kg)이나 delta만 스케일 → 영향 작음(~0.03).
- **주요 발견(control+underwater 합의, sim도 지적):** dry inertia를 소수 3자리까지 다듬는 동안 **회전 added mass [0.12,0.12,0.12] (BlueROVHeavy.yaml)는 등방 placeholder로 방치** — 생략한 유체 r² 항이 계산한 ΔI와 동급 크기("두 동급 항 중 작은 쪽만 정밀화"). 권장 anisotropic ≈ Kp'0.07 / Mq'0.18 / Nr'0.22 (von Benzon&Fossen 2022 order로 주장됨 → P3에서 검증).
- **sim 지적:** 텐서 3중 복제(rov_model.py:54 · MJCF diaginertia · compute_heavy_inertia.py 출력)를 묶는 verify/test 없음; 스크립트가 baseline 하드코딩 → drift 위험.
- **control 지적:** Heavy는 roll/pitch commanded(NU=6) → 관성이 "don't care"→"~10-20% 중요"로 승격. EAOB는 quasi-static 오차는 흡수하나 ω̇-상관(기동 중) 오차는 못 함.  [UNVERIFIED: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- **이견:** sim/control "in-sim 수정 불요(값 유지)" vs underwater "added mass 지금 고쳐라" → 화해: in-sim 깨진 것 없음, 단 added-mass가 진짜 fidelity gap이라 다음 투자는 dry inertia가 아니라 거기.
- **리스크:** 회전 added-mass 과소·등방(sim-to-real 자세 fidelity); 텐서 3중 복제 drift.
- **최소수정(영향순):** ① 민감도 스윕(plant 고정, predictor 관성 farol vs 유도 → pitch/roll·ŵ RMS 비교 → EAOB 흡수 정량화) ② BlueROVHeavy.yaml 회전 added mass anisotropic화 ③ test_heavy_inertia 3중 일치 게이트 + 스크립트 baseline import.
- **검증법:** roll/pitch step 후 ŵ의 ω̇-상관 펄스 관찰(관성오차 지문); yaw step→Iz 직접; bifilar(공기)+in-water decay 실측 bound.

- 2026-07-01 [workflow: review-observe-mode (4 dims × 2 skeptics)] Q: teleop `--observe` 자유표류 diff 적대적 리뷰(역학누출/루프·스레드/UX) → 5지적 중 1 확정: viser observe에서 조종버튼이 여전히 보임(비기능) → 숨김 처리. 반증 4: ①"hydro.reset()이 byte-identical 깨뜨림"=오판 — reset()은 필터상태만(계수/모델 불변, hydro.py:108-114), recenter는 의도적 상태리셋이라 byte-identical은 "recenter 미발동 시"만 주장. 오히려 reset 안 하면 표류속도가 필터에 남아 거대 added-mass 스파이크 발생 → 호출이 정답(viser Reset pose와 동일) ②③ _recenter_request 락 없음 = 무해(bool·멱등, 기본경로는 매프레임 재중심) ④ recenter CSV 속도 불연속 = 의도된 상태리셋. [memory: observe-free-drift-mode]

- 2026-07-03 [workflow: review-pid-poleplacement-apply (transcription+regression × verifier)] Q: pole-placement PID 적용 diff(GAINS_HEAVY/GAINS_BLUEROV2 분기 + r_ref yaw-rate FF) 적대적 리뷰 → 전사·게인값·r_cmd 계산 무결; 실회귀 1건 확정(실행 재현): test_controller.py·test_square_mission.py가 bluerov.xml(rank-5)을 하드코딩하는데 DEFAULT_GAINS 기본이 heavy가 되면서 GAINS_HEAVY 주입 → pitch 텀블(46.8°/58.7° > 45°) 테스트 실패 → 두 테스트에 `gains=C.GAINS_BLUEROV2` 명시 고정으로 수정, 기본 env 재실행 전부 PASS. [memory: pid-gains-provenance]  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-07 [workflow: review-run-compare-extension (4 dims × adversarial verify)] Q: run_compare 원샷 배치 확장 diff(pairing:grid 방향그리드 + per-run runs/ CSV·meta + trajectory_compare 전방향 오버레이 + MPC 파랑) 적대적 리뷰 → 16 findings, 확정 6(중복 제거): ①_load_run_traj 무방비 genfromtxt — 부분/1행 CSV 하나가 그림 단계에서 main을 죽여 results.csv 통째 소실(재현됨) → try/except+atleast_1d+호출부 가드+부분파일 삭제 ②plot_trajectories._find가 새 _c/_w 파일명에서 --dir-deg/--seed 무시하고 sorted[-1]을 조용히 그림(mislabel, 재현됨) → seed·heading 패턴 추가+fallback WARN ③같은 scenario 두 블록이 runs/ 파일 경쟁 → 명시적 ValueError ④범례 n=sweep×seeds 과대표기 → n≤N 처리 ⑤단일모드 그림 full-run RMS 무표기 → 범례 title 명시 ⑥record_runs 디스크 무경고(dp 1000s×120런≈160MB) → 시나리오 길이 기반 추정 상시 출력. 반증 5(_deg_tag 충돌, wave_swept 일탈 등). 추가로 그리드 테스트가 기존 버그 발견: _prebuild_acados가 dobmpc 전용이라 mpc-only 병렬런이 tera 경합→IPOPT 폴백(20배 느림) → mpc/dobmpc 공용 프리빌드로 수정(107.6s→5.7s/run). 전부 수정 후 40런 e2e 재검증 PASS. [memory: finite-depth-disturbance-env]  [UNVERIFIED×2: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-21 [P2: simulation-advisor + control-theory-advisor (병렬 독립)] Q: MPC reference-preview 구현 diff(set_reference_traj/_xref_ned_traj + make_square_ref + run_one/run_viewer 배선 + 테스트) 리뷰 → **wrong 0건 합의**(프레임 S-conjugation·−r 부호·stage/terminal 시간정렬·unwrap 앵커·샘플러=라이브루프 정렬 모두 양측 독립 검산 통과). 확정 최소수정 3건 반영: ①meta.json에 controller.ref_preview provenance(이전 기록과 비교불가 문제 — pid_gains 전례와 동일 원칙) ②샘플러 (K,3) 전치 반환의 silent scramble 방지 shape assert ③reset()이 _ref_traj 해제(스테일 샘플러 오동작 예방). 권고 반영: 샘플러↔라이브루프 동치 회귀 테스트 추가(test_square_ref_matches_live_loop, 1랩 <1e-9). 이론 리뷰 판정: mid-turn sway 속도참조 = position preview와 유일하게 운동학적 일관 선택(정답), sharp square 참조 유지가 벤치마크로 옳음(smoothing은 과제 변경), RTI warm-start는 오히려 개선 예상(vertex에서 전 stage 90° 동시회전 → tick당 1샘플 미끄러짐), QN=Q·terminal r(t0+N·dt) 정합. 이견 없음. 실험 재실행은 사용자 몫. [memory: mpc-reference-preview]

- 2026-07-23 [workflow: eaob-review-fanout (R1/R4/R5 병렬 3 agents) + 수치검증 + 폐루프 A/B] Q: 외부 EAOB 리뷰(C0–C4) verify-first 검증 → 재튜닝 적용. R1–R6 전부 실코드 확정(노이즈 주입 전무·R=스칼라I·deadbeat 실측 6.8–13.4 ms·EAOB_* 소비처는 eaob.py 기본 kwargs뿐·게이팅/NIS 부재+KNOWN_ISSUES가 요구·부호/프레임 OK). 적용: perf 프로파일(시그마 유도 블록대각 R + tau→K→q 레시피, 노브 EAOB_TAU_DIST 하나) + 센서노이즈 주입(meas_noise) + verify/verify_eaob.py(NIS/NEES/RMSE/운동상관). 반복 로그: τ 0.5 CDW 실패(NIS 25.4/NEES 77) → 0.3/0.2/0.1 스윕 → 0.2 확정(CDW 17.1/15.0 PASS, CD NIS 14.4 PASS). 스펙과 다르게 한 것 2건: ①χ² 게이트는 구현했으나 기본 OFF — A/B 실측(τ0.5)에서 25–76% 기각으로 파랑 추종 차단(CDW 4.5→15.5 cm), 게이트 ON 측정은 NIS 통계도 오염(137 vs 25); ②CD NEES ~9(<14)는 실기체 보수성(SIG_ALLOC 등)이라 시그마 하향 반복 안 함. 헤드라인 보존: DP C dc −0.11 cm vs MPC +2.13. 정직한 비용: square CD 1.02→1.21, CDW 3.09→3.51 cm. meta controller.eaob = 새 provenance 경계. [memory: eaob-perf-retune]  [UNVERIFIED×6: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-23 [workflow: mpc-state-source-review (R4 소비처/R3 yaw·bound 병렬 2 agents) + 폐루프 A/B 12런] Q: MPC 상태 입력 truth→EAOB 추정치 전환 spec verify-first 검증 → 적용. R1–R6 확정: x_ned는 소비처 1개(단일 스위치 지점 증명), EAOB update가 solve보다 선행(지연 0), 두 솔버 모두 yaw/위치 bound 없음+EAOB yaw는 연속 신호 → C1 unwrap 누산기 불필요(스킵), lazy-init이 첫 읽기값 시작이라 C2 워밍업 불필요(스킵), truth 소비처(recorder/mission/각 runner) 전수 확인. 적용: MPC_STATE_SOURCE="estimate"(dobmpc+perf 전용) + _psi_ned_now 동반 전환 + meta mpc_state_source(세 번째 경계) + verify_state_source.py(A/B: 추종/chatter/포화/NIS/NEES/yaw연속). 구파랑 A/B 전부 PASS: DP C 0.33→1.46, CDW 1.51→2.22 cm; square CDW 3.51→3.92 cm; NIS/NEES 루프 폐합 무영향; square에선 chatter 오히려 감소(관측기=상태필터). 검증 중 발견: base.yaml 파랑이 19:28에 강화(Hs 1.2/Tp 6)돼 현 config CDW는 소스 무관 NIS 41–80 실패 → KNOWN_ISSUES 등재, 재튜닝은 사용자 몫(과제 제약 준수). [memory: mpc-state-source-switch]  [UNVERIFIED×2: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-23 [P1 control-theory-advisor 상담 → 구현: DOB 플러그인 구조 + 3-source A/B] Q: EAOB를 순수 disturbance 플러그인으로(MPC x0 = state estimator 출력, EAOB eta/nu_hat 폐기, w_hat만 MPC로) — 사용자 지시로 우선 noisy measurement를 x0로. 구현: `MPC_STATE_SOURCE` 3-way("meas" 신규 기본=estimator출력, "estimate"=EAOB필터 opt-in, "truth"), _control_step에서 noisy meas를 EAOB·x0 공유 샘플로 뽑아 3-way 분기; plain mpc도 noisy x0로(단일변수 ablation); verify_state_source에 --sources + meas 추가. 검증(구파랑, truth→meas→estimate): 추종은 소스 무관 거의 동일(DP C 0.33/1.44/1.46 cm)이나 **"meas" chatter가 truth의 9–15배**(chatF 0.71/10.89/1.18 N/tick), effF ~2–3배 — MPC가 5cm/tick 지터 직접 추종; "estimate"는 동일 추종을 truth 수준 chatter로(=상태 필터). 발산 없음/포화 0/n_fail 0/EAOB yaw 연속(|Δψ̂|≤0.08). advisor 결론(실기 SLAM+IMU 융합이 스무딩 소유, 시뮬 5cm 백색은 비관적)과 일치. 완화(SLAM-proxy KF 등)는 estimator 확정 후 사용자 결정 — EAOB 튜닝 불변(제약 준수). [memory: mpc-state-source-switch]  [UNVERIFIED×2: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-24 [측정노이즈 축소: sigma 스케일 스윕 + A/B 재검증] Q: noisy measurement가 추종오차보다 커서 x0 chatter 유발 → 추종오차 아래로 축소 요청. sigma 스케일 스윕(pos 5/2/1/0.5/0.25 cm, square C meas)으로 chatter/tracking/NIS 곡선 확인 후 **x/y 0.5 cm** 선택(추종오차 ~1.2 cm의 0.4배). params.EAOB_SIG_ POS/ANG/LVEL/AVEL만 축소(good SLAM+IMU 값), ACC/AACC/ALLOC(R_tau·q_dist)는 불변→w_hat 보존. A/B(gentle): meas chatF 10.9/11.8/12.0 → 1.76/2.91/3.08(truth의 ~2–3배, 이전 ~15배), 추종은 소스 무관 truth 수준, DC 편향 누출 소멸, 상태추정 pose~1.6mm. w-RMSE 0.24/0.34/0.39 N(구 0.29/0.41/0.48보다 좋음). NIS/NEES는 ~13/9로 목표 18 아래=보수적(과신 아님)→verify_eaob·verify_state_source 게이트를 과신(상한 24)만 판정하도록 변경(하한은 conservative 보고). meta에 eaob.sig_pos_xy+mpc_state_source 기록(5cm 시절과 경계). 별건으로 base.yaml에 6-스펙트럼 sea-state 사다리(storm 1.8→gentle 0.75) 추가+smoke 검증; run_compare 4500런 규모/2700 낭비(NONE/C/CD 파도무관 중복) 경고+trim 권고는 사용자 대기. [memory: eaob-perf-retune, mpc-state-source-switch]  [UNVERIFIED×4: 산출물 없음/예시/유도값 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-24 [P2 워크플로 3렌즈(mpl-correctness/edge-integration/consistency)+검증, 세션한도로 1렌즈만 완료 → 인라인 보강] Q: run_compare 그림 3건 수정(사용자 요청: xwave bar y축 공유+x라벨 겹침해소+범례 1개 하단; trajectory 패널 reference square 최전면; wave_comparison에 CW·CDW sea-state별 궤적 합성 2장) 후 신규 그림코드 리뷰. mpl-correctness가 1건 적발(검증 에이전트는 세션한도로 미실행 → 내가 육안+코드로 확정): reference square를 zorder 6/6.5로 올리자 RMS 주석 ax.text(기본 zorder 3)가 사각형 점선·코너마커 아래로 내려가 숫자가 덧그려짐(NONE/C 등 저외란 패널) → ax.text 2곳 zorder=10로 수정, ALLMODES 재생성해 겹침 해소 확인. edge/consistency는 인라인 점검(caller는 main 1개뿐·테스트 없음, preview_jobs→wave_dirs 순서 일치, seed_override 전파가 run_wave와 동일, record_runs=false/runs 부재 시 len(panels)<2로 무해 스킵, 단일 ctrl에도 legend ncol≥1, 트래젝토리 블록만 try-guard라 bars·CSV 손실 없음) → 추가 결함 없음. 그림 재생성은 재시뮬 없이 runs/ CSV+config+results_all_waves.csv로 수행(scratchpad regen_figs.py). README/docstring/[memory: wave-spectrum-sweep-preview] 갱신.

- 2026-08-03 [P2 워크플로 5렌즈(lifecycle/dataset-validity/axes-config/edits-blast-radius/safety-resources) + 건별 adversarial verify, 38건 제기 → 세션한도로 verify 20/38 완료] Q: 신규 `c3_camera/c3_option_sweep.py`(컬러해상도 × 압축 × depth on/off × depth해상도 스윕, 셀마다 완전한 TUM RGB-D 데이터셋 + results.csv) 및 source.py/dataset.py 수정 리뷰. **선행 매핑 워크플로가 잡은 결정적 사실 2건**: (1) depthai 2.32에 `ImgFrame.getFrameType()`이 **없어서**(hasattr False 실측) frame_type이 항상 ""→ DatasetWriter의 첫 I프레임 게이트가 영영 안 열림 → **h264/h265 데이터셋은 0프레임 기록**(c3_collect도 동일 영향); (2) `c3_dataset_check`는 depth.txt/associations.txt 공백을 hard FAIL로 봐서 `--depth off` 셀을 정상인데 "고장"으로 판정. 리뷰가 추가로 확정한 주요 결함과 조치 — `row["ok"]=1` 무조건 대입(추출 실패해도 성공으로 보고) → `ok = recorded and not error` + 테이블에 NOT USABLE 행; post-roll(writer.close/메타/ffmpeg)이 `with C3Source` **안**에서 실행 → h26x blocking 큐를 수 분간 안 읽어 디바이스 stall → with 블록 밖으로 이동; `finally`의 `writer._closed` 게이트가 뒤집힘(close()가 첫 줄에서 플래그 세팅 후 최대 120s flush → 그 창에서 Ctrl-C 시 metadata.json 없음) → **artefact 존재 여부로 게이트**; 실패 셀이 이전 실행 디렉터리 크기를 자기 것으로 보고 → `writer is not None` 가드 + 비어있지 않은 셀 디렉터리 **기록 거부**; `--min-free-gb`가 `written % 40`(3개 writer 스레드가 증가시키는 카운터)라 사실상 미발동 → 2초 wall-clock 주기; Annex-B 스캔 2회/패킷을 캡처 스레드에서 per-byte로 수행 → `bytes.find` 단일 스캔(**232x 빠름**, 20만 랜덤+짧은문자열 전수에서 출력 동일 확인); h26x 셀 디스크 추정이 비트스트림만 계산(추출 PNG 미반영) → 계획표 보정 + 추출 전 여유공간 확인; `Camera.fps: {fps:g}`가 7.5 같은 실수 노드를 뱉어 ORB-SLAM3 `readParameter<int>`가 exit(-1) → **정수 고정**; RGB-D yaml에 `Stereo.ThDepth`/`Stereo.b` 누락 → 추가(구 파서용 `ThDepth`도 병기, 캘리 실패 시 공칭 75mm 명시); `_render_metadata`가 모든 데이터셋에 "pixel-for-pixel RGB-D"를 무조건 주장 → 실제 기록 기하로 **분기**; `imu_stats`가 warm-up 표본 포함 → metrics와 함께 리셋; mavlink `message_counts`가 스윕 누적 → per-cell `records_this_dataset` 별도 기록; `rgb_files`가 인덱스 줄 수인데 이름은 파일 수 → `rgb_index`/`rgb_on_disk` 분리(추출 실패가 CSV에서 보이게). 회귀: 기존 186 테스트 전부 통과 + 신규 `tests/test_option_sweep.py` 32개(가짜 C3Source로 데이터셋 e2e 포함). telemetry.csv 스키마 레이스는 공유코드 선재결함이라 미수정 → KNOWN_ISSUES 등재. [memory: c3-option-sweep-tool]  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-08-06 [감사: 정적 스캔 + 이중환경 실행 대조] Q: robust가 python 3.14였던 기간(2026-05-04 21:28 ~ 08-06, 94일)에 나온 수치 결과 중 numpy 피연산자 손상 버그에 오염된 것이 있는가 → **저장된 산출물 중 오염 확인된 것 없음.** 손상되는 함수는 2개 찾았으나 둘 다 출력을 디스크에 남긴 적이 없다. **방법**: (1) 발동 조건을 옛 환경에서 실측 확정 — `a*b(→a)`, `b*a(→b)`, `a+b`, `a*2.0`, `2.0*a`, `-a` 전부 덮어씀, `(a+1)*b`는 둘 다; `np.sqrt`·팬시인덱싱·`.sum()`·`out=`은 무사; **임계 256 KB**(dtype 무관, float64 32768개/float32 65536개); 함수 지역변수 한정이고 코드 형태에 따라 발동/미발동이 갈림. 3.14에선 파라미터로 넘긴 배열도 refcount 1로 보여 **정적 추론이 불가** → 판정을 전부 실행 대조로. (2) AST 스캐너로 "지역변수가 산술 피연산자로 쓰인 뒤 다시 읽히는 곳" 전수 추출 = 182파일 630후보. (3) `robust314_bad` vs `robust`에서 같은 명령을 돌려 **경로 제외 내용 해시 + stdout diff**. **노이즈 바닥 확정**: 두 numpy 빌드는 SIMD 커널이 달라 최대 3.0e-16(≈1.4 ULP) 차이 — `make_synthetic_capture`의 ground_truth.json에서 실측(다른 float 61개, 중앙값 1.6e-16), 판정 기준 1e-14. **동일 확인**: marinegym 13테스트·verify 6종(특히 `verify_state_source` 출력 60줄 radRMS/NIS/NEES 전부 동일)·c3_camera 4스위트 193개·`umi_handheld.warp`(PNG 456개 바이트 동일)·`extract_gripper_width`·`record --source synthetic`·`make_synthetic_capture`(이미지 166개 동일, JSON만 1.4 ULP)·`rebuild_trajectory_html`(궤적 7,547행 바이트 동일)·`fov_audit`·`gen_pool_apriltags`·`c3_dataset_check`. **손상 2건**: `c3_encode_quality.ssim`(동일 이미지 SSIM 7.05e8, `a*a`→`b*b`→`a*b`가 연쇄로 덮어써 사실상 a²·b²) — `c3_camera/encode_quality/` 부재 = 한 번도 실행 안 됨; `geometry.depth_to_xyz`(z 최대 109 km, 점 42146 vs 86403) — 호출자 3곳 중 저장 경로인 `save_pointcloud`의 `.ply` **0개**, `c3_depth_accuracy` 미실행, 나머지는 라이브 미리보기. **실행 못 한 경로**(하드웨어 15파일, 최대 노출은 tagslam_core 52후보/442산출물)는 크기 논거로 판정 — 변수가 3-벡터·태그코너(태그당 4점)·LM 야코비안·프레임별 스칼라이고 저장된 최장 궤적이 7,547행(60 KB)으로 임계의 1/4, 같은 모듈 오프라인 절반은 실행 대조로 동일 확인. 기록: [docs/NUMPY314_AUDIT.md](docs/NUMPY314_AUDIT.md), KNOWN_ISSUES 해당 항목 삭제(해소). 옛 환경 `robust314_bad`(2.6 GB)는 재현용으로 보존. [memory: environment-numpy-constraint]
- 2026-08-10 [P2 workflow: 3 reviewers × adversarial verify, 9 agents] 대상: pose 파이프라인/3D맵/depth 프로브 diff → 6건 확정·전부 수정: _log 섀도잉(사전 발견 재확인), close()-로드 레이스(mid-torch abort), 더블클릭 후행 Release 재장전, TRACK-off에도 프레임 복사, CAMERA 레코드 in_air 역방향(감사와 모순 — medium:water로 정정+지상 1.33배 경고 반전), MESH 원샷/CSV t=0. 테스트 61/61 양env. [memory: rovgui-pose-env]
- 2026-08-12 [P2 workflow: 5 finders × adversarial verify, 18 agents(10 완료, 8은 세션한도)] 대상: rov_gui `--mpc` 폐루프 스택 전체(신규 rov_gui/control/ + 배선) → 13발견 중 5건 확정·전부 수정: (1) 테이크오버 시 all_stop()이 파일럿의 held key까지 소거(기체가 관성 주행하는 동안 조종사는 후진을 쥐고 있다고 믿음 → takeover latch로 분기, 테스트로 고정), (2) acados 첫 빌드 중 stop() 타임아웃이 실행 중 QThread 참조를 버려 Qt abort(→ _ORPHANS 파킹+60s drain), (3) START TRAJ가 배치된 square의 지오펜스 검사 없음(→ 코너 4점 사전 검사+거부), (4) MpcWorker 행 시 창이 ENGAGED에 영구 latch(→ fresh["mpc"] 워치독 1.5s로 창 자체 해제), (5) HEARTBEAT 스냅샷 무한 보존으로 armed 인터록이 죽은 스트림을 신뢰(→ _arm_state에 3s 에이징, 2026-08-07 스러스터 패널 사고와 동류). 미검증 7건 중 5건 자체 판정 후 수정(min_tags 미배선, 압력 depth 신선도, ambig CSV 칼럼, demo 가속도 ω×v 항, geometry 문서 과장), 1건 KNOWN_ISSUES 보류(QTimer 지터 vs 고정 dt), port-fidelity finder는 미실행이라 구현 시 자체 검증(참조 로직 라인 포팅+생성기 테스트 고정+E2E)으로 갈음. 수정 후 19/19+61/61+demo E2E 재통과. [memory: rov-mpc-hardware-stack]
- 2026-08-14 [P3-ish workflow: 4 설계 × 적대적 검증, 10 agents] Q: 중복 AprilTag id를 어떻게 다룰 것인가 → **사용자 제안("같이 찍힌 이웃으로 어느 사본인지 판정")이 옳았고 워크플로와 나의 초기 반대는 틀렸다.** 반대 논거는 "사본 시트가 이웃도 데려간다"였는데, 사용자의 20×7 육안 전수조사가 매트는 **반복 시트가 아니라 커스텀 배치**임을 보여줬다 — 두 사본이 공유하는 이웃은 자기 짝(60↔61 등)뿐. 배열은 기존 맵과 **46/47 일치**(불일치 1건은 표의 12행7열 "147"이 실제 149, 관측 47회·불확실도 11.2 mm). 구현 = **confirm-or-drop**: 유일 태그 41개로 자세 확정 → 중복 태그는 `dup_confirm_px` 6.0 px 안일 때만 채택. 다른 사본 **위치를 몰라도 되는 것**이 핵심이라 재측량 없이 오늘 적용 가능. 부수 수정 2건: 한 프레임 동일 id 2회 → 둘 다 폐기(미신고 중복 자수 + min_tags 이중계수 버그, 예전엔 태그 1쌍이 min_tags=2 충족), 거절 프레임에도 tag_ids 기록(2026-08-13 기록에서 0/114·0/146이라 범인 특정 불가였음). **워크플로 주장 1건 기각**: "다중 태그 평면 거울 모호성으로 2~4 m 오차가 0.3~0.6 px로 통과" → 실측과 같은 기하 4종에서 재현 실패(거울 해가 27~65 px로 벌어져 기존 3 px 게이트가 막음). **실측 기준선 확립**(sessions/nav_runs/20260813_174043·174354): 채택률 80%/94%, 17~18 Hz, reproj p50 1.22~1.34 px — 로컬라이저는 고장난 적 없고 거절은 시야가 넓어져 중복 구역이 들어올 때 몰린다. 32/32+61/61. [memory: rov-mpc-hardware-stack]
- 2026-08-14 [실기 데이터로 전체 태그맵 재구축] `sessions/nav_runs/20260813_215640`(2984프레임/168 s/60311검출) 한 번으로 `rov_gui/tools/build_tag_map.py`가 **128 id / 140 물리 태그** 맵 생성 → `config/tag_map_full.yaml`, hw_nav.yaml 전환. 방식: 2026-05-29 갠트리 서베이 47개를 **동결 앵커**로 두고 프레임별 카메라 자세 → 같은 프레임의 미지 태그 해 → 라운드 사이에만 승격. **그래프 최적화가 없어서 경로 누적오차 구조 자체가 없음**(사용자가 두려워한 "일그러짐"의 원인이 부재). 효과: 채택률 74.3→82.3%, fix당 태그 9→23, reproj p50 1.20→1.27 px. 사용자의 20×7 육안 표와 **140칸 중 138 일치**, 오기 2건(12행7열 147→149 사용자 인정, 8행4열 54→64 미확인이라 54를 duplicate_ids에 보존). 112·113은 매트 밖 태그로 판명(112는 격자 밖 132 mm·평면 밖 62 mm) → `--exclude`. **개발 중 잡은 진짜 버그 2건**: (1) IPPE가 퇴화 사각형에서 NaN을 뱉는데 `NaN > thresh`가 항상 False라 reproj 게이트와 face-up 게이트를 **둘 다 통과**해 맵을 오염(→ `not (x <= t)`로 반전), (2) 승격 게이트를 클러스터 **전체 범위**에 걸었더니 실측에서 89/89 전량 탈락 — 태그별 중앙값 편차는 12.7 mm인데 3~4 m 이상치가 섞여서(→ mode-seeking + 중앙값 기준 inlier 필터). **기각한 개선**: 평면 제약(매트가 평평하다는 가정)은 채택률 82.3→71.6%로 악화 → z 편차 39 mm는 오차가 아니라 실제 굴곡. TagMap 다중 인스턴스 지원 추가(`"<id>#k"`), TagNav `_pick_instance`가 유일 태그 자세로 사본 선택(2등이 확실히 나쁠 때만) → 중복태그 활용 3071→9883회. 34/34+61/61+demo E2E+smoke. [memory: rov-mpc-hardware-stack]
