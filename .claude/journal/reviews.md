# P2 — Parallel-review syntheses

<!-- Populated when P2 (/review-change) lands. One block per review:
     ## YYYY-MM-DD <artifact>
     reviewers: <agents> · 합의: … · 이견: … · 리스크: … · 최소수정: … -->

## 2026-06-22 — Heavy 관성 텐서 [0.3291,0.6347,0.6109] (parallel-axis 유도, farol USD 대신)
reviewers: simulation-advisor · control-theory-advisor · underwater-robotics-advisor (병렬·독립)

- **합의(3/3):** (a) parallel-axis 유도법 타당, farol [0.21,0.245,0.245] 기각 정당(코너 질량 추가가 Iz를 낮출 수 없음 → 비물리·Gazebo hand-tune). (b) **in-sim non-issue** — MJCF plant `diaginertia` == predictor(params)라 closed-loop이 관성값에 불변; 논쟁은 sim-to-real에서만 유효. (c) 0.15kg/thruster는 예산 bookkeeping(실제 T200 ~0.344kg)이나 delta만 스케일 → 영향 작음(~0.03).
- **주요 발견(control+underwater 합의, sim도 지적):** dry inertia를 소수 3자리까지 다듬는 동안 **회전 added mass [0.12,0.12,0.12] (BlueROVHeavy.yaml)는 등방 placeholder로 방치** — 생략한 유체 r² 항이 계산한 ΔI와 동급 크기("두 동급 항 중 작은 쪽만 정밀화"). 권장 anisotropic ≈ Kp'0.07 / Mq'0.18 / Nr'0.22 (von Benzon&Fossen 2022 order로 주장됨 → P3에서 검증).
- **sim 지적:** 텐서 3중 복제(rov_model.py:54 · MJCF diaginertia · compute_heavy_inertia.py 출력)를 묶는 verify/test 없음; 스크립트가 baseline 하드코딩 → drift 위험.
- **control 지적:** Heavy는 roll/pitch commanded(NU=6) → 관성이 "don't care"→"~10-20% 중요"로 승격. EAOB는 quasi-static 오차는 흡수하나 ω̇-상관(기동 중) 오차는 못 함.  [UNVERIFIED: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- **이견:** sim/control "in-sim 수정 불요(값 유지)" vs underwater "added mass 지금 고쳐라" → 화해: in-sim 깨진 것 없음, 단 added-mass가 진짜 fidelity gap이라 다음 투자는 dry inertia가 아니라 거기.
- **리스크:** 회전 added-mass 과소·등방(sim-to-real 자세 fidelity); 텐서 3중 복제 drift.
- **최소수정(영향순):** ① 민감도 스윕(plant 고정, predictor 관성 farol vs 유도 → pitch/roll·ŵ RMS 비교 → EAOB 흡수 정량화) ② BlueROVHeavy.yaml 회전 added mass anisotropic화 ③ test_heavy_inertia 3중 일치 게이트 + 스크립트 baseline import.
- **검증법:** roll/pitch step 후 ŵ의 ω̇-상관 펄스 관찰(관성오차 지문); yaw step→Iz 직접; bifilar(공기)+in-water decay 실측 bound.

- 2026-07-01 [workflow: review-observe-mode (4 dims × 2 skeptics)] Q: teleop `--observe` 자유표류 diff 적대적 리뷰(역학누출/루프·스레드/UX) → 5지적 중 1 확정: viser observe에서 조종버튼이 여전히 보임(비기능) → 숨김 처리. 반증 4: ①"hydro.reset()이 byte-identical 깨뜨림"=오판 — reset()은 필터상태만(계수/모델 불변, hydro.py:108-114), recenter는 의도적 상태리셋이라 byte-identical은 "recenter 미발동 시"만 주장. 오히려 reset 안 하면 표류속도가 필터에 남아 거대 added-mass 스파이크 발생 → 호출이 정답(viser Reset pose와 동일) ②③ _recenter_request 락 없음 = 무해(bool·멱등, 기본경로는 매프레임 재중심) ④ recenter CSV 속도 불연속 = 의도된 상태리셋. [memory: observe-free-drift-mode]

- 2026-07-03 [workflow: review-pid-poleplacement-apply (transcription+regression × verifier)] Q: pole-placement PID 적용 diff(GAINS_HEAVY/GAINS_BLUEROV2 분기 + r_ref yaw-rate FF) 적대적 리뷰 → 전사·게인값·r_cmd 계산 무결; 실회귀 1건 확정(실행 재현): test_controller.py·test_square_mission.py가 bluerov.xml(rank-5)을 하드코딩하는데 DEFAULT_GAINS 기본이 heavy가 되면서 GAINS_HEAVY 주입 → pitch 텀블(46.8°/58.7° > 45°) 테스트 실패 → 두 테스트에 `gains=C.GAINS_BLUEROV2` 명시 고정으로 수정, 기본 env 재실행 전부 PASS. [memory: pid-gains-provenance]  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-07 [workflow: review-run-compare-extension (4 dims × adversarial verify)] Q: run_compare 원샷 배치 확장 diff(pairing:grid 방향그리드 + per-run runs/ CSV·meta + trajectory_compare 전방향 오버레이 + MPC 파랑) 적대적 리뷰 → 16 findings, 확정 6(중복 제거): ①_load_run_traj 무방비 genfromtxt — 부분/1행 CSV 하나가 그림 단계에서 main을 죽여 results.csv 통째 소실(재현됨) → try/except+atleast_1d+호출부 가드+부분파일 삭제 ②plot_trajectories._find가 새 _c/_w 파일명에서 --dir-deg/--seed 무시하고 sorted[-1]을 조용히 그림(mislabel, 재현됨) → seed·heading 패턴 추가+fallback WARN ③같은 scenario 두 블록이 runs/ 파일 경쟁 → 명시적 ValueError ④범례 n=sweep×seeds 과대표기 → n≤N 처리 ⑤단일모드 그림 full-run RMS 무표기 → 범례 title 명시 ⑥record_runs 디스크 무경고(dp 1000s×120런≈160MB) → 시나리오 길이 기반 추정 상시 출력. 반증 5(_deg_tag 충돌, wave_swept 일탈 등). 추가로 그리드 테스트가 기존 버그 발견: _prebuild_acados가 dobmpc 전용이라 mpc-only 병렬런이 tera 경합→IPOPT 폴백(20배 느림) → mpc/dobmpc 공용 프리빌드로 수정(107.6s→5.7s/run). 전부 수정 후 40런 e2e 재검증 PASS. [memory: finite-depth-disturbance-env]  [UNVERIFIED×2: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-21 [P2: simulation-advisor + control-theory-advisor (병렬 독립)] Q: MPC reference-preview 구현 diff(set_reference_traj/_xref_ned_traj + make_square_ref + run_one/run_viewer 배선 + 테스트) 리뷰 → **wrong 0건 합의**(프레임 S-conjugation·−r 부호·stage/terminal 시간정렬·unwrap 앵커·샘플러=라이브루프 정렬 모두 양측 독립 검산 통과). 확정 최소수정 3건 반영: ①meta.json에 controller.ref_preview provenance(이전 기록과 비교불가 문제 — pid_gains 전례와 동일 원칙) ②샘플러 (K,3) 전치 반환의 silent scramble 방지 shape assert ③reset()이 _ref_traj 해제(스테일 샘플러 오동작 예방). 권고 반영: 샘플러↔라이브루프 동치 회귀 테스트 추가(test_square_ref_matches_live_loop, 1랩 <1e-9). 이론 리뷰 판정: mid-turn sway 속도참조 = position preview와 유일하게 운동학적 일관 선택(정답), sharp square 참조 유지가 벤치마크로 옳음(smoothing은 과제 변경), RTI warm-start는 오히려 개선 예상(vertex에서 전 stage 90° 동시회전 → tick당 1샘플 미끄러짐), QN=Q·terminal r(t0+N·dt) 정합. 이견 없음. 실험 재실행은 사용자 몫. [memory: mpc-reference-preview]

- 2026-07-23 [workflow: eaob-review-fanout (R1/R4/R5 병렬 3 agents) + 수치검증 + 폐루프 A/B] Q: 외부 EAOB 리뷰(C0–C4) verify-first 검증 → 재튜닝 적용. R1–R6 전부 실코드 확정(노이즈 주입 전무·R=스칼라I·deadbeat 실측 6.8–13.4 ms·EAOB_* 소비처는 eaob.py 기본 kwargs뿐·게이팅/NIS 부재+KNOWN_ISSUES가 요구·부호/프레임 OK). 적용: perf 프로파일(시그마 유도 블록대각 R + tau→K→q 레시피, 노브 EAOB_TAU_DIST 하나) + 센서노이즈 주입(meas_noise) + verify/verify_eaob.py(NIS/NEES/RMSE/운동상관). 반복 로그: τ 0.5 CDW 실패(NIS 25.4/NEES 77) → 0.3/0.2/0.1 스윕 → 0.2 확정(CDW 17.1/15.0 PASS, CD NIS 14.4 PASS). 스펙과 다르게 한 것 2건: ①χ² 게이트는 구현했으나 기본 OFF — A/B 실측(τ0.5)에서 25–76% 기각으로 파랑 추종 차단(CDW 4.5→15.5 cm), 게이트 ON 측정은 NIS 통계도 오염(137 vs 25); ②CD NEES ~9(<14)는 실기체 보수성(SIG_ALLOC 등)이라 시그마 하향 반복 안 함. 헤드라인 보존: DP C dc −0.11 cm vs MPC +2.13. 정직한 비용: square CD 1.02→1.21, CDW 3.09→3.51 cm. meta controller.eaob = 새 provenance 경계. [memory: eaob-perf-retune]  [UNVERIFIED×6: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-23 [workflow: mpc-state-source-review (R4 소비처/R3 yaw·bound 병렬 2 agents) + 폐루프 A/B 12런] Q: MPC 상태 입력 truth→EAOB 추정치 전환 spec verify-first 검증 → 적용. R1–R6 확정: x_ned는 소비처 1개(단일 스위치 지점 증명), EAOB update가 solve보다 선행(지연 0), 두 솔버 모두 yaw/위치 bound 없음+EAOB yaw는 연속 신호 → C1 unwrap 누산기 불필요(스킵), lazy-init이 첫 읽기값 시작이라 C2 워밍업 불필요(스킵), truth 소비처(recorder/mission/각 runner) 전수 확인. 적용: MPC_STATE_SOURCE="estimate"(dobmpc+perf 전용) + _psi_ned_now 동반 전환 + meta mpc_state_source(세 번째 경계) + verify_state_source.py(A/B: 추종/chatter/포화/NIS/NEES/yaw연속). 구파랑 A/B 전부 PASS: DP C 0.33→1.46, CDW 1.51→2.22 cm; square CDW 3.51→3.92 cm; NIS/NEES 루프 폐합 무영향; square에선 chatter 오히려 감소(관측기=상태필터). 검증 중 발견: base.yaml 파랑이 19:28에 강화(Hs 1.2/Tp 6)돼 현 config CDW는 소스 무관 NIS 41–80 실패 → KNOWN_ISSUES 등재, 재튜닝은 사용자 몫(과제 제약 준수). [memory: mpc-state-source-switch]  [UNVERIFIED×2: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-23 [P1 control-theory-advisor 상담 → 구현: DOB 플러그인 구조 + 3-source A/B] Q: EAOB를 순수 disturbance 플러그인으로(MPC x0 = state estimator 출력, EAOB eta/nu_hat 폐기, w_hat만 MPC로) — 사용자 지시로 우선 noisy measurement를 x0로. 구현: `MPC_STATE_SOURCE` 3-way("meas" 신규 기본=estimator출력, "estimate"=EAOB필터 opt-in, "truth"), _control_step에서 noisy meas를 EAOB·x0 공유 샘플로 뽑아 3-way 분기; plain mpc도 noisy x0로(단일변수 ablation); verify_state_source에 --sources + meas 추가. 검증(구파랑, truth→meas→estimate): 추종은 소스 무관 거의 동일(DP C 0.33/1.44/1.46 cm)이나 **"meas" chatter가 truth의 9–15배**(chatF 0.71/10.89/1.18 N/tick), effF ~2–3배 — MPC가 5cm/tick 지터 직접 추종; "estimate"는 동일 추종을 truth 수준 chatter로(=상태 필터). 발산 없음/포화 0/n_fail 0/EAOB yaw 연속(|Δψ̂|≤0.08). advisor 결론(실기 SLAM+IMU 융합이 스무딩 소유, 시뮬 5cm 백색은 비관적)과 일치. 완화(SLAM-proxy KF 등)는 estimator 확정 후 사용자 결정 — EAOB 튜닝 불변(제약 준수). [memory: mpc-state-source-switch]  [UNVERIFIED×2: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-24 [측정노이즈 축소: sigma 스케일 스윕 + A/B 재검증] Q: noisy measurement가 추종오차보다 커서 x0 chatter 유발 → 추종오차 아래로 축소 요청. sigma 스케일 스윕(pos 5/2/1/0.5/0.25 cm, square C meas)으로 chatter/tracking/NIS 곡선 확인 후 **x/y 0.5 cm** 선택(추종오차 ~1.2 cm의 0.4배). params.EAOB_SIG_ POS/ANG/LVEL/AVEL만 축소(good SLAM+IMU 값), ACC/AACC/ALLOC(R_tau·q_dist)는 불변→w_hat 보존. A/B(gentle): meas chatF 10.9/11.8/12.0 → 1.76/2.91/3.08(truth의 ~2–3배, 이전 ~15배), 추종은 소스 무관 truth 수준, DC 편향 누출 소멸, 상태추정 pose~1.6mm. w-RMSE 0.24/0.34/0.39 N(구 0.29/0.41/0.48보다 좋음). NIS/NEES는 ~13/9로 목표 18 아래=보수적(과신 아님)→verify_eaob·verify_state_source 게이트를 과신(상한 24)만 판정하도록 변경(하한은 conservative 보고). meta에 eaob.sig_pos_xy+mpc_state_source 기록(5cm 시절과 경계). 별건으로 base.yaml에 6-스펙트럼 sea-state 사다리(storm 1.8→gentle 0.75) 추가+smoke 검증; run_compare 4500런 규모/2700 낭비(NONE/C/CD 파도무관 중복) 경고+trim 권고는 사용자 대기. [memory: eaob-perf-retune, mpc-state-source-switch]  [UNVERIFIED×4: 산출물 없음/예시/유도값 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-24 [P2 워크플로 3렌즈(mpl-correctness/edge-integration/consistency)+검증, 세션한도로 1렌즈만 완료 → 인라인 보강] Q: run_compare 그림 3건 수정(사용자 요청: xwave bar y축 공유+x라벨 겹침해소+범례 1개 하단; trajectory 패널 reference square 최전면; wave_comparison에 CW·CDW sea-state별 궤적 합성 2장) 후 신규 그림코드 리뷰. mpl-correctness가 1건 적발(검증 에이전트는 세션한도로 미실행 → 내가 육안+코드로 확정): reference square를 zorder 6/6.5로 올리자 RMS 주석 ax.text(기본 zorder 3)가 사각형 점선·코너마커 아래로 내려가 숫자가 덧그려짐(NONE/C 등 저외란 패널) → ax.text 2곳 zorder=10로 수정, ALLMODES 재생성해 겹침 해소 확인. edge/consistency는 인라인 점검(caller는 main 1개뿐·테스트 없음, preview_jobs→wave_dirs 순서 일치, seed_override 전파가 run_wave와 동일, record_runs=false/runs 부재 시 len(panels)<2로 무해 스킵, 단일 ctrl에도 legend ncol≥1, 트래젝토리 블록만 try-guard라 bars·CSV 손실 없음) → 추가 결함 없음. 그림 재생성은 재시뮬 없이 runs/ CSV+config+results_all_waves.csv로 수행(scratchpad regen_figs.py). README/docstring/[memory: wave-spectrum-sweep-preview] 갱신.

- 2026-08-03 [P2 워크플로 5렌즈(lifecycle/dataset-validity/axes-config/edits-blast-radius/safety-resources) + 건별 adversarial verify, 38건 제기 → 세션한도로 verify 20/38 완료] Q: 신규 `c3_camera/c3_option_sweep.py`(컬러해상도 × 압축 × depth on/off × depth해상도 스윕, 셀마다 완전한 TUM RGB-D 데이터셋 + results.csv) 및 source.py/dataset.py 수정 리뷰. **선행 매핑 워크플로가 잡은 결정적 사실 2건**: (1) depthai 2.32에 `ImgFrame.getFrameType()`이 **없어서**(hasattr False 실측) frame_type이 항상 ""→ DatasetWriter의 첫 I프레임 게이트가 영영 안 열림 → **h264/h265 데이터셋은 0프레임 기록**(c3_collect도 동일 영향); (2) `c3_dataset_check`는 depth.txt/associations.txt 공백을 hard FAIL로 봐서 `--depth off` 셀을 정상인데 "고장"으로 판정. 리뷰가 추가로 확정한 주요 결함과 조치 — `row["ok"]=1` 무조건 대입(추출 실패해도 성공으로 보고) → `ok = recorded and not error` + 테이블에 NOT USABLE 행; post-roll(writer.close/메타/ffmpeg)이 `with C3Source` **안**에서 실행 → h26x blocking 큐를 수 분간 안 읽어 디바이스 stall → with 블록 밖으로 이동; `finally`의 `writer._closed` 게이트가 뒤집힘(close()가 첫 줄에서 플래그 세팅 후 최대 120s flush → 그 창에서 Ctrl-C 시 metadata.json 없음) → **artefact 존재 여부로 게이트**; 실패 셀이 이전 실행 디렉터리 크기를 자기 것으로 보고 → `writer is not None` 가드 + 비어있지 않은 셀 디렉터리 **기록 거부**; `--min-free-gb`가 `written % 40`(3개 writer 스레드가 증가시키는 카운터)라 사실상 미발동 → 2초 wall-clock 주기; Annex-B 스캔 2회/패킷을 캡처 스레드에서 per-byte로 수행 → `bytes.find` 단일 스캔(**232x 빠름**, 20만 랜덤+짧은문자열 전수에서 출력 동일 확인); h26x 셀 디스크 추정이 비트스트림만 계산(추출 PNG 미반영) → 계획표 보정 + 추출 전 여유공간 확인; `Camera.fps: {fps:g}`가 7.5 같은 실수 노드를 뱉어 ORB-SLAM3 `readParameter<int>`가 exit(-1) → **정수 고정**; RGB-D yaml에 `Stereo.ThDepth`/`Stereo.b` 누락 → 추가(구 파서용 `ThDepth`도 병기, 캘리 실패 시 공칭 75mm 명시); `_render_metadata`가 모든 데이터셋에 "pixel-for-pixel RGB-D"를 무조건 주장 → 실제 기록 기하로 **분기**; `imu_stats`가 warm-up 표본 포함 → metrics와 함께 리셋; mavlink `message_counts`가 스윕 누적 → per-cell `records_this_dataset` 별도 기록; `rgb_files`가 인덱스 줄 수인데 이름은 파일 수 → `rgb_index`/`rgb_on_disk` 분리(추출 실패가 CSV에서 보이게). 회귀: 기존 186 테스트 전부 통과 + 신규 `tests/test_option_sweep.py` 32개(가짜 C3Source로 데이터셋 e2e 포함). telemetry.csv 스키마 레이스는 공유코드 선재결함이라 미수정 → KNOWN_ISSUES 등재. [memory: c3-option-sweep-tool]  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-08-06 [감사: 정적 스캔 + 이중환경 실행 대조] Q: robust가 python 3.14였던 기간(2026-05-04 21:28 ~ 08-06, 94일)에 나온 수치 결과 중 numpy 피연산자 손상 버그에 오염된 것이 있는가 → **저장된 산출물 중 오염 확인된 것 없음.** 손상되는 함수는 2개 찾았으나 둘 다 출력을 디스크에 남긴 적이 없다. **방법**: (1) 발동 조건을 옛 환경에서 실측 확정 — `a*b(→a)`, `b*a(→b)`, `a+b`, `a*2.0`, `2.0*a`, `-a` 전부 덮어씀, `(a+1)*b`는 둘 다; `np.sqrt`·팬시인덱싱·`.sum()`·`out=`은 무사; **임계 256 KB**(dtype 무관, float64 32768개/float32 65536개); 함수 지역변수 한정이고 코드 형태에 따라 발동/미발동이 갈림. 3.14에선 파라미터로 넘긴 배열도 refcount 1로 보여 **정적 추론이 불가** → 판정을 전부 실행 대조로. (2) AST 스캐너로 "지역변수가 산술 피연산자로 쓰인 뒤 다시 읽히는 곳" 전수 추출 = 182파일 630후보. (3) `robust314_bad` vs `robust`에서 같은 명령을 돌려 **경로 제외 내용 해시 + stdout diff**. **노이즈 바닥 확정**: 두 numpy 빌드는 SIMD 커널이 달라 최대 3.0e-16(≈1.4 ULP) 차이 — `make_synthetic_capture`의 ground_truth.json에서 실측(다른 float 61개, 중앙값 1.6e-16), 판정 기준 1e-14. **동일 확인**: marinegym 13테스트·verify 6종(특히 `verify_state_source` 출력 60줄 radRMS/NIS/NEES 전부 동일)·c3_camera 4스위트 193개·`umi_handheld.warp`(PNG 456개 바이트 동일)·`extract_gripper_width`·`record --source synthetic`·`make_synthetic_capture`(이미지 166개 동일, JSON만 1.4 ULP)·`rebuild_trajectory_html`(궤적 7,547행 바이트 동일)·`fov_audit`·`gen_pool_apriltags`·`c3_dataset_check`. **손상 2건**: `c3_encode_quality.ssim`(동일 이미지 SSIM 7.05e8, `a*a`→`b*b`→`a*b`가 연쇄로 덮어써 사실상 a²·b²) — `c3_camera/encode_quality/` 부재 = 한 번도 실행 안 됨; `geometry.depth_to_xyz`(z 최대 109 km, 점 42146 vs 86403) — 호출자 3곳 중 저장 경로인 `save_pointcloud`의 `.ply` **0개**, `c3_depth_accuracy` 미실행, 나머지는 라이브 미리보기. **실행 못 한 경로**(하드웨어 15파일, 최대 노출은 tagslam_core 52후보/442산출물)는 크기 논거로 판정 — 변수가 3-벡터·태그코너(태그당 4점)·LM 야코비안·프레임별 스칼라이고 저장된 최장 궤적이 7,547행(60 KB)으로 임계의 1/4, 같은 모듈 오프라인 절반은 실행 대조로 동일 확인. 기록: [docs/NUMPY314_AUDIT.md](docs/NUMPY314_AUDIT.md), KNOWN_ISSUES 해당 항목 삭제(해소). 옛 환경 `robust314_bad`(2.6 GB)는 재현용으로 보존. [memory: environment-numpy-constraint]
- 2026-08-10 [P2 workflow: 3 reviewers × adversarial verify, 9 agents] 대상: pose 파이프라인/3D맵/depth 프로브 diff → 6건 확정·전부 수정: _log 섀도잉(사전 발견 재확인), close()-로드 레이스(mid-torch abort), 더블클릭 후행 Release 재장전, TRACK-off에도 프레임 복사, CAMERA 레코드 in_air 역방향(감사와 모순 — medium:water로 정정+지상 1.33배 경고 반전), MESH 원샷/CSV t=0. 테스트 61/61 양env. [memory: rovgui-pose-env]
- 2026-08-12 [P2 workflow: 5 finders × adversarial verify, 18 agents(10 완료, 8은 세션한도)] 대상: rov_gui `--mpc` 폐루프 스택 전체(신규 rov_gui/control/ + 배선) → 13발견 중 5건 확정·전부 수정: (1) 테이크오버 시 all_stop()이 파일럿의 held key까지 소거(기체가 관성 주행하는 동안 조종사는 후진을 쥐고 있다고 믿음 → takeover latch로 분기, 테스트로 고정), (2) acados 첫 빌드 중 stop() 타임아웃이 실행 중 QThread 참조를 버려 Qt abort(→ _ORPHANS 파킹+60s drain), (3) START TRAJ가 배치된 square의 지오펜스 검사 없음(→ 코너 4점 사전 검사+거부), (4) MpcWorker 행 시 창이 ENGAGED에 영구 latch(→ fresh["mpc"] 워치독 1.5s로 창 자체 해제), (5) HEARTBEAT 스냅샷 무한 보존으로 armed 인터록이 죽은 스트림을 신뢰(→ _arm_state에 3s 에이징, 2026-08-07 스러스터 패널 사고와 동류). 미검증 7건 중 5건 자체 판정 후 수정(min_tags 미배선, 압력 depth 신선도, ambig CSV 칼럼, demo 가속도 ω×v 항, geometry 문서 과장), 1건 KNOWN_ISSUES 보류(QTimer 지터 vs 고정 dt), port-fidelity finder는 미실행이라 구현 시 자체 검증(참조 로직 라인 포팅+생성기 테스트 고정+E2E)으로 갈음. 수정 후 19/19+61/61+demo E2E 재통과. [memory: rov-mpc-hardware-stack]
- 2026-08-14 [P3-ish workflow: 4 설계 × 적대적 검증, 10 agents] Q: 중복 AprilTag id를 어떻게 다룰 것인가 → **사용자 제안("같이 찍힌 이웃으로 어느 사본인지 판정")이 옳았고 워크플로와 나의 초기 반대는 틀렸다.** 반대 논거는 "사본 시트가 이웃도 데려간다"였는데, 사용자의 20×7 육안 전수조사가 매트는 **반복 시트가 아니라 커스텀 배치**임을 보여줬다 — 두 사본이 공유하는 이웃은 자기 짝(60↔61 등)뿐. 배열은 기존 맵과 **46/47 일치**(불일치 1건은 표의 12행7열 "147"이 실제 149, 관측 47회·불확실도 11.2 mm). 구현 = **confirm-or-drop**: 유일 태그 41개로 자세 확정 → 중복 태그는 `dup_confirm_px` 6.0 px 안일 때만 채택. 다른 사본 **위치를 몰라도 되는 것**이 핵심이라 재측량 없이 오늘 적용 가능. 부수 수정 2건: 한 프레임 동일 id 2회 → 둘 다 폐기(미신고 중복 자수 + min_tags 이중계수 버그, 예전엔 태그 1쌍이 min_tags=2 충족), 거절 프레임에도 tag_ids 기록(2026-08-13 기록에서 0/114·0/146이라 범인 특정 불가였음). **워크플로 주장 1건 기각**: "다중 태그 평면 거울 모호성으로 2~4 m 오차가 0.3~0.6 px로 통과" → 실측과 같은 기하 4종에서 재현 실패(거울 해가 27~65 px로 벌어져 기존 3 px 게이트가 막음). **실측 기준선 확립**(sessions/nav_runs/20260813_174043·174354): 채택률 80%/94%, 17~18 Hz, reproj p50 1.22~1.34 px — 로컬라이저는 고장난 적 없고 거절은 시야가 넓어져 중복 구역이 들어올 때 몰린다. 32/32+61/61. [memory: rov-mpc-hardware-stack]
- 2026-08-14 [실기 데이터로 전체 태그맵 재구축] `sessions/nav_runs/20260813_215640`(2984프레임/168 s/60311검출) 한 번으로 `rov_gui/tools/build_tag_map.py`가 **128 id / 140 물리 태그** 맵 생성 → `config/tag_map_full.yaml`, hw_nav.yaml 전환. 방식: 2026-05-29 갠트리 서베이 47개를 **동결 앵커**로 두고 프레임별 카메라 자세 → 같은 프레임의 미지 태그 해 → 라운드 사이에만 승격. **그래프 최적화가 없어서 경로 누적오차 구조 자체가 없음**(사용자가 두려워한 "일그러짐"의 원인이 부재). 효과: 채택률 74.3→82.3%, fix당 태그 9→23, reproj p50 1.20→1.27 px. 사용자의 20×7 육안 표와 **140칸 중 138 일치**, 오기 2건(12행7열 147→149 사용자 인정, 8행4열 54→64 미확인이라 54를 duplicate_ids에 보존). 112·113은 매트 밖 태그로 판명(112는 격자 밖 132 mm·평면 밖 62 mm) → `--exclude`. **개발 중 잡은 진짜 버그 2건**: (1) IPPE가 퇴화 사각형에서 NaN을 뱉는데 `NaN > thresh`가 항상 False라 reproj 게이트와 face-up 게이트를 **둘 다 통과**해 맵을 오염(→ `not (x <= t)`로 반전), (2) 승격 게이트를 클러스터 **전체 범위**에 걸었더니 실측에서 89/89 전량 탈락 — 태그별 중앙값 편차는 12.7 mm인데 3~4 m 이상치가 섞여서(→ mode-seeking + 중앙값 기준 inlier 필터). **기각한 개선**: 평면 제약(매트가 평평하다는 가정)은 채택률 82.3→71.6%로 악화 → z 편차 39 mm는 오차가 아니라 실제 굴곡. TagMap 다중 인스턴스 지원 추가(`"<id>#k"`), TagNav `_pick_instance`가 유일 태그 자세로 사본 선택(2등이 확실히 나쁠 때만) → 중복태그 활용 3071→9883회. 34/34+61/61+demo E2E+smoke. [memory: rov-mpc-hardware-stack]
- 2026-08-14 [운영자 피드백 4건 반영] (1) teleop 기본 출력 20→50% (물속 항력에서 20%는 안 움직여 오히려 스틱을 크게 쓰게 됨). (2) 미션 행 위젯 가독성: 원인은 테마가 아니라 **위젯 개별 `setStyleSheet`이 앱 전역 QSS를 대체**해 Qt 기본(밝은) 룩으로 되돌아간 것 — 인라인 시트를 지우고 폰트만 조정, theme.py에 QComboBox/QAbstractSpinBox 규칙 신설. (3) **START 시 화면이 틀어지던 문제**: 플롯이 datum 프레임이라 engage yaw만큼 매트 전체가 회전했음 → **플롯을 MAP 프레임 고정**으로 뒤집고(`view.set_datum()`이 MpcStatus만 맵으로 환원) 맵태그·풀은 아예 변환하지 않음. NavFix도 datum 변환 제거. (4) **접근+정착 단계 신설**: START가 멀다고 거부하지 않고 DP로 원점 태그까지 날아가 `settle_s`(10 s) 대기 후 경로 시작; 도착 판정 start_err_max_m, 드리프트 시 타이머 리셋, approach_max_s(180 s) 타임아웃, STOP/DISENG이 취소. 이를 위해 **지오펜스를 MAP 프레임으로**(`geofence_frame: map`, 기본) — datum 상대 상자로는 "풀 안에 있어라"를 표현할 수 없어 풀 가로지르기가 불가능했음. 42/42+61/61+demo E2E(square·line). [memory: rov-mpc-hardware-stack]
- 2026-08-14 [풀 세션 회귀 3건] 운영자 보고 "line START했는데 reference가 안 그려지고 그 자리에만 있음" → **하나의 원인**: 내가 지오펜스를 datum→map 프레임으로 바꾸면서 z 밴드 `[-1.0, 2.0]`(datum 기준 숫자)을 그대로 옮겨 적었는데, 태그 프레임은 바닥이 z=0이고 +z가 아래라 **매트 위 기체는 음수**(실측 −1.30~−0.54, sessions/nav_runs/*/fixes.csv). 운용 범위 절반이 펜스 밖 → START TRAJ가 배치검사에서 거부 → traj_on=False → scenario 없음 → reference 미표시 + DP hold 유지(“그 자리에만 있음”의 정체). 수정: z `[-1.8, 0.15]`, 실측 기록에 대고 고정하는 회귀 테스트 추가. **부수 UX 버그**: 거부가 로그 줄로만 나가서 chip은 계속 `engaged (DP hold)` → `_refuse()`가 self.reason도 세팅(`START refused: …`). 데모 백엔드도 태그 프레임을 잘못 모델링(기체가 매트 아래 z=+0.4)해 같은 펜스에 걸렸음 → `mat_depth=1.4` 도입해 tag z = depth − mat_depth. 추가 요청: 플롯의 ROV를 점 대신 **17×21 inch 선체 사각형**(heading이 17 inch, `rov_footprint_m`), 축 눈금의 `-2.77556e-16` 부동소수 찌꺼기 제거. 44/44+61/61+demo E2E(square·line). [memory: rov-mpc-hardware-stack]
- 2026-08-14 [풀 세션 피드백: line이 출렁이고 단계 구분 불가] (1) **STATION 모드 신설**(shape: station) = 태그 위로 가서 경로 없이 정지 유지 + **절대 방위** `yaw_map_deg`(기본 90=맵 +y). 사용자 요청대로 line 전에 먼저 검증할 미션이고, 기본 shape을 station으로 전환. datum 상대 `yaw_fixed_deg`로는 "+y를 봐라"를 표현할 수 없다는 게 핵심(engage 헤딩마다 뜻이 달라짐). (2) **출렁거림 원인 = 접근 설정값 순간이동**: 목표점을 2 m 밖으로 한 번에 던지면 위치 제어기엔 풀-오소리티 스텝 명령 → 오버슈트+워블. **설정값 램프**(approach_speed_m_s 0.10) + **하드 리시**(approach_lead_m 0.35, 스텝 게이트가 아니라 결과 클램프라 느린 틱에도 안 새어나감)로 교체. (3) **phase를 MpcStatus에 신설**(warmup/approach/settle/station/line/square + phase_detail) → 칩이 `GOING TO START 0.83 m to go` / `SETTLING 6 s` / `STATION HOLD`로 표시. 예전엔 접근 중에도 `DP HOLD`라 "태그로 가는 중"과 "선을 도는 중"이 같아 보였다. (4) 주황 점선 상자가 뭔지 모르겠다는 질문 → 플롯에 `GEOFENCE` 라벨 추가(engage 중에만 그리는 건 유지). 45/45+61/61+demo E2E(station·line). [memory: rov-mpc-hardware-stack]
- 2026-08-14 [풀 세션 피드백 2차] **버그 2건 확정·수정**: (1) `dir_deg`가 **datum 상대**로 해석돼 "맵 +y로 1 m"가 실제로는 −x로 그려짐(설정 주석은 이미 map이라고 주장 중이었으니 코드가 틀린 쪽) → arming 시 `dir_deg -= yaw0`로 맵 절대 방위 변환, `scenario.dir_map_deg`로 원래 값 보존, engage yaw 0/30/−115°에서 같은 물리 방향임을 테스트로 고정. (2) **`HwPid`에 `set_line_ned`가 없어서** PID+line이 시작점까지 가고는 아무 일도 안 일어남(AttributeError) → 구현 + "HwPid가 워커가 부르는 모든 shape을 갖고 있는가" 테스트 추가, demo E2E `pid line` 통과. **UI**: 플롯 텍스트를 `err/lap`(+w_hat)만 남기고 정리 — 위치·측위·솔버·태그수·거부사유는 각각 SENSORS·칩·MISSION LOG에 이미 있음. **MISSION LOG 신설**(PAYLOAD의 CAMERA TILT 아래 빈 공간, `bus.mpc_event`): 벽시계 시각 + 짧은 영문 한 줄(`going to tag 79 (1.24 m)`, `arrived, held 10 s`, `LINE started`, `LINE complete`), events.log와 동일 문자열. 2줄 최소치는 레이아웃 예산이 정한 값(3줄이면 --mpc 최소높이 1011 > 1000 예산). 47/47+61/61+demo E2E(station·line·pid line). [memory: rov-mpc-hardware-stack]
- 2026-08-14 [실기 4주행 사후분석 → 수정] 대상: `sessions/mpc_runs/20260814_183736`(mpc station)·`_183901`(mpc line)·`_184151`(pid line)·`_184443/_184456`(dobmpc line). **(1) station "그 자리에 못 감"은 사실 4.2 cm DC offset**: 정착 후 err p50 5.0 cm/p95 8.1인데 평균 오프셋 (+4.1,−1.0) cm에 그 주위 산포가 **1.2 cm**뿐이고 축 명령은 0.042(≈2.5 N)에 포화 0% — 배회가 아니라 **일정한 힘에 대한 nominal-MPC 상수 오프셋**(sim에서 이미 측정된 그 현상, C모드 2.75 cm→DOB 0.16). 처방은 DOB-MPC 또는 P4 축게인 캘리브. **(2) "trajectory tracking이더라"가 정확**: along/cross 분해 결과 cross 0.7/0.6 cm vs along 13.8/23.6 cm(비 9.4×/16.3×) — 기체는 선 위에 있었고 늦었을 뿐. → **governed path clock 도입**(`_advance_path_clock`: 가상 목표가 `path_lead_m` 이상 뒤처지면 시계가 감속, 2배에서 정지). 같은 주행 기록 재생 검증: along p50 13.8→**7.5**, 23.6→**6.4** cm, 경로 96~99% 완주, cross 불변. 벽시계 백스톱 `traj_timeout_factor: 3`. **(3) dobmpc 실패 = `tag fix stale` 단발 샘플**(0.51/0.54 s): tag_age는 **fix rate가 아니라 카메라 지연이 지배**(mpc/pid p50 0.14·p95 0.19 vs dobmpc p50 0.26·p95 0.40)이고, dobmpc의 acados solve가 p99 17~32 ms로 튀어 공유 프로세스를 늦춘다(프로파일: EAOB 자체는 0.99 ms, 스파이크는 솔버 쪽). 게이트 0.5 s는 dobmpc에 헤드룸 0 → **0.7 s + `tag_stale_hold_s: 0.4` 디바운스**(다른 인터록은 이미 전부 디바운스됨), 디바운스 중엔 명령을 아예 안 냄(sink deadman이 담당). 49/49+61/61+demo E2E(station·line·dobmpc line). [memory: rov-mpc-hardware-stack]
- 2026-08-14 [진단: "line/square가 DP만 하고 시작 안 함"] 스크린샷 상태바가 `outside geofence_ned x[-0.57,1.15] ... Shorten the path or widen the fence` → **그 코드 경로는 이미 제거된 상태**(지오펜스 전면 삭제)이므로 실행 중이던 것은 **구 빌드**. 두 번째 증거: MISSION LOG에 `ENGAGED (mpc)`만 있고 `LINE requested`가 없음 — 현행 코드는 START 즉시 그 줄을 남긴다(구 코드는 펜스 검사가 그 앞이라 조용히 거부). 현행 코드 검증: 실제 config로 station/line/square **셋 다 정상 무장**, 1 m 떨어진 지점에서 engage→접근→10 s 정착→`LINE started` 전 구간 통과, `dir_map_deg` 90° 보존(datum −10°). **부수 발견 1건(구 빌드의 실제 버그)**: 펜스 검사가 `_mission_points`를 **dir_deg 변환 전** sq로 호출해서, 검사한 끝점과 실제 무장될 경로가 서로 달랐다 — 그래서 +y 2 m가 맵상 (−1.70,−0.53)로 보고됐다(펜스와 함께 제거됨). **재발 방지**: MpcWorker.setup이 미션 로그에 **빌드 지문** 한 줄을 남긴다(`ready: dobmpc, path-following, no geofence, mission=station @tag79`) — "지금 최신 코드인가"를 한눈에. demo_e2e 드라이버가 10 s 정착을 못 기다려 실패하던 것도 수정(SETTLE_S). 49/49+72/72+demo E2E(station·line·square).
- 2026-08-14 [실기 피드백: square가 기울어지고, line의 err가 안 줄어듦] **(1) square 배치 버그 = line `dir_deg`와 같은 종류**: 사각형이 **datum 프레임**에 놓여 있어 START 때 기체가 향하던 각도만큼 통째로 기울었다(운영자 스크린샷). 수정: `rot_deg`를 arming에서 map→datum으로 접고(`rot_map_deg`로 원본 보존), **`mirror_y` 신설** — FLU→NED 미러(S=diag(1,−1,−1))가 y를 뒤집는 탓에 입력 태그가 y **최대** 모서리(플롯 우하단)로 가던 것을 되돌려 **min-x/min-y(좌하단)** 로 만들고 첫 변이 맵 +x로 나가게 했다. engage yaw 0/30/91.26/−115°에서 변이 맵 축과 평행하고 태그가 c[0]이며 샘플된 참조가 그 모서리에서 시작함을 테스트로 고정. **(2) "path following 맞냐" → 맞다, 그리고 이미 작동 중**: `sessions/low_level_controller_data/20260814/2018/mpc_201843.csv`(MPC line 1.5 m, 3095행) — governed clock이 벽시계의 **0.79배**로 돌았고(governor가 없으면 정의상 1.00), **cross-track 1.0 cm(std 0.8, p95 2.4)** vs **along-track lag 11.5/12.9 cm**. 즉 `err 12.5 cm`는 전부 종방향 지연이고 기체는 선 위에 있었다. → UI/CSV를 **`off`(횡, +가 진행방향 왼쪽) / `lag`(종, +가 늦음)** 로 분리(`MpcWorker._path_split`, CSV `e_along`/`e_cross`, meta에 `reference_clock{path_following,path_lead_m}` — 이전 meta엔 이 정보가 아예 없어 사후에 regime을 알 수 없었다). **(3) 12 cm lag의 정체 = governor 평형점**: 기체가 지령속도의 **81%**(0.0404 vs 0.05 m/s)밖에 못 내니 `gov = 1−(lag−lead)/lead`가 lag를 `lead`와 `2·lead` 사이에 세운다. 실측 lag 0.125 → gov 0.75, 실측 시계비 0.79로 일치. 1차 모델 `v = k·lag`(실측 k = 0.324 s⁻¹)로 `lag = 2v_cmd/(k + v_cmd/lead)` [유도]: lead 0.10→0.121 m(79% 진행), 0.05→0.076 m(49%), 0.03→0.050 m(33%) — **lag는 lead로 직접 살 수 있고 대가는 주행속도**. 근본 처방은 DOB(EAOB) 또는 P4 축게인 캘리브(현재 axis_gain 60 N은 [예측]). **(4) 사용자가 본 "규칙적으로 붙었다 떨어졌다"**: 횡방향 리플 **±5 mm(1σ), p95 1.1 cm**, 지배주기가 왕복 방향에 따라 갈린다(외항 0.33~0.37 Hz, 귀항 0.10~0.12 Hz). 횡축 명령이 |ax_sway| p50 **0.013(전권한의 1.3%)** 뿐이고, 기체가 보고한 mean|PWM−1500|가 p50 **27 µs**(25% 구간은 25 µs 미만)라 T200/Basic ESC 데드밴드 ±25 µs [스펙, 벤더 공표치·본 리포 미실측]와 겹친다 → 이 크기의 횡방향 오차에는 **실효 권한이 거의 없다** [유도]. 즉 리플은 제어 발산이 아니라 액추에이션 분해능 바닥이며, 쫓을 대상은 12 cm lag 쪽. 51/51+72/72+demo E2E(station·line·square, 실 acados). [memory: rov-mpc-hardware-stack]
- 2026-08-14 [실기 피드백: PID square가 MPC square와 다른 모양] **원인 = `HwPid.set_square_ned`가 placement 산술을 자기 사본으로 들고 있었고 그게 뒤처졌다.** 세 군데: (1) **`size_y`를 아예 무시** — 운영자가 2.00 × 0.50 m를 넣으면 MPC는 직사각형, PID는 **2.00 × 2.00 m 정사각형**을 그림(실측 재현: path box y 0.50 vs 2.00), (2) `mirror_y` 누락으로 태그가 min-y가 아니라 **max-y 모서리**(플롯 우하단)로 감 — 원점 y=+0.70일 때 경로가 [+0.20,+0.70]에 놓임(정상은 [+0.70,+1.20]), (3) `T_run = laps·4·size/speed`라 직사각형에서 주행시간이 틀림(500 s vs 800 s). 즉 **A/B 비교의 전제가 깨져 있었다 — 두 컨트롤러가 서로 다른 미션을 날고 있었음.** 같은 종류(HwPid가 HwDobMpc를 손으로 따라가다 뒤처짐)의 3번째 사고(1차: `set_line_ned` 부재, 2차: `dir_deg` datum 해석, 3차: 이번 건). **처방 = 사본 제거**: `reference.place_square_ned` / `place_line_ned` **단일 정의** 신설(NED→FLU 미러도 여기 한 곳), 두 브리지가 위임만 함. 컨트롤러별 자유는 `dt`와 `preview_s`(MPC는 N·dt, PID는 0) 둘뿐. **회귀 테스트**: 두 컨트롤러를 실제로 무장시켜 scenario dict 동일 + 저장된 world-FLU 샘플러 출력이 97개 시점에서 전부 일치하는지 확인(공유 헬퍼를 **한쪽만** 부르면 메서드 존재 검사로는 안 잡히므로 경로 자체를 걸어봄) + 직사각형이 2.0×0.5로 나오는지 별도 확인. `HwDobMpc`는 acados 없이 `__new__`로 세워 검사(무장 메서드는 DT_CTRL·N·set_reference_traj만 건드림). 52/52+72/72+demo E2E(pid square·mpc square·pid line). [memory: rov-mpc-hardware-stack]
- 2026-08-16 [기능+P2 리뷰: GUI 미션 속도 필드] 요청 "trajectory 할 때 속도도 조절" → 미션 행에 **speed 스핀박스**(0.01–0.50 m/s) 신설: `_emit_scenario`가 line·square에 `speed`를 실어 기존 override 경로(`sq.update(_scenario_override)` → `place_line_ned`/`place_square_ned`)로 흘려보내므로 **컨트롤러 코드 변경 0**, station은 속도 개념이 없어 숨김. 이어 3-lens 병렬 리뷰(correctness/safety/consistency, 각 발견마다 반증 시도) 12건 중 **9건 확정 → 전부 수정**. 핵심 2건: **(1) [major] 미션 동결 기준이 틀렸다** — `traj_on`만 보고 잠갔는데 워커는 **START 순간 미션을 스냅샷**해(`set_traj` → `_approach["sq"]`, 정착 후 `_arm_path(ap["sq"])`) approach+settle 구간(최대 approach_max_s 180 + settle_s 10 s) 동안 `traj_on=False`다. 그 사이 속도를 바꾸면 패널 라벨과 `_scenario_override`는 갱신되는데 **실제 비행은 옛 값**이고 meta.json의 `mission.panel_override`가 `trajectory.speed`와 모순됨 → 동결 조건을 `traj_on or phase in (approach, settle)`로 교정(warmup/DP hold는 계속 편집 가능 — 거기가 미션을 세팅하는 자리). **(2) [minor] 반쯤 타이핑한 속도가 안 날아감** — 숫자 필드는 `keyboardTracking(False)`이고 패널 버튼은 전부 NoFocus라, `0.25` 입력 후 Enter 없이 START를 누르면 focus-out이 없어 **이전 값으로 비행하면서 박스엔 0.25가 보임** → START가 `interpretText()`로 먼저 커밋하고 emit(두 신호가 같은 워커로 큐잉되므로 scenario가 start보다 앞섬). 나머지: 0.50 상한에 `[예측]` 태그(0.12 초과 비행 실적 없음, 측정 인용 규칙), 패널 폴백 0.05→**0.12**(MpcConfig.square 기본값과 단일화, 생성 시 초기값도 고정), README "세 필드/속도는 파일에서" 문구 갱신, signal payload 주석에 `speed` 추가. 회귀: `test_trajectory_panel_carries_the_speed` 신설(시드/override/station 숨김/approach·settle 동결/START 커밋 순서 고정). 73/73+55/55.
- 2026-08-16 [MPCC 전면 구현 + 코너 게이트 제거] 사용자 진단("MPC/PID가 trajectory tracking이고 horizon이 코너를 못 넘는다")은 정확했으나, 직전에 들어온 `path_tracking.py`는 **완성·연결·12/12 통과 상태였고 방향이 정반대**였다 — 모듈 문서가 "never expose the next segment until the endpoint is captured at low speed"이고 테스트 이름이 `test_square_horizon_never_previews_an_uncaptured_next_leg`. 실주행 계측으로 확정: 코너 10 cm 전부터 참조속도가 전 호라이즌 0이고 다음 변의 |y|가 60스텝 어디에도 안 나타남 = **매 코너 완전정지 후 재출발**. → 사용자 선택(B: MPCC 전면, 코너정지 완전제거)대로 구현. **설계**: x=[eta(6),nu(6),theta], u=[tau(6),v_theta], 비용 = contour 300 / lag 60 / progress 400(v_ref 추종) + 자세·레이트·R. **경로는 솔버 재빌드 없이 교체** — 6채널 파라메트릭 B-spline(px,py,tcos,tsin,psi_cmd,v_ref)을 고정 정규격자에 얹고 계수를 acados 파라미터로, 윈도우 길이도 파라미터(속도에 따라 적응). 접선을 px,py 미분이 아니라 **별도 채널로 운반**한 게 핵심(제자리 반전 `_Pivot`에서 dp/dtheta=0이라 프레임이 특이해짐). **누진 슈팅격자**(24×0.05 + 20×0.10 + 16×0.25 = 7.2 s, 60스테이지 유지, substep을 h≈25 ms로 고정 — roll/pitch가 |λ|~85라 RK4 안정영역 밖이면 폭발). **하드 교훈 2건**: (1) 윈도우를 s∈[0,L]로 클립하면 경로가 평평해져 θ₀에서 ∂p/∂θ=0 → 최적화기가 θ를 밀 이유가 없어 **미션이 조용히 시작조차 안 함**(solver status는 0 정상) → `ArcPath.sample`이 양끝 접선을 직선으로 연장; (2) `W_PROGRESS=40`은 같은 방식의 데드락(θ 정지 → v_ref가 creep에 머묾 → 자기일관적 고착), 스윕 결과 40→30 s에 0%, **400→100%**; 잔차가 속도(m/s)라 길이(m)인 contour와 같은 스케일로 놓는 게 맞다. `v_creep`도 데드락 가드(피벗·시작점에서만, 위치가 안 움직이는 곳이라 공짜). **결과**(2.0×1.0 m, 0.12 m/s, 2랩, Fossen 폐루프): 전 구간 완주 106 s, **횡오차 p50 0.2 mm/p95 2.8 mm**, 코너 p50 3.0/p95 9.2 mm(날카로운 코너의 ~20 mm 바닥 대비), **호라이즌 선견거리 0.78 m로 코너(호 0.24 m)를 넘김**, solve p50 13.1/p99 13.9 ms(engage probe 한계 25), n_fail 0. PID도 **같은 ArcPath**를 `PathCursor`(투영+lead)로 소비 → 컨트롤러 차이가 기하 차이가 되지 않음. 제거: `path_tracking.py`+테스트, `path_corner_*`/`path_brake_accel` 설정, CSV의 `path_segment,corner_state,corner_hold_s` 열. meta `reference_clock.strategy`가 3-값 경계(`wall_clock_trajectory` | `active_segment_projection_corner_gate`(08-14~16) | `mpcc_contouring`/`arc_projection_cursor`) — 경계 넘어 합산 금지. 57/57+74/74+11/11+16/16, demo E2E square 통과. **미검증: 실기 주행 전무(전부 sim 폐루프)**.
- 2026-08-17 [실기 세션 진단 3건 → 수정] 대상 `sessions/low_level_controller_data/20260817/0817_103431`. **(1) "MPCC를 돌렸는데 이상했다"는 MPCC가 아니었다**: 5개 런의 meta가 전부 `type: dobmpc`/`pid`. 원인은 내가 모드 콤보 첫 항목을 "mpcc"로 넣고 **설정값으로 시드하지 않은 것** — Qt가 생성 시 0번을 선택하는데 connect가 addItems 뒤라 신호가 안 나가, 패널엔 mpcc가 보이고 워커는 YAML의 dobmpc로 돈다. 한 세션 전체가 아무도 고르지 않은 모드로 날고 기록됐다. → `set_mode_default()`(blockSignals: 패널은 상태를 *반영*하지 명령하지 않는다) + window가 시드. **(2) 실제 이상거동의 원인 = EAOB w_hat 레일링**: `mpc_103538.csv`에서 w0가 매 틱 ±15 N(클립) 사이를 튀고 ax_surge가 ±0.5(전권한)로 **3.6 Hz 추력 반전**, |ax| 포화 51.9%. 디스크의 **모든** dobmpc 런을 감사한 결과 X 레일 점유율이 2026-08-12부터 66~95% — **기존 문제이지 이번 회귀가 아니고, 다만 아무도 말해주지 않았다**. 클립에 붙은 DOB는 추정이 아니라 발산 신호이고 NMPC는 그 레일을 상쇄하느라 전권한을 쓴다. 유력 용의자 둘 다 미실측: `axis_gain` [예측](틀리면 note_applied가 "안 나간 힘"을 보고해 그 차이가 통째로 외란으로 들어감), 그리고 신선한 측정을 가정하는 관측기에 들어가는 **tag_age p50 0.268 s**(pid 런은 0.119). → `_note_w_hat_rail()`이 3 s 연속 레일이면 error 로그 + events.log("w_hat railed: DOB diverged")로 소리치고 mpcc/mpc 사용을 권고. **근본 처방은 P4 축게인 캘리브레이션이며 그 전엔 dobmpc를 날리지 말 것.** **(3) PID 속도 미반영은 진짜 버그, 내 것**: 0.2 m/s 지령에 42 s 동안 경로 1.09 m = **0.026 m/s**(mpc_103624.csv), `ref_speed_m_s` 열이 전부 0. 원인 두 겹 — `PathCursor`가 setpoint를 **투영으로만** 전진시켜 참조가 lead_m(10 cm) 앞에 고정된 당근이 됐고(지령 속도가 루프에 아예 없음), 워커가 커서의 v_ref를 계산해놓고 `set_target_ned`에 **넘기지 않아** PID의 D항이 0에 대해 미분했다. 두 결함은 결합돼 있다: 리시가 오차를 10 cm로 묶는데, FF 없이 0.2 m/s를 유지하려면 (drag+kd·v)/kp = 27 cm가 필요해 **구조적으로 불가능**했다(FF 있으면 drag/kp = 4 cm). → 커서에 속도구동 전진(`_theta_cmd`, v_ref·dt 후 투영+lead로 리시) + 워커가 v_ned를 FF로 전달. 폐루프 재현: 0.026 → **0.128 m/s**, 프로파일 자체 평균이 0.125이므로 이제 실현가능 상한을 그대로 추종(잔여 격차는 코너한계 sqrt(a_lat·R)=0.087이라 `path_lat_accel_m_s2`로 사는 것). **(4) 요청 기능**: `MpcStatus.speed_m_s`/`ref_speed_m_s` 신설 — 칩에 `0.13/0.20 m/s`(실측/참조), 플롯 코너에 `v 0.128 m/s ref 0.200`. 이번 세션을 날린 "속도 박스가 루프에 없음"을 화면에서 즉시 읽을 수 있다. 67/67+75/75+13/13+16/16+demo E2E.
- 2026-08-17(2) [MPCC 속도·필렛 튜닝 + PID 확인 + DR 오버레이] **(1) 속도가 안 나오던 진짜 원인은 `path_lat_accel_m_s2: 0.05`**: 코너속도 = sqrt(a_lat·R)이라 필렛 0.15에서 코너가 0.087 m/s로 막혀 0.20 지령이 평균 0.125로 눌렸다. 기체 자체 한계는 a_lat = axis_cap·axis_gain/m_eff_sway = 0.5·60/26.42 = **1.14 m/s²**[예측]이므로 0.05는 20분의 1. → 기본값 a_lat 0.05→**0.30**(가용의 1/4), a_long 0.05→**0.15**. **(2) 필렛 0.15→0.06**(1 m 변의 6%, 눈으로 직사각형; PID와 같은 그림이어야 비교가 성립). 둘은 상충하는 요구인데 a_lat을 풀어야 동시 달성된다 — 작은 R은 코너를 더 느리게 만들기 때문. **(3) 그 조합이 MPCC를 완전히 얼렸다 — 새 데드락 3번째**: a_long 0.05→0.15만으로 진행이 0.118 m/s → **정확히 0**. 원인은 v_ref를 θ의 함수(스플라인 채널)로 둔 것: 잔차 (v_theta − v_ref(θ))의 Gauss-Newton 헤시안에 W_PROGRESS·v_ref'(θ)²이 들어가고, 가파른 프로파일이면 RTI 1회 반복이 못 넘는 장벽이 된다. → **v_ref를 스테이지별 yref 상수로 이동**(`v_ref_schedule`이 프로파일을 전방적분해 스케줄 생성; θ 결합 소멸, 채널 6→5로 더 싸짐). 결과 0.20 지령에 **0.178 m/s**(89%), 오차 p50 6.9/p95 25.4 mm. **(4) N_GRID는 41이 정답**: 81로 올렸다가 solve p99 26.5 ms로 게이트(25)를 넘겼는데, 41/61/81의 폐루프 결과가 **완전히 동일**(6.9/25.4 mm)하고 41만 11.8 ms — 6 cm 필렛의 스플라인 오차 1.5 mm는 25 mm 오차 옆에서 무의미. 근거 없이 올렸던 주석을 실측으로 교체. **(5) PID도 path tracking 맞음**(재확인): 투영으로 진행, setpoint는 lead 0.10 m로 리시, **MPCC와 문자 그대로 같은 ArcPath**(`path_from_scenario` 단일 정의). 같은 조건 폐루프에서 0.185 m/s = 프로파일 상한 그대로, 횡오차 p50 21.0/p95 49.6 mm(MPCC 6.9/25.4) — 이제 컨트롤러만 다른 정직한 A/B. **(6) 주황 점선 = IMU dead-reckoning 오버레이**(`--imu-dr` opt-in 실험 도구, `imu_dr.enabled: false`가 기본). 표류가 km 단위라 플롯을 덮어버려서 **기본 숨김**으로 전환 + 패널에 `DR` 토글(추정기가 실제로 보고할 때만 나타남). 단 `mode: control`이면 강제 표시·비활성 — 지금 그걸로 날고 있는 걸 숨길 수는 없다. 67/67+75/75+15/15+16/16+demo E2E.
- 2026-08-17(3) [실기 MPCC 1호 진단: "십자가를 쫓는 것 같다"] 대상 `.../20260817/0817_114023/mpc_114023.csv`(mpcc, 5랩 19.48 m 완주, 242 s). **사용자 직관이 정확했고 원인은 내가 전날 만든 것**: 오차 분해가 e_cross p50 1.67 cm 대 **e_along(lag) p50 29.06 cm** — 십자가가 0.88 m 다리의 **33%만큼 앞서 있었다**. 결정적 지표: **44.6%의 틱에서 θ와 hull이 서로 다른 경로 구간에 있었고**, 그 틱들의 |e_cross| p50이 **4.92 cm 대 1.11 cm**(같은 구간일 때)로 4.4배. θ가 contouring 좌표계의 앵커이므로, θ가 이미 코너를 돌면 기체는 **아직 도달하지도 않은 다음 변** 기준으로 조종된다 = contouring 모자를 쓴 trajectory tracking. 코너 오차(직선 1.34 vs 호 4.37 cm)의 정체. **원인**: 전날 v_ref를 θ-함수에서 스테이지 상수로 옮기며(그건 GN 헤시안 장벽 때문에 옳았다) **진행률의 폐루프 결합까지 같이 없앴다** → 스케줄이 지령 0.20 m/s로 개루프 전진, 기체는 0.088밖에 못 내 lag가 벌어짐. **수정**: `HwMpcc._update_governor` — 측정 lag로 스케줄 전체를 스칼라 배율 `gov = clip(1-(lag-lead)/lead, 0, 1)`. OCP 밖에서 곱하므로 스테이지 상수 성질이 유지돼 장벽이 재발하지 않는다. 검증(약한 플랜트 gain 0.15로 실기 재현, **참된 path 지표 = 곡선까지의 최단거리**로 측정 — 이전 스윕은 |p−ref|를 썼는데 그건 lag를 포함해 W_LAG를 높게 고르도록 편향돼 있었다): 무거버너 lag 20.6 cm·오차 2.53/6.37 cm → **거버너 lag 13.9 cm·오차 1.24/4.95 cm**(중앙값 절반). 강한 플랜트(gain 1.0)에선 lag 1 cm라 무동작 = 회귀 없음. **더 근본적인 발견 — `axis_gain`이 크게 틀렸다**: 축 명령이 전혀 포화하지 않는데(|ax| p50 0.226, max 0.290, 상한 0.5) 속도가 0.088 m/s. 그 속도의 모델 항력은 ~0.6 N인데 컨트롤러는 5~13 N을 명령 중 → **실효 플랜트 이득이 믿는 값의 1/7~1/20** [유도]. axis_gain 과대이거나 실제 항력(테더!)이 모델보다 훨씬 크거나인데, 어느 쪽이든 P4 스텝 캘리브 한 번으로 정해진다. 지령 0.2 m/s는 현 기체가 못 내는 값이라 lag는 필연이었다. **UI**: `MpcStatus.path_gov` 신설 → 칩에 `gov 0.42`(참조가 기체를 기다리는 중 = path following이 작동 중이라는 뜻이자, 지령 속도가 기체 능력을 넘었다는 신호). 67/67+75/75+16/16+16/16+demo E2E.
- 2026-08-17(4) [MPCC를 순수 path tracking으로 — 타이밍 요구 제거] 사용자 지적("mpcc가 trajectory tracking 아니냐")이 **맞았다**. 진행 항이 `W_PROGRESS·(v_theta − v_ref_schedule)²`였고 그 schedule은 프로파일을 **시간으로 전방적분**한 것 = "이 시각엔 이만큼 나가 있어야 한다"는 일정. 전날 거버너로 lag를 묶은 건 증상 완화였지 원인 제거가 아니었다. **수정**: 속도 목표를 **상수 ceiling(운용자 지령 속도)** 으로 교체. 전개하면 q·v²−2q·v_cmd·v + const = **고전 MPCC의 선형 progress reward + 정칙항**이라 시계가 아예 없고, θ 의존이 없어 GN 장벽도 재발 불가. **코너 감속이 emergent가 됨** — 코너에서 v_theta를 밀면 contouring 비용이 오르므로 최적화기가 기하가 요구하는 만큼만 스스로 줄인다(속도 프로파일이 흉내내던 성질). 거버너·스케줄·v_ref 채널 전부 삭제. **가중치 재선정**: 이전 스윕이 `|p−ref|`(lag 포함)를 지표로 써서 W_LAG를 높게 고르도록 **편향**돼 있었다 → **곡선까지의 최단거리**로 다시 스윕, 실기 재현용 약한 플랜트(추력 0.15배). 결과 W_LAG/W_PROG 60/400 → **600/100**: 약한 플랜트에서 lag 20.6→**2.5 cm**, 경로오차 3.38/6.85 → **0.77/4.17 cm**, θ가 1/4다리 이상 앞선 틱 32.7% → **0.0%**; 강한 플랜트에서도 0.50/3.12 → **0.39/2.23 cm**로 더 정확(대가는 속도 11%). **W_LAG > W_CONTOUR가 문헌과 반대로 보이지만 의도적**: e_l은 미션 요구가 아니라 **θ를 수선의 발에 붙들어 두는 파라미터화 제약**이고, θ가 앞서면 e_c가 "곡선까지 거리"이기를 그만두고 아직 도달 못한 구간 기준이 된다(그게 44.6% 구간불일치의 정체). 테스트도 지표를 교체: `test_theta_stays_at_the_hulls_foot_point_not_ahead_of_it`이 lag p50<6 cm, 1/4다리 초과 틱<2%, 곡선거리 p50<2 cm를 약한 플랜트에서 고정. 죽은 `path_gov` 배선 제거. 16/16+67/67+75/75+16/16+demo E2E.
- 2026-08-17(5) [스러스터 데드밴드 보상 — 속도 급락의 진짜 원인] 사용자 보고 "600/100으로 바꾸니 속도가 더 안 나고 오차도 커졌다"가 사실이었다: `.../0817_122659/mpc_122659.csv`는 134 s에 **1.16 m**(19.48 중), 속도 p50 0.021 m/s, e_cross p50 4.10 cm — 직전 60/400 런(19.48 m 완주, 0.088 m/s, 1.67 cm)보다 전면적으로 나쁨. **원인은 컨트롤러가 아니라 액추에이터**: |ax| p50이 0.226→0.157로 내려가며 pwm_dev가 31→**24 µs**가 됐고, T200/Basic ESC 데드밴드 ±25 µs[스펙, 벤더 공표·미실측] 아래로 **틱의 55%**가 떨어졌다(이전 20%). 그 틱은 추력이 0이다. 악순환: 기체 정지 → θ는 W_LAG=600으로 선체에 붙어 있어 θ도 정지 → 참조가 기체 위 → 밀어낼 오차 없음. **내 sim 스윕에 데드밴드가 없어서** 600/100이 좋아 보였던 것 — sim은 작은 명령에 작은 힘을 주고 실기는 0을 준다. **수정 = allocation에 deadzone inverse**: |request| > null이면 [deadband,1]로 사상해 어떤 실요구든 문턱을 넘게 하고 그 위로는 선형 복원. `null`은 장식이 아니라 필수 — 없으면 솔버 dither ±1e-6이 ±deadband 추력 반전으로 증폭된다. **0.16은 [유도]**: PWM 피드백이 있는 모든 실기 런의 engaged 틱 **121,755개**를 |ax|로 비닝하니 25 µs 교차가 0.150~0.175 구간, 선형적합 0.162. 단 pwm_dev는 깊이 유지 중인 수직 스러스터까지 포함한 8기 평균이라 수평축을 **과소보고**하므로 0.16은 하한으로 읽을 것. **EAOB에는 요구 렌치를 준다**(`requested_wrench`) — 보상된 축을 역산하면 데드밴드만큼 부풀려져 그 차이가 통째로 유령 외란이 된다. DP hold 회귀 확인(데드밴드 있는 액추에이터 모델로 30 cm에서 복귀): 보상 OFF 1.95 cm → **ON 0.84 cm**, 명령 부호반전 0회(채터 없음); null 0.06은 1.80 cm로 오히려 나빠 0.03 채택. meta에 `axis_deadband`/`axis_null` 기록(이 설정이 다르면 런은 비교 불가), engage마다 로그로 고지. 기본 코드값은 0.0(=이전 선형 사상), YAML만 0.16. 68/68+75/75+16/16+16/16+demo E2E.
- 2026-08-17(6) [데드밴드 보상 철회 + 정정 + 축 슬루 제한] `.../0817_124624` 실기에서 심한 채터. **내 데드밴드 보상이 원인**: ax_yaw |a| p50 0.012~0.043 → **0.358(~8배)**, surge 명령 on/off 토글 0.00 → **3.10/s**, pwm_dev 24~31 → 62 µs. 두 가지가 겹쳤다 — (a) 데드밴드는 ArduSub **믹서 하류의 스러스터별** 특성인데 MANUAL_CONTROL에서는 손이 닿지 않는다(축 도메인 역보상은 옳은 모양을 틀린 자리에 적용한 것; surge+sway+yaw 혼합 명령이면 어떤 스러스터는 문턱 위, 어떤 건 아래), (b) null 구간이 0→0.16 불연속이라 노이즈 있는 상태추정이 초당 몇 번씩 넘나든다 = 관측된 3.10/s 펄싱. **→ `axis_deadband: 0.0`으로 철회**(코드·테스트는 off 상태로 보존). **중대한 자기정정**: 앞서 "axis_gain이 실제의 1/7~1/20"이라 한 것은 **틀렸다**. 그 추정은 테더 항력이 빠진 모델 수평항력에 기댄 것이었다. 깊이 유지는 순부력에 대한 정상상태 힘 평형이라 훨씬 깨끗한데, **29개 런 중앙값 |ax_heave| = 0.100 → heave_n ≈ 57 N**으로 가정한 60 N과 사실상 동일 [유도; 순부력 −5.7 N 자체가 모델값이라 자릿수 수준으로만]. 즉 축게인은 대체로 맞고, 수평 속도가 안 나는 이유는 다른 데(테더 항력 유력, 그리고 명령 진동 자체의 상쇄) 있다. **채터의 주파수 성분은 데드밴드 이전부터 존재**했다(114023이 2.69 flips/s로 오히려 최고; 다만 진폭이 0.012라 체감 불가) → 액추에이터 모델에 의존하지 않는 일반 대책으로 **축 슬루 제한** 신설(`axis_slew_per_s`, allocation.slew_axes, 미전송 시 `_axes_prev=None`으로 리셋해 재engage 시 낡은 명령에서 램프하지 않음). 1.5/s로 사이징한 근거: 4 mm 상태노이즈를 넣은 MPCC 폐루프에서 명령 자체의 |da/dt| p95가 **0.93/s**라 제한이 아예 발동하지 않고 추종이 비트단위로 동일(0.39/2.15 cm, 0.169 m/s), 반면 문제 런의 yaw 10.51/s는 잘린다 = 정상 시 무비용·병리 시 결정적. meta에 `axis_deadband`/`axis_null`/`axis_slew_per_s` 기록. 69/69+75/75+16/16+16/16+demo E2E.
- 2026-08-17(7) [데드밴드 보상 전면 삭제 — P2 리뷰 결과 반영] 32-에이전트 적대적 리뷰가 내 철회 판단을 뒷받침하면서 더 나쁜 것을 찾았다. **critical**: 축 단위 pre-distortion이 믹서에서 **같은 vectored 스러스터에 2~3번 누적**된다 — 실측 `|surge|+|sway|+|yaw|` 중앙값 0.246 → **0.685(×2.78)**, 세 축이 동시에 null을 넘는 틱 65%; 크기뿐 아니라 **방향도 왜곡**((0.40, 0.031) 요청의 sway/surge 비가 4.8배). cap은 축별이라 합산 authority를 못 막는다. **major 3건(모두 잠복)**: (a) `requested_wrench`가 null을 무시해 축이 0으로 나간 틱에도 EAOB에 최대 1.8 N을 보고 → w_hat이 유령 외란을 적분하고 dobmpc가 그걸 상쇄하려 요청을 키워 **null 구역이 스스로를 탈출**(DP hold hunting), (b) `axis_cap ≤ axis_deadband`면 모든 non-null 요청이 즉시 full cap = authority 축소 노브가 bang-bang 스위치로 반전, `imu_dr.axis_cap_dr`(0.10~0.15)로 실제 도달 가능한데 검증이 없음, (c) null 임계에 히스테리시스·dwell 없음, 실측 교차가 초당 0.56~1.37회라 docstring의 "dither ±1e-6" 전제가 사실과 다름. **결론: 끄는 것으로 부족 — 함정으로 남는다.** `deadzone_inverse`·`requested_wrench`·`axis_deadband`·`axis_null` 전부 삭제하고 `axes_to_wrench` 경로로 복귀(그러면 리뷰의 minor "죽은 import"도 해소). YAML에는 코드 대신 **왜 안 되는지**를 남김 — 다시 시도하려면 MANUAL_CONTROL을 떠나 스러스터 직접 인터페이스로 가야 한다. 유지: `axis_slew_per_s`(액추에이터 모델 무관, 정상 시 미발동·병리 시 결정적). 68/68+75/75+16/16+16/16+demo E2E.
- 2026-08-17(8) [mpcc 속도·코너 근본 해결: axis_gain 실측 교정] 사용자 관찰 "코너에서 십자가가 빨리 간다"가 **정확했고 중앙값이 그걸 가리고 있었다**: 전체 lag는 3.44 cm지만 호 구간에서 v_theta가 p50 0.0389/p95 0.1143(직선 0.0238/0.0501의 1.6~2.3배), 호 틱의 **12%에서 십자가가 기체보다 3배 이상 빠르다**. 원인은 컨트롤러가 아니라 **기하**: 곡선 투영은 차량이 곡률중심 안쪽에 들어가면(|e_cross| > R) 퇴화해 최근접점이 호를 가로질러 건너뛴다. R=0.06인데 호 구간 |e_cross|가 p50 3.44/p95 8.72/max 14.55 cm — **R 초과가 17%, R/2 초과가 58%**. 즉 "거의 직사각형" 요청에 맞춰 필렛을 0.15→0.06으로 줄인 내 변경이 만든 조건이었다. **속도의 원인은 별개이자 더 근본적**: 29개 런을 pooling하면 `speed = 0.692·(|ax| − 0.096)`, r=**0.95**. x절편이 뚜렷한 양수 = 문턱(데드밴드), 기울기로 0.20 m/s에는 |ax| 0.385가 필요한데 컨트롤러는 **0.035**만 요청한다. 힘으로 환산하면 실제 `F ≈ 6.4·max(0, |ax|−0.096)` N인데 config는 60 N/축을 믿고 있었다 — **9배 과대**. mpcc는 w_hat=0이라 이걸 교정할 메커니즘이 아예 없다(dobmpcc였다면 관측기가 흡수했을 것이나 사용자 지시로 미사용). **수정**: `axis_gain` surge/sway 60→**6.4**[측정 기반 유도]. heave는 60 유지(깊이유지 정상상태가 ~57 N을 지시, 수직은 전용 스러스터라 수평과 같을 이유 없음), yaw도 20 유지(비교 측정 없음, 가장 예민한 축을 추측으로 건드리지 않음). **최대 추력은 불변** — `axis_cap` 0.5가 그대로라 명령 상한이 동일하고, 바뀌는 건 컨트롤러가 "요청하는" 값뿐. 실측 액추에이터 법칙을 넣은 시뮬레이터(마침내 하드웨어를 재현: gain 60/fillet 0.06이 0.035 m/s로 실측 0.027에 근접)로 검증: gain 6.4에서 **0.156~0.162 m/s(4.6배), 곡선오차 p50 1.32→0.80 cm, p95 3.77→1.32 cm**, 그리고 오차가 1 cm대로 내려가므로 **작은 필렛이 다시 안전해진다**(0.06에서도 p95가 0.26×R). 필렛은 sim이 실기보다 2.3배 낙관적인 점을 감안해 **0.10**으로 설정(여유 10배). 회귀 테스트 2건 신설: 필렛이 예상 횡오차의 2배를 넘는지, axis_gain이 실측 법칙과 일치하는지 — 한쪽만 줄이면 조용히 통과하지 못한다. 68/68+75/75+18/18+16/16.
- 2026-08-17(9) [1단계 원복 + 2단계 waypoint 호라이즌] 118개 런 감사가 두 가지를 확정했다. **(1) 내가 작동하던 설정을 망가뜨렸다**: 최고 기록은 `0817_110145`(mpcc, 5/5랩 18.71 m, e_cross p50 2.13/p95 6.59 cm, 포화 **0%**)인데, 이후 나빠진 네 런은 전부 내가 바꾼 `axis_gain 6.4` 하나를 공유하고 포화율이 64~77%(다른 모든 런은 0.5% 미만). 실측 법칙 `speed = 0.692(|ax|−0.096)`은 진짜지만 그건 **추력 − 테더항력의 루프 전체**를 잰 것이지 스러스터 이득이 아니었고, 그 기울기를 axis_gain에 넣자 표현 가능 authority가 30 N → **3.2 N**으로 붕괴해 축이 상시 레일에 붙었다. → axis_gain 60 원복, fillet 0.15, accel 0.05/0.05, W_LAG/W_PROG **60/400** 원복. 그 법칙의 진짜 귀결은 **예측모델의 항력**(8~12배 과소)이지 여기가 아니다. 시뮬레이터 스윕이 600/100을 골랐던 건 그 플랜트에 ESC 데드밴드가 없고 항력이 낮아서 "밀기 대 기하" 노브를 순위매길 수 없기 때문 — 주석에 그대로 남김. 되돌린 설정이 110145 meta와 일치하는지 **산출물에 앵커한 회귀 테스트**(`test_shipped_settings_match_the_best_recorded_mission`) 신설, 내가 또 표류하면 잡힌다. **(2) mpc/dobmpc 호라이즌에 경로 기하가 아예 없었다**: `_arm_path`가 `set_square_ned`로 샘플러를 심는데 바로 다음 틱의 `_advance_path_clock` → `set_target_ned`가 마지막 줄에서 `_ref_traj = None`을 해버려(mpc_bridge.py:328), NMPC가 폴백으로 **단일 목표점을 접선 방향 직선으로 61스테이지 외삽**했다. 즉 추종 MPC는 코너의 존재를 모른 채 매번 직진으로 뚫고 지나갔다. 소비자(`_xref_ned_plan` N+1, `HwPid` 1스테이지)는 멀쩡히 살아 있고 생산자만 없었다(path_tracking.py 삭제 이후) → `path_geometry.NedPlan` + `PathCursor.plan(steps, dt, ...)` 신설, 워커가 `set_path_plan_ned`로 설치. **코너 처리는 사용자 선택 A안**: 필렛 없는 꼭짓점을 헤딩 불연속으로 감지해 속도를 고정 — 참조가 감속해 도착하므로 90° 방향전환이 |v|≈0에서 일어나 **속도 참조가 연속**이고, 멈춰 있는 동안 호라이즌 전 스테이지가 그 점에 모여 setpoint regulation이 저절로 된다(모드 전환·불연속 없음). 여기서도 데드락 한 번: 꼭짓점에 v=0을 정확히 두면 커서가 영원히 못 떠나 첫 코너에서 미션이 멈춘다 → 피벗과 같은 creep 바닥(0.02 m/s) 적용. sim 폐루프(추종 MPC + waypoint 호라이즌, 필렛 0): 12/12 m 완주, **곡선까지 거리 p50 0.4 mm / p95 1.6 mm**, lag 0.52 cm, 솔버 실패 0. `plan(1)`이 기존 setpoint와 비트단위 동일해 PID는 불변. meta strategy에 `waypoint_vertex_stop` 신설(합산 경계). 72/72+75/75+17/17+16/16+demo E2E. **실기 미검증** — 1단계 재현 확인이 먼저다.

- 2026-08-17(10) [workflow: 4 code-readers + 1 completeness critic] Q: 네 번째 미션 모양 **circle**(입력 태그 = 원의 맨 아래점, 반지름 선택)을 넣으려면 어디를 다 건드려야 하나 → 접점 260개 + 크리틱이 추가로 21개. 구현은 기존 뼈대에 얹었다: `_Arc` 하나(turn=2π)라 필렛도 피벗도 불필요, 배치 변환은 square와 공유(`_placed`), 세 브리지 모두 `set_circle_ned`. **크리틱이 잡은 진짜 결함 2건**(둘 다 circle이 드러냈을 뿐 원인은 공용 코드) — (a) `PathCursor` 탐색창이 랩 길이와 무관한 고정 0.60 m라, 원의 방사 투영에서 선체가 중심 쪽으로 밀리면 창 전체가 등거리 → argmin이 엉뚱한 곳을 고르고 theta는 단조라 영구 전진: r 0.15 m(랩 0.94)에서 한 틱에 랩의 **64%** 점프 실측 → 창을 랩의 1/4로 제한; (b) `HwMpcc._install`이 창을 **지령** 속도로 재고 있어, 곡률이 금지한 속도를 넣으면 창이 랩보다 길어짐(r 0.15 @ 0.50 m/s → 창 4.9 m = 5.2랩, B-스플라인 재현 **3.2 mm**) → **실현 가능** 속도로 변경해 0.12 mm, 다각형은 직선에서 둘이 같아 기존 미션 불변. **크리틱 지적 중 기각한 것**: "heading_follow 원은 시작에 90° 슬루가 생기니 yc를 접선으로 시드하라" — 그러면 t=0에 90° **계단**이 되고 `slew_heading`이 존재하는 이유가 바로 그걸 피하는 것이다(square도 rot_deg≠engage heading이면 같은 슬루를 이미 한다). 부수: meta schema 3→4(`trajectory`가 모양마다 키가 다름), 곡률 상한 수치에 [유도] 태그와 고정 테스트 부착, `control/__init__.py`의 "sim 검증 상속" 문구·`allocation.py`의 roll/pitch 근거·`hw_nav.yaml` wall 기하 주석 정정(크랩 원은 헤딩은 유지해도 range 0..2R·lateral ±R **이동**한다). 테스트 +13(control 78/78, mpcc 24/25 — 1건은 트리에 이미 있던 `path_fillet_m: 0.0`과 충돌하는 기존 실패, offline 77/77, plot_runs 17/17, imu_dr 24/24). **실기 미검증.** [memory: circle-mission-shape]
- 2026-08-30 [safety-code-reviewer] Q: replay 미션(plan_stream+gripper 경로) 구현 안전 감사 → **FAIL 판정, 전부 수정 후 재검증 green**. CRITICAL: sink 딜맨이 axes만 중립화하고 **래치된 gripper 버튼 비트를 안 놓음**(워커 사망+jaw close 래치 = 20 Hz로 close 비트 영구 송신, heartbeat는 살아있어 GCS failsafe도 안 뜸) → 딜맨 stale 에지에서 grip/tilt drive 0.0. HIGH: replay 참조가 벽시계로 행진하는데 기하 미션의 PathCursor leash 같은 **기체-참조 발산 가드가 없음** → 2×anchor_max(0.6 m)/0.5 s 디바운스 가드 추가(초과 시 정지+DP hold). MEDIUM 3(one-shot 기각이 조용히 "complete"로 둔갑 → 정직한 즉시 종료 / anchor·jump·box NaN 통과 → config 검증 / _tick_replay 예외 = engaged 좀비 → try/except+disengage). LOW 3(box 미설정 경고, 첫 샘플 grip 에지 방지, obs_t opt-in 함정→KNOWN_ISSUES). 부수 발견 2건: 필터가 코너 가속을 기각만 하던 것 → **시간 팽창이 3개 위반 공통 수리**(α가 v를 1/α, a를 1/α², r을 1/α로)로 확장; demo_e2e가 기각된 replay를 완주로 오판 → end_reason 정직성 체크 추가(첫 e2e "OK"는 거짓 통과였음). jaw 중재 잔존 갭은 M0 조건부 수용(에지 방출+기본 OFF+딜맨) [memory: replay-mission-plan-stream]
