# P1 — Specialist-advisor consults

<!-- One line per consult: - YYYY-MM-DD [agent] Q: … → conclusion [memory: slug] -->

- 2026-06-22 [hardware domain · answered by main] Q: BlueROV2 C3 (= MarineSitu C3) depth camera에 IMU 내장? → C3 사양엔 IMU 미표기. OAK-D(OV9282 stereo + IMX378 12MP) 기반이라 Bosch BMI270/BNO08x가 있을 수 있으나 미보장·미캘리브, 다수 OAK-D는 IMU 미탑재 출하. 자세 소스는 BlueROV2 Navigator의 IMU가 정식. (hardware-advisor가 아직 dispatch 불가였어 main이 직접 답함 → 이 빌드로 수정됨)
- 2026-06-22 [hardware domain · answered by main] Q: BlueROV2에 IMU 내장? → 예. Navigator 보드 ICM-20602 6축(gyro ±2000°/s, accel ±16g, ~1kHz) + MMC5983/AK09915 compass + BMP280; 외장 Bar30이 수중 depth. consumer-grade MEMS, 단독 dead-reckoning 불가(DVL/USBL 필요), 수중 magnetometer heading 신뢰 낮음.
- 2026-06-22 [hardware-advisor] Q: C3를 BlueROV2에 추가 시 2차 하드웨어 영향/BOM 주의점? → C3는 gigabit이지만 tether는 Fathom-X ~80Mbps(실측 15–50) 병목 → tether 가로질러 gigabit 불필요; 압축 stereo+color 전송 후 depth는 topside 재구성. onboard gigabit non-PoE 스위치(GigaBlox) 필요(BR 10/100 스위치 부족), penetrator/end-cap 여유 확인, 12–24V 별도 fused feed, 전방 장착 → 재-trim. 사전확정 필수: tether 길이·perception 시나리오·full-res 30FPS live 필요 여부(Fathom-X vs fiber 결정). [first live dispatch of the fixed hardware-advisor]  [UNVERIFIED: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- 2026-06-22 [simulation-advisor] Q: PPT용 시뮬레이터 콘텐츠 맵(기초→이론→구현) 정리 → 9-섹션 슬라이드 흐름 도출(big-picture 스택, FLU/NED, 2변종, Fossen hydro, T200/할당 rank5·6, 외란, PID→MPC→DOB-MPC, verify_*, 도구); 관통 주제 = FLU-everywhere & rank5-vs-rank6; docs/ 폴더와 1:1 매핑돼 재활용 가능
- 2026-06-29 [underwater-robotics-advisor] Q: 외란 모듈 Morison 힘 분해 — drag/inertia 이중계산 회피? → (1) hull D(nu_r)가 이미 Morison drag와 동치 → 0.5ρC_D A v_rel|v_rel| 추가 금지; (2) hydro의 M_A·d(nu_r)/dt가 added-mass C_a part 공급 → 누락분은 Froude-Krylov ρ∀a_wave(C_M=1)뿐, 슬라이드 평탄 C_M=1.5는 surge에만 맞음(축별 C_a=[0.49,1.12,1.29]); (3) FK는 world +a_wave 방향 COM 무토크; (4) h=4·Tp=12 → kh≈0.34 천해, deep-water k 3배 오차·해저 수직속도 미소멸 → ω²=g·k·tanh(kh)+cosh/sinh 필수. [memory: finite-depth-disturbance-env]
- 2026-06-29 [control-theory-advisor] Q: PID/MPC/DOB-MPC 외란실험 설계 + EAOB 상호작용 검토 → w_true=plant힘−still-water모델힘+주입힘으로 정의해 control tick 평균 후 w_hat과 비교(Morison/v_water 공식 재계산 금지); w_dot=0이라 DC/표류 거의완전·wave부분(~0.5–1.6rad/s 한계)·kick불가, horizon-상수 피드포워드라 불안정 아닌 잔류오차; metrics에 band-split RMS+추정오차‖w_true−w_hat‖+DRR(=RMSE_MPC/RMSE_DOBMPC, 완벽 ablation) 보강·multi-seed 필수; rejection은 DP, square는 tracking(nu_ref=0 lag 주의). [memory: finite-depth-disturbance-env]
- 2026-06-29 [simulation-advisor] Q: selfcheck 파랑 입자속도에서 u_x가 u_y·u_z보다 큰 이유 (우연인가)? → 우연 아님, 설계+물리. u_x≫u_y는 설정: beta_bar=0(평균 헤딩 +x) + s=30 좁은 cos^{2s} 분산(로브 반치폭 ~±10.5°)으로 에너지가 x에 집중(waves.py:105,112,123-124,165-166). u_z≪수평은 유한수심 궤도 납작화: Dv/Dh=tanh(k·zb), ROV는 해저 1m 위(zb=1), k≈0.085 → tanh(0.085)≈0.085 (waves.py:135-142). beta_bar=45°면 u_x≈u_y, ROV가 수면 근처면 u_z 약 4배↑(tanh(0.34)≈0.33).
- 2026-07-01 [simulation-advisor + slam-perception-advisor + verifier] Q: 실제 pool의 tag36h11 바닥을 MuJoCo sim에 재현 가능? → POOL_TAGS=1 opt-in wrapper(scene_bluerov[_heavy]_tags.xml)가 seabed + tag36h11 격자(실측47 + 격자fill, ~154) + 반투명 수면(4m)을 로드; gen_pool_apriltags.py가 config/tag_map.yaml→FLU(x유지·y반전·identity+yaw, seabed z=-0.5)로 변환하고 각 PNG를 pupil-apriltags로 왕복검증(cv2.aruco DICT_APRILTAG_36h11 id매핑 불확실→tag_id==N assert). 전부 contype=0 group=1 시각전용 → 3000스텝 롤아웃 Δ=0(교란 포함) 역학불변. 함정: MuJoCo box는 +Z면에 텍스처, geomgroup 3-5는 기본 숨김(→group1 사용). [memory: pool-apriltag-floor]  [UNVERIFIED×3: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]
- 2026-07-01 [simulation-advisor + verifier] Q: 해류+파도를 하나의 출렁이는 수면으로 MuJoCo에 시각화(역학 불변)? → POOL_TAGS 수면을 hfield로 바꾸고(gen_pool_apriltags.py) 신규 water_viz.py가 실제 disturbance 필드에서 eta(x,y,t)=Σa cos(k·x−ωt+φ)를 격자로 평가→hfield_data 기록, 해류로 advection해 하나의 수면. teleop=viewer.update_hfield(공식), run_viewer offscreen=mjr_uploadHField(renderer._mjr_context, private지만 3.9.0 존재·가드). legacy/modern 둘 다 .a_m 유무로 분기. 전부 contype=0 시각전용 → 매 스텝 애니메이트에도 롤아웃 Δ=0(두 변종·faithful/stylized). 실제 파장 6~76m≫풀이라 기본 물리충실은 잔잔, WATER_WAVE_LAMBDA로 출렁 강조. 함정: geomgroup 3-5 기본숨김→group1. [memory: water-surface-wave-viz]
- 2026-07-02 [workflow: simulation-advisor→control-theory-advisor→verifier×2+repair] Q: PID Kp/Ki/Kd를 해석적으로 유도 가능? → 가능. hover 선형화 m_eff·a=F−d·v + 3차 극배치: Kp=m(1+2ζα)ωn², Kd=m(2ζ+α)ωn−d, Ki=mαωn³; 설계점 ωn=2(병진)/3(yaw), ζ=0.9, α=0.2 → 수평등방 kp/kd/ki=131.6/90.6/38.7, heave 141.8/99.1/41.7, yaw 8.95/4.32/3.95 (+동반변경 4건: surge f_max 6→30, e_gate 0.5→0.15, slew 30→120, yaw rate FF). 검증서 정정: 현행 게인은 "파랑 증폭"이 아니라 ωn 0.59–0.88이 대역 내라 무억제(|S|≈0.82–1.01, 동작점 d_eff 기준); world-frame 이방성 게인은 yaw-불변 아님(square+heading_follow가 primary라 등방화 필수); 코드 주석 "heave Kd=22→ζ~0.65"는 added-mass만 쓴 오계산(실제 ζ≈0.60). [memory: pid-analytic-pole-placement] → run_compare.py는 gains 무전달 → controller.py:36-48 DEFAULT_GAINS 그대로(kp 6/12/20, kd 8/12/22, ki 1/1.5/1.2, yaw 5/3/0.5). 출처: 2026-06-14 bluerov2(rank-5) 기준 hand-tune "starting gains"; 문서화된 근거는 heave Kd=22→ζ≈0.65와 surge f_max=6N(rank-5 pitch 커플링 유래) 둘뿐, 나머지 수치는 sweep/문헌 근거 없음. heavy 재튜닝 이력 없음(6N surge cap 근거 소멸 상태로 유지). recording 폴더엔 gains/ROV_MODEL 미기록(갭). [memory: pid-gains-provenance] → 두 모델 구분: MODERN `disturbance/`(run_compare/run_viewer)는 wave=`beta_bar_deg`+`s`(cos^2s 퍼짐), current=`theta_c_deg`+`V_bar_c`(config.py:80-85, current.py:4) — 실험 config.yaml에서 본 이름. teleop(--observe 포함)은 LEGACY `disturbances.py`라 wave=`heading_deg`(+spread_s), current=Cartesian 벡터 `(0.2,0,0)`. --observe 기본 current는 0이 아니라 0.2 m/s(+x). 사용자 요청으로 --observe에 β·θ_c 랜덤 뽑기 구현(N/버튼 재추첨, --wave-deg/--current-deg/--current-speed/--dir-seed; disturbances.py 무수정, field 제자리 갱신). [memory: observe-free-drift-mode]  [UNVERIFIED×2: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- 2026-07-02 [workflow: control-theory-advisor + simulation-advisor] Q: 외란 모르는 nominal MPC가 왜 PID를 이기나 (toy: x*=goal+d offset 맞나?) → toy 정확 (offset=d/kappa, R↑일수록 악화; R=0 MPC ≡ kp=1 P제어기 + constraints), 실데이터에 실재: C모드 MPC dc_radial 2.75cm(하류방향 ±2°), DOB 0.16, PID는 적분 덕 1.53. 그러나 격차 본질은 외란 아님 — NONE서 이미 17.8 vs 2.1cm(8.6×) = PID drag-lag(e≈drag/kp≈17cm; 6.7s마다 목표힘 회전→적분 수렴불가) vs MPC 모델FF + 3s preview + 유효강성 ~90N/m. wave대역은 적분 무용·대역폭 싸움(CW band_wave 32.8/6.7/1.9). 각주: sat_freq=0 전구간, PID 500Hz vs MPC 20Hz, run은 heavy(PID Mx=My=0 → pitch 방치 ±12–42°). [memory: mpc-pid-gap-decomposition]  [UNVERIFIED: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]
- 2026-07-02 [workflow: umi-manipulation-advisor×2 + control-theory-advisor] Q: "선행논문 overshoot/drift는 로봇이 상태를 몰라서" motivation 주장 검증 → 부분만 참. overshoot 원인은 dynamics-unaware PID(UMI-U 스스로 귀속 Sec.V.a; 완전상태 sim서도 PID 17.8cm = 통제된 반례), 시스템은 overshoot 감지·대응까지 함(Sec.III-B.h). drift는 두 논문에 단어조차 없음(정지수 풀 + 외부카메라 = 회피된 문제) → "선행논문의 drift"로 쓰면 오인용, gap 논증으로 전환. 참인 절반: 온보드 속도추정 전무(양쪽), AquaBot "cannot precisely detect the position and orientation" = 최강 인용, 추정은 MPC의 enabler(원인 아님). AquaBot을 overshoot 근거로 인용 금지(정책이 방지했다고 긍정 보고). [memory: precursor-papers-overshoot-drift]
- 2026-07-02 [control-theory-advisor] Q: DOB-MPC는 현재 외란만 아나 예측도 하나 + 왜 MPC보다 좋나 → 예측 안 함: w_hat을 60 스테이지 ZOH(mpc_acados.py:171-173; per-stage p 인프라는 존재). 성능 이유 = feedback-only(S(jw) 한계, DC offset)→estimate-and-cancel(한계가 관측기 지연 T_d로 이동, 사인 잔차≈w·T_d: omega_p서 3–5%) + w_dot=0=IMP 적분기(offset-free MPC 비선형판) + lumped w의 모델 mismatch 흡수(sim2real). 파도 예측 확장: harmonic-EAOB(DP wave-filtering 전통)/stage-wise w_traj+AR(Fusco&Ringwood 1–2주기 가능)이나 이득 상한 −17%(비파도 성분 2.83cm) — oracle-preview ablation(반나절)으로 ceiling 먼저 실측 권고. [memory: dobmpc-disturbance-preview]

- 2026-07-07 [workflow: diagnose-dobmpc-corner-deviation (4 investigators + adjudicator)] Q: 왜 DOB-MPC가 CW/CDW에서 (1,1) 우상단 코너에서만 크게 이탈하나 → 세 요인의 곱: (M1) sharp-square 기준 + 60°/s yaw 슬루의 코너 기하 과도현상(무외란서도 전코너 ~5cm, run_compare square_setpoint/slew_heading) × (M2, dominant selector) 이차 hull drag가 상대속도 nu_r에 작용→wave 힘 이득∝상대속도, 상류 edge 0.35m/s가 하류 0.05의 ~7배(hydro.py:150,159; env water_velocity에 wave 궤도속도) × (M4) DOBMPC가 yaw-rate FF(r_ref) 폐기+호라이즌 내내 yaw 고정→슬루 지연 1.5→3.2s·199° 오버슈트(dobmpc_controller.py:91-92,137,143) × (M3/M3b) EAOB w_dot=0 body-frame ZOH가 wave대역 부분기각+빠른 yaw중 외란 오회전(mpc_acados.py:171-175). C모드 평탄=DC 완전기각. pitch −15° 스파이크는 별개 액추에이터(수직추력)의 co-symptom(원인아님). 최악점은 코너서 0.36m 지난 슬루 중간. 옆흐름 두 코너 비대칭(11.9 vs 7.6)은 wave-phase beating로 설명(얇은 근거). [memory: dobmpc-corner-deviation]  [UNVERIFIED×2: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]
- 2026-07-21 [control-theory-advisor] Q: tracking MPC는 보통 미래 참조(trajectory)를 받는가 → 표준 정식화(RMD/Borrelli)와 주요 스택(rpg_mpc, acados 관행, MPCC, OCS2, 해양 NMPC) 전부 stage별 시변 yref 또는 path+진행변수; 현 ZOH-직진외삽은 DP/teleop/cascade엔 적합한 흔한 shortcut이나 알려진 경로 벤치마크에선 anticipation 상실; MPC에 미래 참조 주는 건 학계에서 방법론 일부(불공정 아님); solver는 이미 stage별 yref 지원(mpc_acados.py:172) → _xref_ned+인터페이스만 수정하면 됨 [memory: compare-20260720-sweep-findings]
- 2026-07-21 [workflow: control-theory + Explore + verifier (5 agents, 2 CONFIRMED)] Q: heavy 기준 6-DOF 독립제어 맞나 + rank-5 surge 정형(slew/pitch-guard/clamp) 제거 + roll/pitch PD→PID → (Q1) heavy 계열은 rank-6 fully-actuated, docstring "NEVER pitch"는 bluerov2 한정으로 낡음; 레벨링은 rp_kp로 이미 지원(gripper만 ON). (Q2) pitch-guard=rank-5 전용 제거(heavy 얼로케이션 pinv가 Fx⊥My), 단 bluerov2엔 load-bearing(없으면 test_controller tumble>45°)이라 `pitch_guard_deg` 게인 키로 게이트해 heavy만 OFF; slew는 3축 균일화(유지); clamp는 일반 포화(유지, 이미 등방). (Q3) PID 채택 — PD는 일정 토크에 type-0(φ_ss=τ/(kp+B), 0.4N·m서 3.5° 실측)→PID type-1(0.0° 실측); ki=I_eff·α·ωn³(roll2.60/pitch4.64), gated+clamped anti-windup, 3차극 전부 LHP ζ≈0.83-0.89. controller.py만 수정, 회귀 5개 테스트 PASS. bluerov2 리포 전면 삭제는 ~20파일(자산/테스트 삭제 포함)이라 확인 후 별도 진행. [memory: controller-heavy-rank6-pid-leveling]  [UNVERIFIED×2: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]
- 2026-07-21 [실행: bluerov2 변종 제거 + pitch-guard 정정] 사용자 승인(테스트는 heavy 이관, dobmpc는 보류)으로 실행: rov_model/_MODELS·_POOL_WRAP, controller GAINS_BLUEROV2·DEFAULT_GAINS 폴백, 3개 루트 스모크 테스트 heavy 이관, verify_meta 허용목록, gen_pool 래퍼, config vol, README/KNOWN_ISSUES 정리. **핵심 반전**: pitch guard를 heavy에서 지웠더니 test_controller에서 pitch가 71°까지 스윙 → 정적 얼로케이션(Fx⊥My)은 맞지만 **동적 Coriolis 커플링**이 있어 guard가 heavy에도 유용 → 되살림(gain 키 게이트, dormant). test_controller pitch 임계 heavy 재보정(뒤집힘<90°+복귀<8°), PD-vs-PID 헤드룸 축소. bluerov.xml/BlueROV.yaml은 8개 hydro/thruster 검증 스크립트+heavy 관성 provenance의 fixture라 **삭제 안 함**(삭제 시 8개 깨짐). dobmpc NU=4/test_dobmpc rank-5는 보류(KNOWN_ISSUES). 회귀 9개 PASS. [memory: controller-heavy-rank6-pid-leveling]  [UNVERIFIED: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]
- 2026-07-22 [workflow: control-theory-advisor ×2 + simulation-advisor + verifier (4 agents, 11/12 verified)] Q: mpc_acados w_hat ±50 클립 적정성 + yref u=0 적절성 → 클립: authority 초과 시 u*는 클립 수준과 무관(active set 동일)이라 "큰 외란이 지워진다" 걱정은 무근거, 실기능은 RTI 수치보호; 힘 50 N은 물리 worst(X≈35/Y≈37/Z≈3 N) 위라 우연히 무해하나 스웨이 보수스택 85–100 N(=blowup 레짐)에선 물리고, 토크 50 N·m은 authority 8–10의 5–6배로 부적절 → per-axis [15,45,45,5,5,8]+발동 카운터+EAOB innovation gating 권장. u_ref=0: turnpike 논증으로 내부 offset 소멸, 잔여는 QN=Q 종단경계층 0.3–0.5 mm뿐(관측 DOB 잔여 1.7 mm는 EAOB lag 지배; nominal 2.73 cm = 2.11 N/k_p 77.5 N/m 정확 일치) → trim u_ref=g−w_hat_lp는 공짜 개선이지만 calm 코너는 제약-제한(surge 8 N derate vs PID 30 N)이라 참조 블렌딩+derate ablation이 진짜 노브. 부수 발견: mpc_acados.py:94 SyntaxError(주석 중 trailing dot), 코드 미수정. [memory: mpc-whatclip-uref-review]  [UNVERIFIED×4: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-23 [control-theory-advisor] Q: EAOB를 순수 disturbance 플러그인으로(MPC x0는 state estimator 출력, EAOB eta/nu_hat 폐기) — 타당한가? → 타당: DOBC 정통 구조(vs 현행 offset-free MPC 계열)이고 diffusion-policy 통합(플래너·트래커 동일 상태 소스)과 mpc/dobmpc 단일변수 ablation에 유리; w_hat 관측가능성은 소비자와 무관(어제 A/B로 실증). 유일 쟁점 = 시뮬의 "추정기 출력": raw 5cm 백색을 x0에 넣으면 chatter +66%↑ 예상 → 권고 순서: mpc_state_source="meas" A/B로 비용 실측 → 필요시 SLAM-proxy KF(EAOB·MPC 양쪽 공급, 실기 tagslam σ 근거로). x0단 LPF는 금지(순수 위상지연). 사용자 결정 대기.

- 2026-07-24 (4) [직접 측정] Q: trajectory_compare에서 DOB-MPC가 코너에서만 참조와 크게 벌어지는 이유 — 그것도 8 N surge 박스 탓인가? → **아니오**. NONE(외란 0) 박스 A/B에서 코너 오차가 2.00/1.92/2.04 cm로 8 N·30 N **소수점까지 동일**(코너 surge 명령 평균 0.9–1.4 N, 박스 발동 0.7%) → authority 아님. 진짜 원인은 **참조가 코너에서 실현 불가**: 위치 경로가 각진 사각형이라 참조 속도가 한 샘플(0.05 s)에 90° 반전(요구 가속도 무한)하고, yaw는 60°/s 슬루라 위치 참조와 모순(필요 선회율 126–474°/s ≫ 60). MPC는 preview로 미리 돌아 안쪽을 자르는 최적 절충(corner-cutting). 파랑 하에선 박스도 기여하나 코너·직선을 거의 같은 비율(33%/37%)로 줄여 코너 특유 효과 아님; 박스는 코너 **직후 회복**을 개선(위상 21.33: 8.78→3.52 cm). DOB-MPC에서 도드라지는 건 직선이 0.6–1.3 cm로 완벽해 상대적으로 튀는 것(절대값은 3사 중 최소). 해법은 참조 필렛/감속/슬루상향 = 벤치마크 재정의. [KNOWN_ISSUES 한계 등재]  [UNVERIFIED×3: 산출물 없음/예시/유도값 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-24 (3) [구현+검증] Q: MPC도 PID와 같은 조건(surge 30 N)으로 맞춰달라 → heavy `U_MAX[0]` 8→30 적용(rank-5 분기는 8 유지, 커플링 실재), acados 재빌드 후 컴파일된 솔버 bound 확인. 재빌드 검증(storm/gentle CW, 10lap×3헤딩): storm PID 26.14 / MPC 28.82→**17.86** / DOB 17.36→**7.30**, gentle 11.56 / 9.51→**7.63** / 3.14→**1.87** → **MPC 양단 모두 승, crossover 소멸**, DOB가 최대 수혜(storm 2.4×). sat_freq=0 유지·피크 스러스터 40.3 N(<64.1). 회귀: verify_acados PASS(Δu 0.0615<0.25), verify_meta PASS(`controller.u_max` 기록), test_controller/test_square_mission PASS. verify_eaob는 **기존 FAIL이 오히려 개선**(A/B: NIS 245.8→100.0, NEES 398.9→122.0; 게이트 24 초과는 2026-07-23 등재 τ_dist 이슈로 잔존 → 재스윕은 30 N 기준). run meta `controller.u_max`가 경계(키 없으면 8 N), full sweep 재실행은 사용자 몫. [memory: mpc-surge-box-benchmark-artifact]  [UNVERIFIED×4: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-24 (2) [workflow: 4 data/code analysts + control-theory synthesis + 4 adversarial verifiers(4/4 REFUTED=정밀도·귀속 기각, 정성핵심 존치) + 직접 ablation] Q: 대칭-state sweep(compare_20260724_160210)에서 강파랑=PID 승, 약파랑=MPC 승의 원인 → **주원인은 MPC 자신의 surge 박스 U_MAX[0]=8 N(PID f_max=30의 1/3.75)**: storm에서 틱의 60%(mpc)·75%(dobmpc) 물림 vs gentle 7%. 런타임 박스 해제 ablation(10lap×4헤딩 페어드): storm PID−MPC −4.24→**+8.60 cm 부호역전**(MPC 42%↑), gentle +4.01→+4.87(10%↑) → **crossover 소멸**. sat_freq=0이라 기존 지표가 못 잡음(소프트웨어 한계, box30에서도 스러스터 36.1 N/무포화). 주파수 분해(전 1500런 PSD): MPC는 <0.7 rad/s에서 **모든** 해상상태 3–8배 우세(모델+preview), 지는 구간은 ω_p 걸친 창뿐이고 그 폭이 사다리 따라 축소→gentle에서 소멸. 검증팀 기각: "주파수가 진폭을 지배"(6 rung뿐이라 a 유의하지 않음 p=0.218, gamma 대입 시 역전) 및 고정 compliance 비 ω_x=0.89±0.03(2 시뮬 seed 공유, gentle엔 교차 없음) — 사다리가 Hs·Tp 교란이라 분리 불가, ablation이 대신 결판. [memory: mpc-surge-box-benchmark-artifact] [KNOWN_ISSUES 등재]  [UNVERIFIED×6: 산출물 없음 — docs/MEASUREMENT_AUDIT.md]

- 2026-07-24 [workflow: 3 data/code analysts + control-theory synthesis + 8 verifiers(4 CONFIRMED, 4 세션한도 사망→핵심 수치 직접 재확인)] Q: 20260724 wave sweep에서 MPC가 PID에 지는 이유(전반·wave 강도별 역전·NONE/C/CD state-노이즈 가설) → NONE은 MPC 승(1.09 vs 1.51, ref preview가 코너 과도 제거); C/CD의 MPC 3.0 cm은 90%가 무적분 dc offset(2.70 cm = 전류항력 2.11 N / 암묵강성 77 N/m, 전류방향 정렬 −0.2±1.8°), state 노이즈 기여 ~0%(band_high 모드불변, NONE 대조군, dobmpc 반사실 97% 회복); wave crossover는 moderate(Hs1.0/Tp8)↔rough(Hs1.2/Tp6) 사이 — PID ωn 2.3–2.8 rad/s가 유한수심 감쇠로 캡핑된 강제대역 0.5–1.3 rad/s를 항상 상회(평탄강성 120–128 N/m)라 완만 저하(7.11 cm/m-Hs), MPC는 비모델 강제력 초선형 성장+U_MAX_x 8 N 박스+storm acados n_fail 77/100으로 가파른 저하(12.33 cm/m-Hs); sat_freq=0 전 구간(스러스터 물리포화 아님, 설계 박스). PID truth-state vs MPC meas-state 비대칭은 실재하나 격차 원인 아님. [memory: compare-20260724-wave-sweep-findings]  [UNVERIFIED×2: 예시/유도값 — docs/MEASUREMENT_AUDIT.md]

- 2026-08-04 [직접 측정, 신규 `umi_handheld/bench_stereo.py`] Q: `umi_handheld/record.py`가 `test_oak_depth.py`보다 성능이 나빠 보이는 이유 → **두 개의 서로 다른 이야기가 섞여 있었고, 프레임률은 원인이 아니었다.** (1) 기본 프로파일은 test_oak와 **동률 30 fps**(record 30.00 / test_oak normal 30.83, 800p extended는 20.01) — 차이는 `decimation factor 2`가 만드는 depth **320×200 vs 1280×800(샘플 16배)**와, threshold 100–3000 mm + 무효픽셀 순검정 렌더 때문. PNG 4장 저장·4스트림·컬러 페어링은 **전부 0 fps 비용**(30.00→30.00 실측)이라 "녹화하느라 느리다"는 틀렸다. (2) maxres의 8.43 fps는 "고해상도 정직한 depth의 대가"가 아니라 **speckle 필터 단 하나의 대가**: −speckle 30.00, −colour 8.43, −temporal 8.43, shaves 8/slices 6 → 9.00, speckle_range 28→5 → 8.43. (3) **median 7×7이 speckle을 공짜로 대체**(같은 장면 1런): speckle 61.5%/tstd 39.2/rough 20.0/tail 7.5%/8.43fps vs median7x7 64.6%/24.7/5.0/2.2%/29.00fps — 모든 품질지표 우위에 3.4배 속도, 트레이드오프 없음. 후처리 전무(=test_oak)는 tstd 61.4/tail 10.2%로 **가장 불안정**했다. median이 꺼져 있던 건 `extends:`로 상속된 `median_filter:"off"`인데, 그 근거(extended×subpixel=1520 > median 한계 1024)는 **기본 프로파일에만** 해당하고 maxres는 extended off라 95×8=760으로 합법 — 이유가 상속되지 않은 사례. **적용**: `configs/pipeline_maxres.yaml`에 median 7×7 + speckle off. 실기 검증 take `sessions/demonstration_0015` = 12s / 341프레임 / **29.06 fps** / 1280×800 / valid 0.628 / colour skew 5.6 ms / 650 MB(→60s당 3.2 GB). 검증 중 신규 발견: 29 fps에서 **컬러 스트림이 프레임 1.8% 드롭**(0015 gaps 6 vs colour off `demonstration_0018` gaps **0** @30.00 fps; writer 6→12스레드는 오히려 8 gaps라 디스크 아님) → 미수정, KNOWN_ISSUES 등재. 부수 확인: record.py는 depthai **2.x 전용**이라 base conda(3.5.0)에선 `dai.node.XLinkOut` AttributeError로 즉사, `~/.venvs/c3-depthai`(2.32)에서만 동작. [memory: stereo-maxres-median-fix] [KNOWN_ISSUES 등재 ×3]

- 2026-08-04 (2) [구현 + 실기 검증] Q: test_oak_depth.py처럼 `E` 키로 extended 전환 + extended에서 최고 사양·fps → **구현 완료**. extended는 런타임 config 필드가 없는 **빌드타임 설정**이라 디바이스를 닫았다 다시 여는 수밖에 없다(test_oak_depth.py도 동일). 설계: 델타 계산이 아니라 **config 파일 2개를 E로 순환** — `--config-extended`(기본값은 `<config>_extended.yaml` 형제 파일 자동탐지, `none`으로 비활성) → 각 모드가 독립적으로 `--dry-run`·`bench_stereo` 대상이 된다. `record_device`를 outer 루프 + `_device_session`(→"quit"|"toggle")로 분리, `kind_i`/`written`은 재시작을 건너 보존, 녹화 중 `E`는 거부(두 모드는 depth 기하가 달라 session.json 하나로 못 적는다 — `K`와 같은 규칙). **extended 최적 탐색**(`bench_stereo.py --sweep extended`, 800p, 같은 장면 1런): subpixel과 median은 **양립 불가**(190×8=1520 > median 한계 1024, 장치가 매 프레임 error 뱉고 median을 끔) → subpixel **끄는 쪽이 fps·품질 모두 승**(19.43 fps/tstd 27.9/tail 3.5% vs subpixel-on 16.00/34.6/9.0%), temporal은 normal 모드와 달리 **필수**(끄면 tstd 28.2→55.5), shaves 8/slices 6이 소폭(19.29→19.43). 채택: ext + subpixel off + median 7x7 + shaves 8/6. **단, tstd가 못 보는 대가 발견**: subpixel off는 disparity가 정수라 **1 m에서 depth 계단 1.0 → 21.0 mm**(bench에 `lvls`/`q@1m` 지표 추가해 실측; 847 vs 1333 levels). extended의 존재 이유가 0.22–0.5 m 근접대라 그 구간 계단은 ~2 mm[유도]로 무해하지만 z_far 2 m에선 부적합 → 그때는 far 프로파일. `rough 0.0`은 매끄러움이 아니라 양자화 아티팩트라 subpixel on/off 간 비교 불가. 실기 검증 `sessions/demonstration_0019`(12 s, 228프레임, **19.68 fps**, valid 0.904, **프레임 공백 0회** — 29 fps 컬러 드롭이 프레임률 문턱임을 확인). **MinZ 세 번째 실측 확증**: threshold floor를 150 mm(유도값 224.5 아래)로 두고 최근접 반환이 **224 mm** → `min_z_provenance` 문자열에 추가(기존 112·449와 함께 두 disparity 범위 전부). dry-run에 median 1024-레벨 가드 추가(두 모드 다 검사). 회귀: synthetic 경로 72프레임 OK, far 모드 재녹화 OK, 토글 루프 단위검증(모드 순환·kind 보존·written 누적) OK. [memory: stereo-maxres-median-fix]

- 2026-08-04 (3) [지연 진단 + 수정, 실기] Q: 프리뷰에 전반적으로 latency가 있다 → **범인은 `stereo.left/right` 입력 큐의 depthai 기본값 8**. non-blocking이라 넘칠 때 오래된 걸 버리지만 노드는 **앞에서 꺼내므로**, 연산 한계에 걸리면 큐가 영구히 꽉 차서 항상 8프레임 늦은 입력을 처리한다. `setQueueSize(1)`로 **maxres 252→153 ms, extended 382→229 ms, pipeline.yaml 277→166 ms**, 게다가 **드롭도 감소**(시퀀스번호 정밀계수 4.33%→3.33%). q=2부터 바로 242 ms라 gradient가 아니라 **절벽** → 기본 1 확정, config 키 `record.stereo_input_queue`. 주의: wall-clock 공백으로 보면 6→14회로 **악화한 것처럼 보인다**(2프레임 공백이 1프레임 공백 여러 개로 바뀜) — 시퀀스번호로 세야 실체가 보인다. **배제한 가설 전부 실측**: 호스트 루프 아님(디스플레이 없을 때 640 kHz, `draw_overlay` 3.0 ms), XLink 대역 아님(`streams`에서 right·color 빼도 153→159 ms 무변), `setXLinkChunkSize(0)` 무효, 호스트 큐 크기 무효, 후처리 필터 거의 무효(median+temporal 다 꺼도 152.8). **남은 152 ms는 해상도 자체**(같은 체인 640×400은 36.1 ms) — 디바이스 내부 파이프라인 깊이라 설정으로 못 줄임, KNOWN_ISSUES 등재. 부수 수확 2건: (1) **측정 중 벤치가 네트워크의 C3(PoE 192.168.2.191)를 잡은 걸 발견** — `dai.Device()`가 장치를 안 고르면 아무거나 잡는다(그 행은 2.25 fps/valid 8.8%로 결과처럼 보였다). record.py·bench_stereo 둘 다 **카메라 모델의 `device_mxid`로 핀 고정**(`CameraModel.device_mxid` 신설, 불일치 시 거부, `--any-device`로 해제) — 안 고쳤으면 C3 프레임을 OAK-D-W 캘리브로 기록할 수 있었다. (2) 죽은 키였던 `record.streams:`를 **실제로 동작하게** 구현(left+depth 필수, right/color 선택) → KNOWN_ISSUES에서 삭제; 컬러 드롭의 임시대응이 이걸로 바뀜(컬러 ON 3.67% vs OFF 0.33%, downstream은 left+depth만 읽으므로 안전). 프리뷰 합성뷰가 2560 px라 WM이 매 프레임 리스케일하던 것도 `--preview-width`(기본 1600)로 1회 INTER_AREA 축소 — 디스플레이가 없어 지연 효과는 **미실측**. 회귀: dry-run 양 모드·synthetic 72프레임·실기 far/extended take·`streams` 반영 확인 OK. [memory: stereo-maxres-median-fix] [KNOWN_ISSUES +1 −1]

- 2026-08-05 [구현 + 실측 진단] Q: c3_collect의 플래그 벽을 YAML로 정리하고, `~/.venvs/c3-depthai/bin/python` 대신 `python -m`으로 쓰고 싶다 (+ "그냥 robust에 depthai 깔면 안 되나?") → **셋 다 처리했고, 도중에 리포 전체에 걸린 환경 버그를 하나 찾았다.** (1) **환경**: `python -m c3_camera.c3_collect`는 원래부터 동작했고 문제는 `python`이 conda base(depthai 3.5.0)라는 것뿐이었다. robust에 `depthai 2.32.0.0 + pymavlink + PyYAML` 설치 — cp314 wheel이 실제로 존재하고 numpy/opencv/gtsam은 한 줄도 안 건드림(`pip --dry-run`으로 먼저 확인). conda 미리보기 창이 죽던 것도 원인 특정: **`cv2/config-3.py:14-18`이 import 시점에 `QT_QPA_PLATFORM_PLUGIN_PATH`를 무조건 `<cv2>/qt/plugins`로 덮어쓰는데** robust에선 그 디렉터리가 PyQt5/PySide6 공존용으로 `plugins.disabled`로 rename돼 있음 → `viz.py`에 조건부 8줄(`_qt_plugin_path`)로 복구, venv에선 무동작(실측: robust `IMSHOW OK`, venv 불변). `umi_handheld/record.py`에도 같은 8줄. (2) **☠ 발견**: robust는 python 3.14.4 + **소스빌드** numpy 1.26.4(`Tag: cp314-cp314`, meson)인데, 3.14의 `LOAD_FAST_BORROW`가 지역변수의 refcount를 올리지 않아 numpy의 임시배열 재사용이 **살아 있는 지역변수를 덮어쓴다** — `def f(): a=np.full(100_000,3.0); t=a*a` 후 `a[0]==9.0`. `a*b`/`a+b`/`a*2.0` 전부, 단 ~256 kB 이상 + 함수 지역변수일 때만(그래서 눈에 안 띈다). 증상은 `test_encode_quality`가 robust에서만 4/36 실패(SSIM 1.0 대신 7.05e8). **리포의 sim/MPC/SLAM이 전부 robust에서 도므로 영향 범위가 c3_camera 밖**이라 손대지 않고 KNOWN_ISSUES 등재 + `./c3`를 venv 1순위로 되돌림 + `./c3 env`에 `numpy elide` 프로브 추가. (3) **프로파일**: `c3_camera/profile.py`(+`profiles/base.yaml`, `research_near.yaml`) — `extends:` 체인·사이클 가드·파일별 사전검증·YAML 불리언 트랩(`median: off`) 거부·오타/잘못된 섹션 제안. 우선순위 `StreamConfig < mode < profile < CLI`이고 "명시적으로 쳤는가"는 **SUPPRESS 클론 파서**로 판정(문자열 sentinel은 argparse가 `type=`을 돌려서 exit 2, `parse_args([])` 비교는 `--imu-rate 200`처럼 기본값과 같은 값을 놓쳐서 프로파일이 CLI를 이김 — 둘 다 실측 후 기각). `--dry-run`이 설정마다 default/mode/profile/cli 출처를 찍는다. CLI가 노출 안 하던 15개 StreamConfig 필드도 파일로 설정 가능. (4) **MinZ 정정**: 녹화 depth PNG 전수(47,270 프레임/3.6e9 px) 바닥이 정확히 300 mm(ext 150 mm)이고 그 아래 0픽셀 → 문서·코드의 유도값 298.9/149.4를 교체하고 `TESTING.md:484`의 "CAM_B냐 CAM_C냐" 미해결을 **CAM_C**로 종결(fx_rect ∈ [380.0,381.3) 브래킷, 300:150의 2:1, host_depth 실측 ~380.00과 0.04% 일치). 회귀: 229/229(venv), robust는 위 numpy 건으로 encode_quality 4건만 실패. [KNOWN_ISSUES +1, 1건 부분해소] [memory: c3-profiles-and-env]

- 2026-08-06 [환경 마이그레이션 + 사전 실증] Q: robust를 python 3.12로 내리면 문제가 생기는지 확인하고, 괜찮으면 바꿔달라 → **문제 없었고, 교체 완료(3.14.4 → 3.12.13).** 먼저 `robust`를 건드리지 않고 `robust312`를 따로 세워 전수 대조했다. (1) **가용성**: pip 46개 중 44개가 고정 버전 그대로 cp312 휠 존재(`pip download --python-version 3.12 --only-binary=:all:`), `acados-template`은 순수 python editable(`py3-none-any`, `.so` 0개)이라 무관, `pyzed 5.2`는 Stereolabs가 cp312 휠 배포(URL 1.4 MB 바이너리 응답 vs 존재하지 않는 cp399는 245 B XML — 대조군 확인). **핵심은 numpy**: 3.14에선 cp314 휠이 없어 `Generator: meson` 소스빌드였는데 3.12에선 `cp312-cp312-manylinux_2_17` 정식 휠이다. (2) **대조 결과**: 19개 핵심 import 전부 OK, gtsam LM 최적화·apriltag·tagslam_core OK, `marinegym/tests` 13개는 양쪽 12 PASS/1 FAIL(`test_dobmpc`의 casadi `4x1 vs 6x1`, 기존 문제), `verify_acados/hydro/hydro_precise/meta` 양쪽 PASS, `verify_eaob`·`verify_state_source` 양쪽 FAIL — 특히 `verify_state_source`는 **출력 60줄의 radRMS/NIS/NEES가 완전히 동일**(벽시계만 다름). c3_camera는 3.14에서 225/229 → 3.12에서 **229/229**. numpy elision 프로브 9.0 → 3.0, `depth_to_xyz` 포인트클라우드 z 최대 109 km → 4.27 m. (3) **교체**: `conda rename robust → robust314_bad`(보존) + `robust312 → robust`, 셔뱅 재작성 확인. 교체 후 재검증 전부 통과. `./c3` 인터프리터 우선순위를 robust 1순위로 되돌려 **venv 없이 한 환경으로 통합**(venv는 폴백 유지). 빠진 conda 패키지 7개(h2/hpack/hyperframe/pysocks/brotli-python/backports.zstd/libmpdec)는 requests의 선택적 전이 의존성이고 리포 미사용 — requests/urllib3/decimal/zstandard 동작 동일 확인. (4) **함정 하나**: `get_python_api.py`가 `pip install --ignore-installed`로 돌아 numpy를 2.5.1로, cython을 3.2.9로 올린다 → 뒤에 `pip install numpy==1.26.4 cython==3.2.4`로 되돌려야 하고, 그 뒤 뜨는 `pyzed requires numpy>=2.0` 경고는 기존에 기록된 그 무해한 경고다. **남은 것**: 3.14 기간에 robust에서 나온 수치 결과가 미감사 — KNOWN_ISSUES 항목을 "환경 버그"에서 "과거 결과 미감사"로 교체했고, 옛 환경이 `robust314_bad`로 남아 그때 값을 재현·대조할 수 있다. [KNOWN_ISSUES 1건 교체] [memory: environment-numpy-constraint, c3-profiles-and-env]
- 2026-08-06 [umi-manipulation-advisor + 직접 검증] Q: demonstration take에서 뭘 찍어야 하나 → **"무엇을 잡느냐"보다 "어디서 끊느냐"가 지배적.** affordance label은 그리퍼 신호에서 자동 유도되고 goal 이미지는 에피소드 **마지막 50프레임**에서 균등 추출(`train_affordance_depth_unet.yaml:155 goal_end_window_frames: 50`, 실측 확인) → tail 50프레임 전부가 완료 상태여야 하고 끝나고 녹화를 흘리면 rig 내려놓는 장면이 goal이 된다. 에피소드 구조 = 완전개방 프레이밍(0.5–1s) → 접근(3–5s) → **1초 안에 결단력 있게 닫기**(`closing_velocity: -0.003`/frame) → **1초 정지**(`velocity_threshold: 0.005`, `window_size: 15`; 안 멈추면 `closed` 구간이 없어 그 에피소드는 학습 기여 0) → tail(2–3s). 접근 중 반쯤 닫힌 채 멈추면 `closed_openness_threshold: 0.85`가 관대해 오검출. 대상은 frame 0부터 화면 안(supervision이 frame 0부터). 1 take = 1 에피소드 = 1 파지, 리셋은 녹화 금지, 7–9초. 고정: 손 자세(라벨 seed 박스가 이미지 좌표 고정), 마운트, `axis: y`. 변주: 물체 종류 > 자세 > 방해물(≈50%) > 지면 형상. 작업거리 0.25–0.60 m(정규화 dynamic range의 52%). **부수 검증: depth 스케일 정상** — 조언의 "C3 수중캘리브 → 공기중 1.33배 과대" 경고는 녹화 카메라(OAK-D-W, air 캘리브)엔 해당 없음을 기존 데이터로 확인: solvePnP Z(태그 크기 기반) 325.6 mm vs stereo depth(시차 기반) 324.0 mm, **불일치 0.9%**, n=288, sessions/grippercalibration_0004. 줄자 검증 불필요.
- 2026-08-09 [none] Q: `--pose`로 클릭해도 추적만 되고 pose가 안 나온다 → 원인은 3단계(현장 재구성) 미구현: 2단계까지는 `--pose-mesh`가 있어야 자세가 나왔다. 수집→BundleSDF→FoundationPose를 어댑터에 구현(취소 가능한 `Popen`, `_PinholeUnprojector`가 `RectifiedCamera` 대체, 75°/최소6장 가드, `MESH` 로그 레코드). 저장 참조뷰 재생으로 전 구간 검증(8장/75.3°/RMSE 6.2mm → 25 s → 7993 verts). [memory: rovgui-pose-env]
- 2026-08-10 [none + P2 workflow review] Q: 수집이 "collection finished"에서 절대 안 넘어간다 → 원인 2개: (1) PoseWorker의 `_log` 메서드가 SensorLog 속성에 섀도잉되어 on_log가 None/비호출가능 객체(녹화 중 TypeError 매 tick) — `_say`로 개명 + 패키지 전체 AST 섀도잉 금지 테스트, (2) finalize가 state==TRACKING에 갇힘(궤도 끝에서 SAM2가 놓치면 영구 고착) — `_finish_capture` 분리, 상태 무관 완료. 같은 날 3-D pose 맵 창(pose3d.py)+`_pose.csv`+depth 커서 프로브+컬러바 눈금 추가. [memory: rovgui-pose-env]
- 2026-08-18 [none — CSV 직접 분석] Q: pid로 사각형 도는데 오른쪽 위에서 계속 가만히 있다 → **꼭짓점 10 cm 앞 영구 교착**(제어 발산 아님). 세 개가 겹쳤다: (1) `PathCursor.step`의 leash `cmd = min(cmd, theta + lead_m)` — 선체가 서면 참조도 서므로 위치오차가 `path_lead_m` 0.10 m에서 상한(관여 tick의 83.6 %가 leash 포화, `mpc_143938.csv`), (2) `path_fillet_m: 0.0`이라 `speed_profile`이 꼭짓점에서 v_ref를 creep 0.02로 제동하고 setpoint가 꼭짓점을 2 cm 넘으면 접선이 90° 꺾여 **surge 속도 FF `kd·v_ref_b`가 0으로 소멸**(kd_x 59.7 × 0.10 = 5.97 N 증발), (3) 남은 상한 `kp·0.10 + i_max` = 9.33 N [유도] vs 실측 uX 9.47 N → ax_surge 0.158 / `pwm_dev_us` 26 µs로 T200 널존 ±25 µs[스펙]·전루프 실측 문턱 `0.692(|axis|−0.096)` 바로 위 → 실제 속도 0.000 m/s. 직선 구간에서는 11.3–11.6 N으로 0.06 m/s를 냈다. 두 런 연속 **같은 꼭짓점**(tag 37 경로 s≡2.0 m, origin 대각 반대편)에서 40 s/45 s 정지, solver_status 0·태그 21장·reproj 1.85 px로 계기는 전부 정상 — 조용한 실패. 배경: 지령 0.100 m/s에 실제 0.051 m/s라 leash가 사실상의 제어법이었다. 대응: `path_fillet_m: 0.15` 복귀 + 속도 0.05–0.06, 근본은 `i_max_n` 상향과 교착 워치독. 산출물: `sessions/low_level_controller_data/20260818/0818_143802/{mpc_143802,mpc_143938}.csv`. [KNOWN_ISSUES 1건 추가]
- 2026-08-18 [none — 구현] Q: MPC 비용을 종/횡 오차로 분리한 `mpc_tuned` 모드를 만들고 테스트해달라 → **구현·검증 완료(실기 미검증)**. 핵심 설계: 오차를 회전시키는 대신 **가중치 행렬을 회전**(`W = Rz(ψ_path) diag(q_along,q_cross) Rz(ψ_path)ᵀ`) — 직교변환이라 수학적으로 동일하고, acados가 이미 생성된 LINEAR_LS OCP에 `cost_set(k,"W",W_k)`를 런타임으로 받으므로 **`mpc`와 `mpc_tuned`가 같은 컴파일 솔버**(두 번째 codegen·빌드 없음)이고 `along_scale==cross_scale`이면 baseline과 기계정밀도까지 동일(A/B parity). 회전각은 기체 헤딩이 아니라 **경로 접선**이라 `NedPlan.psi_path`를 새로 실어 보냄(crab 미션에서 둘은 무관, 참조 속도는 코너에서 creep이라 대용 불가). 새 파일 `rov_gui/control/path_cost.py`·`tools/sweep_path_cost.py`·`tests/test_path_cost.py`(15/15), 모드 4개로 확장(`mpc|dobmpc|mpc_tuned|dobmpc_tuned`, worker/UI/CLI/config 배선), 경로 plan 없으면 자동 등방 복귀 + 모드 되돌리면 W 복원(오염 방지). **오프라인 스윕 결과 사용자 가설의 절반만 성립**: 레버는 `cross_scale` 하나고(fillet 0.15·0.10 m/s에서 컷 4.8→1.2 mm −74 %, 랩시간 불변), `along_scale`을 내리는 건 거의 효과 없고 waypoint(fillet 0) 미션에선 +57 %로 **악화**; fillet 0에서 남는 오차는 최적화기의 선택이 아니라 참조 실현불가 바닥이라 가중치로 못 옮긴다. 플랜트가 컨트롤러 예측모델이라 전부 [예측]. [KNOWN_ISSUES 1건 추가] [memory: mpc-tuned-path-frame-cost]
- 2026-08-18 [none — 구현] Q: station 모드에 z도 넣어달라(처음 눌렀을 때의 z로) → **z는 원래부터 3축 다 걸려 있었다**(`_arm_path`의 `set_target_ned((x,y,depth),yaw)`, depth는 `square.depth_ned`가 null이면 START 순간의 `_eta[2]`). 진짜 결함은 **그 숫자가 화면 어디에도 없었다**는 것 — `err_xy`가 정의상 수평이라 20 cm 가라앉은 hold가 완벽한 hold로 읽혔다. 조종사 결정: depth 결정 로직은 그대로(매 START의 z), **패널에 표시만 추가**. MPC chip에 `· z -1.05 m (+3 cm)`, 플롯 오차 줄에 `dz`. 두 규약을 테스트로 고정 — z는 MAP 프레임(datum z 복원, 없으면 `z_d`로 이름을 바꿔 datum 상대임을 밝힘), NED 아래가 +라 괄호 +는 setpoint보다 더 깊다는 뜻(chip 툴팁에 명시). `test_offline.py :: test_the_panel_says_what_depth_is_being_held`, 79/79.
- 2026-08-18 [none — 구현] Q: station에서 태그를 놓치면 컨트롤러가 꺼진다, IMU로 버티게 하자 → **STATION BRIDGE 구현**(`rov_gui/control/station_bridge.py`, station 전용). 먼저 밝힌 사실 둘: (1) 꺼지는 이유는 `_runtime_fault`가 `meas`(태그=ground truth)를 보기 때문이라 **`imu_dr: control`을 켜도 안 풀린다** — 분리해야 할 건 "태그 vs IMU"가 아니라 "추정 상실 vs 제어 포기"; (2) disengage는 **깊이 유지까지 끄고** 순부력 −5.7 N이라 가라앉아 재획득을 더 어렵게 만든다. 설계는 축별 사다리(z=압력·roll/pitch=AHRS는 **절대**라 무제한, yaw=자이로는 느린 드리프트, x/y=이중적분만 t²): 0~3 s는 전 축을 bridge 추정치로, 그 뒤 **x/y/yaw 해제하고 깊이+자세만 무제한 유지**(coast는 disengage가 아님 — 드리프팅 추정치로 미는 것보다 흘려보내는 게 안전하고, 지오펜스가 없어서 더욱). 구현 결정 넷: bridge 추정기는 **자체 인스턴스를 매 fix마다 재앵커**(imu_dr은 재앵커 안 하는 게 목적이라 별개) + `attitude="vehicle"`; `xy_source: auto`는 캘리브 없으면 **x/y 고정**으로 폴백하고 그 경로는 **C3 IMU 없이도 동작**; 화이트리스트는 접두어가 아니라 **단어 경계**(`imu stale`/`pressure depth stale`/`no vehicle imu`는 bridge 대상 아님); 복구 시 **드리프트를 로그에 남겨** [예측] IMU 예산을 [측정]으로 바꾼다. CSV `bridge_s`/`bridge_tier` + meta schema **4→5**가 기록 경계. `test_station_bridge.py` 15/15, 무회귀(control 79/79, offline 79/79, imu_dr 25/25, plot_runs 17/17). **실기 미검증 + 문제가 난 로그를 아직 못 봄**(2026-08-18 8개 런 최악 dropout 0.55 s로 기존 1.1 s 예산 안). [KNOWN_ISSUES 1건 추가] [memory: station-tag-dropout-bridge]
- 2026-08-18 [none — 구현] Q: station에서 그 자리에 더 빨리 왔으면 좋겠다(mpc/pid 둘 다) → **추력이 아니라 leash가 속도를 정하고 있었다.** 실측(접근 4회): `|axis|` p50 0.18, **cap 0.50에 닿은 tick 0.0 %** — 권한을 3분의 1도 안 씀. `_tick_approach`가 `set_target_ned`를 **속도 FF 없이** 부르고 있어서(path follower는 늘 넘겨왔다) 정상상태가 `kp·lead = 51.7×0.35 = 18.1 N` → 실측 전루프 `F = 86.7v + 5.76` → **v = 0.084 m/s** [유도]로 잡혔고 실측 0.079~0.102와 일치. 셋을 같이 고침(하나만 바꾸면 2026-08-17의 "루프에 안 들어간 속도 숫자" 반복): (1) setpoint 자신의 속도를 `v_ned`로 전달하되 **컨트롤러 선견거리로 테이퍼**(NMPC는 v_ref로 호라이즌 끝까지 외삽하므로 목표 한 호라이즌 안에서 전속 FF는 "지나쳐 가라"는 뜻; PID는 1 stage라 무영향 → 비교 유지), (2) `approach_speed_m_s 0.10→0.20` + `approach_lead_m 0.35→0.50`(같은 식으로 leash 상한 0.232 m/s, 최대 명령 axis 0.43로 cap 안), (3) **station은 settle 생략**(`settle_station_s: 0.0`) — 직관과 반대로 settle 동안 setpoint는 아직 leash에 묶여 있고 arming이 그 leash를 푸므로 settle은 **느린 쪽 상태로 10 s 더 붙잡아 두는 것**이고, station은 settle 뒤 시작할 것도 없다(armed 타깃 = 접근이 걸어가던 그 점); path는 유지, `imu_dr` 켜지면 static window 때문에 자동 복귀. 예상 0.4 m 23 s→~2 s, 1.3 m 32 s→~7 s [예측]. 마지막 20 cm는 여전히 데드밴드(|axis| 0.066 < 문턱 0.096) — 게인은 조종사 결정으로 안 건드림. engage 기본값을 MpcConfig에도 넣어 hand-built config와 배포 config를 일치시킴. test_control 81/81, 전 스위트 무회귀. [memory: approach-speed-leash-limited]
- 2026-08-21 [none — 구현] Q: gui-twinkly-plum.md대로 물체 추종을 만들어달라 → **`--pose` × `--mpc` 다리 구축 완료(실기 미검증)**. 스테이션에는 서로 모르는 6-DoF 추정기가 둘 돌고 있었고(기체=`bus.nav_fix`, 물체=`bus.pose`의 `T_cam_obj`) 잇는 코드가 하나도 없었다 — pose 로그가 *"NO camera->body transform is applied"*로 못박아 둔 그대로. 새 순수 모듈 `rov_gui/control/object_nav.py`가 **같은 카메라 프레임**의 두 추정치를 합성해 물체를 MAP 프레임에 올린다. 설계의 중심은 **extrinsic 소거**: `TagNav._solution`이 extrinsic을 나눠 body pose를 만들고 이쪽이 다시 곱하므로 같은 프레임이면 정확한 역연산이라 **아직 미실측인 `cam_t_flu` lever arm 0.2855 m가 통째로 무효화**된다(KNOWN_ISSUES 2026-08-17). 프레임이 어긋나면 잔차 `|t_frd_cam|·2sin(θ/2)`(5°=2.5 cm)라 `pair_exact`가 패널·SENSORS·CSV에 **상설 슬롯**을 갖는다 — 소거가 멈췄다는 유일한 경고. 두 메일박스가 `_tap_pose`에서 같은 `t_capture` float을 받으므로 **부동소수 동등비교가 정당한 테스트**다. 합성은 raw `NavFix`만 쓴다(`meas["eta"]`는 압력계 z + AHRS roll/pitch + 속도전파 x/y의 하이브리드라 소거가 조용히 깨진다). `follow` 미션은 START 순간의 오프셋을 **물체 yaw 프레임**에 저장해 병진→병진 / yaw→궤도선회 / roll·pitch→무반응(MANUAL_CONTROL에 K/M 축이 없다); 오프셋을 현재 pose에서 뜨므로 **arm이 기체를 절대 안 움직인다**(approach·settle 없음, 테스트가 못박음). 안전장치 4겹(이탈 클램프=거부가 아닌 **참조 클램프** 1.5 m / 레이트 walk / **결과에 거는** 하드 leash / 선견거리 테이퍼 FF — FF 없으면 leash가 속도를 정해 0.084 m/s에 갇힌다, memory: approach-speed-leash-limited), 신선도 사다리는 `PoseTrack.state`를 봐야 한다(10 Hz로 계속 발행하므로 30초짜리 `registering`도 stamp는 신선 — **age만 보면 절대 못 잡는다**). 태그 dropout은 **먼저 강등, 그 다음 bridge** — 태그를 놓치면 물체도 같이 안 보이므로(합성이 `NavFix` 기반) 두 표류량을 빼는 최악을 피한다; `station_bridge.py`는 문구만 바뀜. 발견한 기존 결함 3건: `HwMpcc.set_target_ned`가 `del v_ned, r_ned`(→ `follow_ok = False`로 mpcc 거부), `HwDobMpc`의 `r_ned`가 `yaw_target=None` 탓에 no-op(→ `r_ned` 안 넘김), **`MpcConfig.load`가 `square.shape`를 검증 안 해 오타가 조용히 사각형을 날렸다(이건 고침)**. 부수로 `window._pose3d_shown` 미초기화 크래시도 고침 — 실제 6-DoF pose가 오는 첫 프레임에 `_on_pose`가 죽었다(오프라인에서 진짜 pose를 만든 적이 없어 잠복). CSV 10열 + meta schema **5→6**(`obj_pair_exact == 1`인 행만 extrinsic-free = 기록 경계), demo 물체는 MAP 프레임 역투영 + stamp 공유라 `pair_exact`가 오프라인에서 실제로 검사된다. `test_object_nav.py` 28/28, 무회귀(control 81/81, offline 79/79, station_bridge 15/15, imu_dr 27/27, path_cost 15/15, plot_runs 17/17), `demo_e2e pid follow {still,drift,orbit}` 전부 통과(pair_exact 100 %). **실기 미검증이고 `--pose`가 실기에서 안 돌았다는 위험을 통째로 상속**; 가장 싼 go/no-go는 "태그 ≥2장과 물체가 한 프레임에 0.3~0.8 m로 들어오는가" — 안 되면 답은 코드가 아니라 두 번째 카메라나 재틸트다. [KNOWN_ISSUES 3건 추가] [memory: object-follow-pose-mpc]
- 2026-08-21 [none — 구현/실기 진단] Q: 실기 첫 런에서 (1) 물체 view 수집이 취소되고 (2) 태그 9장이 보이는데 trajectory에 위치가 안 뜨고 (3) MAP 창이 왜 있는지 모르겠고 (4) trajectory를 3D로 보고 싶다 → **넷 다 처리**. (1) 취소 경로가 둘인데 로그가 구분해 준다: `stopped after N deg with only M usable views`=arc 예산(빨리 돌리면 프레임이 거부당하는 사이 75°가 지나감; **올리면 안 됨** — 상류 실측 per-view 오차가 80°/9.7 mm → 100°/87 mm → 120°/283 mm 절벽), `could not reacquire`=SAM2가 마스크를 놓치고 재획득 15 s 초과. 사용자 로그는 **후자**였고 그 15 s가 CLI에 없었다 → `--pose-lost-timeout`/`--pose-lost-grace` 노출. (2) **표시 결함 2건**: `TrajectoryView.add_status`가 engage 여부와 무관하게 `p_act=None`을 20 Hz로 써서 `add_fix`가 방금 세운 선체 마커를 지웠고(비-engage에서는 add_fix가 소유자), 실패 이유가 갈 곳 둘이 다 안 보였다 — SENSORS의 `TagNav` 행은 `SENSOR_ORDER` 밖이라 8행 캡에 잘렸고(게다가 행이 **제거된 적이 없어** 캡보다 많이 그려지며 마지막 값이 얼어붙었다), chip에 쓴 nav 메시지는 `add_status`가 50 ms 뒤 덮었다. 고침: `_set_p_act`/`_act_fresh`로 **소유권과 나이를 한 곳에** + `add_status`는 engage 중에만 지움, nav note를 2 s 기억해 chip에 유지, 플롯에 **`NO FIX — <이유>`** 줄(빨강), `SENSOR_ORDER`에 TagNav/Object/MPC 추가 + 캡 8→10 + 캡 밖 행 숨김. (3)(4) **MAP 창 제거, trajectory에 `3D` 버튼**: 같은 플롯을 기울여 풀·태그맵·궤적·참조·DR·물체를 각자의 z에 그리고 마커마다 태그면까지 점선을 내린다(직교투영 — 풀장 스케일에서 원근은 등거리를 다르게 보이게만 한다). 핵심 성질은 **elev 90°에서 위에서 본 뷰와 픽셀 단위로 동일**(테스트로 고정) — 버튼을 눌러도 세계가 안 움직인다. 구현 중 부호 버그 1건 잡음: 방위 0에서 눈을 +x에 두면 elev 90 축약은 만족하면서 기울인 순간 **세계가 위아래로 뒤집힌다**(월드 up이 화면 아래로) → 눈을 −x 쪽으로. trail 튜플이 `(t,x,y)`→`(t,x,y,z)`로 확장(소비자 전부 갱신). `pose3d.py` 삭제, video 패널 `MAP` 버튼 제거. 부수로 `_pose3d_shown` 미초기화 크래시는 창 자체가 사라져 소멸. 테스트: offline 81/81(3D 축약·양 모드 페인트·NO FIX 회귀 3건 신규), control 81/81, object_nav 28/28, 나머지 무회귀. **실기 재확인 필요** — (2)에서 로컬라이저가 정말 거부 중이었는지는 이제 화면이 말해 준다.
- 2026-08-24 [workflow: experiment-diagnostic ×2 + video + code + verifier] Q: follow 두 런(0823_213109, ui_213602/ui_213800)이 잘되다 한번 튀고 전멸하는 원인 → 물체 정지·태그 fix 정상·depth-scale 아님(전부 적대검증으로 배제). 금지 메시(333 vs 120 mm) 위 FoundationPose가 yaw 쌍안정→슬라이드→1.32 m 유령 스냅으로 붕괴한 것이 트리거; `yaw_axis:"none"`이 auto로 동작하는 pinning 버그(meta yaw_axis_used="x")로 유령 yaw가 헤딩 참조+무상한 FF(ref_speed 3.15 m/s, 호라이즌 외삽)에 1:1 유입돼 기체가 실제 0.54 m/−50° 끌려감(nav fixes 교차 확인); run3는 실현불가 참조로 acados status 4→IPOPT 폴백 lazy build가 20 Hz 워커를 ~6.5 s 블록→deadman/워치독 해제. KNOWN_ISSUES 5건 추가. [memory: follow-hw-failure-modes]
- 2026-08-24 [workflow: safety-code-reviewer + control-theory-advisor + regression + verifier] Q: follow 실패 수정(yaw_axis 버그·FF 상한·reseed 강등·IPOPT 블록)을 검토해달라 → **FAIL 판정, CRITICAL 2건이 내 수정에 없었다**: (1) `set_traj(False)`의 `if self.traj_on` 가드가 follow를 통째로 건너뛰어 STOP TRAJ가 **v_ned를 남긴 채** "정지"라고 표시 — `_xref_ned`가 매 틱 v_ref로 호라이즌을 재생성하므로 기체가 계속 명령받는다(내 FF 상한은 3.15→0.30 m/s로 낮췄을 뿐 메커니즘을 안 없앰); (2) excursion clamp는 goal만 묶어서 leashed 상태에서 FF가 영구히 바깥을 향함. 추가로 `engage:`가 유일하게 검증 없는 안전 블록이라 `follow_ff_max_m_s: 0`이 "무상한"으로 읽히고(=사고 재현), tick 인터록이 3개 컨트롤러 중 1개만 커버. 전부 수정 후 재검증. **내 첫 escalation 설계 결함도 자체 발견**: |v_ff| 기준으로 강등을 걸었더니 orbit 테스트가 죽었는데, 범인은 물체 속도가 아니라 **기체가 뒤처져 생긴 catch-up 항**이었다 → 판정 대상을 물체 자체 속도(v_track)로 교체, 임계는 3×cap(실측 유령 10×, 정상 orbit 0.7×라 여유 3배). 테스트 +9, rov_gui 전 스위트 무회귀(object_nav 35/36, control 81/81, offline 92/92, 나머지 그대로; red 2건은 사전 존재 config-vs-test), demo_e2e pid follow {still,drift,orbit} 전부 `['following']`만으로 통과. meta schema 6→7. [KNOWN_ISSUES 3건 삭제·4건 추가] [memory: follow-hw-failure-modes]
- 2026-08-24 [none — 구현] Q: 나쁜 메시인지 views 모을 때 알 수 없나(항상 실험 다 하고 알려줘서 번거롭다) → **이미 알려주고 있었다**: `depth_quality`가 마스크 안 depth 산포 vs 마스크 면적이 함축하는 크기를 비교해 0823_213109에서 "3.3x"를 **메시 만들기 33초 전, follow 4분 30초 전**에 찍었다(21:31:33). 결함은 셋: (1) 경고일 뿐 재구성을 안 막음 — mesh check의 "Do NOT fly this mesh"까지 합쳐 **두 번 경고하고 두 번 다 통과**시켰다, (2) `_finish_capture`에서 한 번만 계산해 궤도 도는 중엔 안 보임(고칠 수가 없음), (3) `CAPTURE_RANGE_M=(0.25,1.00)`이 자기 안내문 "30-80 cm"보다 헐렁해 639-912 mm 캡처가 한 프레임도 안 걸렸다. 셋 다 구현: 프레임 단위 게이트(`_frame_depth_quality`를 submit 전에) + 라이브 SMEAR 표시(snapshot→PoseTrack→video chip) + 재구성 거부. **임계값은 아카이브 실측으로 잡았다** — `sessions/pose_meshes/*` 14개 캡처를 shipped 함수로 재채점: usable p50 최대 1.53 / unusable p50 최소 2.44라 **2.0**이 그 간극에 들어가고, 프레임 단위로는 2.0에서 usable이 **0%** unusable이 **88-100%** 탈락이라 같은 수가 양쪽에 쓰인다(비율은 spread/size라 scale-free — `--depth-scale`도 물체 실제 크기도 안 움직인다). 처음 문서 표만 보고 고른 1.8/1.5는 **틀렸다**(리포 자신이 usable이라 부른 1.53 캡처를 거부). 뷰 부족 메시지도 smear 거부 수를 말하게 고침. test_offline +2(94/94), 전 스위트 무회귀. [memory: follow-hw-failure-modes]
- 2026-08-24 [none — 실기 진단/구현] Q: 물체가 태그 58에 있는데 48/49쯤에 보인다 → **`--depth-scale` 미지정**. nav map.json으로 재투영해 확정: 보고 위치가 태그 11(2 cm)/10/52 근처, **매트 밑 51~53 cm**, 기체→물체 실제 1.57 m vs 보고 2.47 m = **과대 배율 1.55x/1.57x** (두 런 독립 일치) — 물속 스테레오 배율 1.562 그 값. 0823 로그엔 "DEPTH SCALED x0.64"가 있고 오늘은 없음, `__main__.py` default=1.0이었음. 메시·smear·실루엣 게이트는 전부 정상 통과했는데 **비율이 scale-free라 균일 배율 오차는 설계상 사각지대**(문서에 명시된 대로). 사용자 가설("뷰 수집도 물속에서 해야")은 **오늘 건 아님** — 배율이 물속 값이고 617/976행 동안 산포 ±4%로 안정적인 건 메시 배율과 라이브 depth 배율이 **일치**한다는 뜻(다르면 FoundationPose가 싸워서 불안정)이라 캡처도 물속이었다. 다만 그 직관의 알맹이는 유효: **메시는 캡처 시점 배율을 영구히 굽는다** → 보정 켜고 → 재캡처 → 비행 순서 강제, 오늘 메시는 폐기. 수정: `--depth-scale` 기본값 **1.0 → 0.64**, backend에 `DEPTH_SCALE_DEFAULT` 단일 정의(CLI와 드리프트 방지), 로그가 default/플래그 출처를 구분, 회귀 테스트 1건(96/96). map-frame 타당성 게이트는 사용자 요청으로 **보류**(KNOWN_ISSUES에 기재). [memory: c3-stereo-depth-not-metric]
- 2026-08-30 [workflow: umi-manipulation-advisor + control-theory-advisor + 코드리더 3] Q: diffusion policy를 reference trajectory 소스로 (obs=depth+proprio, 1 Hz replan → 기존 MPC 추종) — 계획 수립 → UMI-on-Air 패턴으로 설계. 핵심 결론: action은 body-frame-at-t0 상대 waypoint 16×0.25 s(=4 s, replan 1 s+MPC preview 3 s 커버), 라벨은 EXECUTED 궤적(TagSLAM 좌표, spline 평활), goal-click heatmap 채널 유지 강권(UMI-U Table II: goal-agnostic는 background shift 0%), follower는 MPCC 아닌 NMPC(`set_reference_traj`; MPCC는 끝단 ray 외삽+타이밍 폐기로 부적합), 컨트롤러 앞에 PlanFilter+PlanStitcher(새 plan은 측정 상태 아닌 현재 레퍼런스에 앵커, commit-then-blend, 지오펜스 제거 후라 필터의 작업공간 박스가 유일한 위치 방호). 기록 갭 2대: run 폴더에 uint16 depth 부재(표시용 mp4뿐) + CSV 절대시계 부재. 계획: docs/DP_TRAJECTORY_PLAN.ko.md (초안, D1-D5 사용자 결정 대기) [memory: dp-trajectory-plan]
- 2026-08-30 [none — 결정 확정 + Explore 2] Q: DP 궤적 계획의 결정 5+3건 확정 → D1 goal 채널 제외(단일 바위라 유예, multi-object 시 재점화)·D2 태스크=바위 접근+파지(태스크당 모델 1)·D3 yaw 포함·D4 시연=UMI handheld(teleop/gantry 제외)·D5 w_hat 제외·gripper=policy action 포함(action 16×5=80-dim)·**M0 replay 우선**(데모 1개→라벨→실기 replay 게이트→대량 수집)·UI=SHAPES에 replay/policy 엔트리(follow 패턴). Explore 결론 2건: (1) handheld pose-라벨은 글루 ~40줄(CameraModel.K 왜곡0 pinhole + TagNav + C3 extrinsic 합성 + zarr 사이드카), 단 기존 demo 25개는 환경 태그 미기록이라 라벨 불가 추정 + gripper 태그 id 0/1이 tag_map_full의 170mm 태그와 충돌(~8× 스케일 함정, tag_map.yaml 사용) + IMU 없어 min_tags≥2; (2) gripper 송신 경로 이미 존재(cmd_gripper_drive→BTN 76/77, latched level, MPC engage 중 비차단), 갭 6건(중재 부재/피드백 전무/demo 백엔드 드랍/servo 파라미터 미기록/sim 인터페이스 불일치/hold-bridge 동결 규칙). 계획 v0.2: docs/DP_TRAJECTORY_PLAN.ko.md [memory: dp-trajectory-plan]
- 2026-08-30 [none — 구현] Q: DP 계획의 M0 크리티컬 패스를 코드로 → **replay 미션 랜딩**: `umi_handheld/extract_pose.py`(TagNav 재사용 오프라인 pose 라벨, 8/8), `rov_gui/control/plan_stream.py`(PlanFilter/PlanStitcher/replay 트랙, 순수 numpy, 19/19), SHAPES에 `replay`(follow 패턴: arm이 기체를 안 움직임, 현재 pose 앵커, 시간 팽창 속도 제한, one-shot(M0a)+streamed(M0b) 두 모드, gripper 채널은 기존 cmd_gripper_drive 재생·기본 OFF), meta schema 7→8 + plans.jsonl + CSV plan_id/ref_src/grip_cmd. 검증: test_replay 9/9 + 전 스위트 무회귀(red 2건은 사전 존재 config-vs-test) + demo_e2e dobmpc replay 실솔버 완주(honest 판정). safety 감사 FAIL→수정은 reviews.md 참조. 실기 미검증 — 다음 단계 = 태그 배열 앞 데모 1개 촬영 → extract_pose → 실기 replay [memory: replay-mission-plan-stream]
