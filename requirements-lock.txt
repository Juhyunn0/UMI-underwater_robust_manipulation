# Reproducible lock for the merged UMI Underwater Robust Control environment.
# Captured from the `robust` conda env (Python 3.14.4). Covers BOTH subprojects:
#   - Refractive TagSLAM + gantry  (src/)
#   - BlueROV2 DOBMPC MuJoCo sim   (bluerov2_mujoco_dobmpc/)
#
# numpy is pinned to 1.26.4: gtsam 4.2.1 requires numpy<2.0.0 (hard ABI pin).
# pyzed 5.2's metadata asks for numpy>=2.0, so `pip check` prints a benign
# warning, but pyzed.sl imports and runs correctly on 1.26.4 (verified). Do
# not raise numpy to 2.x — it breaks gtsam.

# --- Core scientific stack ---
numpy==1.26.4
scipy==1.17.1
opencv-python==4.10.0.84

# --- AprilTag + SLAM (src/) ---
pupil_apriltags==1.0.4.post11
gtsam==4.2.1

# --- BlueROV2 DOBMPC MuJoCo sim (bluerov2_mujoco_dobmpc/) ---
mujoco==3.9.0
casadi==3.7.2
glfw==2.10.0
PyOpenGL==3.1.10
absl-py==2.4.0
etils==1.14.0
fsspec==2026.4.0
zipp==4.1.0

# --- Plotting ---
matplotlib==3.10.9
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
pillow==12.2.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0

# --- Gantry control GUI (src/gantry_panel.py, src/gui_launcher.py) ---
PyQt5==5.15.11
PyQt5-Qt5==5.15.19
PyQt5_sip==12.18.0
pyqtgraph==0.14.0
QDarkStyle==3.2.3
QtAwesome==1.4.2
QtPy==2.4.3

# --- Misc / transitive ---
PyYAML==6.0.3
colorama==0.4.6
packaging==26.2
six==1.17.0
typing_extensions==4.15.0
munkres==1.1.4
Cython==3.2.4

# --- ZED SDK Python bindings (NOT on PyPI) ---
# Installed from the local vendor wheel, not pip-resolvable on most systems:
#   pip install pyzed-5.2-cp314-cp314-linux_x86_64.whl
# pyzed==5.2
